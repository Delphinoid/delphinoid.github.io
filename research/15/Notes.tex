\input{preamble.tex}

\begin{document}

\thispagestyle{fancy}

\begin{center}
\LARGE\scshape Classification of Fusion Categories\noindent\\[-\linespacing]
\rule{0.75\linewidth}{1pt}
\end{center}
\noindent\\[-0.75\linespacing]

\ruledsection{Prologue}{1}
\noindent\\ What are fusion categories? What are near-groups, Haagerup--Izumi categories and quadratic\linebreak categories? What is modular data? What are $6j$ symbols? What is the even part of a subfactor? I think we need to assume that either $\textup{char}(\mathbbm{k}) \nmid \abs{G}$ or $\textup{char}(\mathbbm{k}) = 0$ for the Schur orthogonality relations.\\

\noindent\textcolor{red}{Need exposition in the deconstruction section saying that we follow Izumi's work closely using a categorical / non-unitary language.}\\

\noindent\textcolor{red}{Outline stuff: Introduction (history, why we care, overview of the paper), Preliminaries (basic definitions), Deconstruction, Irrational Reconstruction, General Reconstruction, Examples, Appendix, References}\\

\noindent\textcolor{red}{1. Introduction\\
	a. History\\
	b. Motivation (why we care)\\
	c. Overview (what we want to do and how we'll do it, c.f. unitary setting)\\
2. Preliminaries\\
	a. Fusion Categories\\
		i.   Definition\\
		ii.  6j-Symbols\\
		iii. Traces and the Quantum Dimension\\
		iv.  Algebra Objects and Subfactor Theory\\
		v.   Quadratic Categories (near-groups and Haagerup--Izumis)\\
		vi.  Properties (all near-groups are spherical)\\
	b. Leavitt Algebra Endomorphisms\\
		i.   Definition of End(A)\\
		ii.  Properties (linear tensor cat, with 1-dimensional hom-space for unit, absolutely simple objects, rigid objects)\\
		iii. Example: Yang--Lee Model\\
		iv.  Idempotent Completion (and why it's necessary)\\
3. Deconstruction\\
	a. Classifying Data\\
	b. Invariants\\
4. Reconstruction\\
5. The Case of m = 0\\
6. Examples\\
7. References
}\newpage

\noindent Let $\mathcal{C}$ be a skeletal fusion category over $\mathbbm{k}$. Recall the Yoneda embedding
\begin{align*}
\begin{split}
\yo_*(X) \coloneqq \homset_\mathcal{C}(-, X)\qquad\text{and}\qquad\yo_*(f : X \to Y) \coloneqq (\homset_\mathcal{C}(-, X) \Rightarrow \homset_\mathcal{C}(-, Y)).
\end{split}
\end{align*}
\noindent Taking the Yoneda embedding of a component $\alpha_{X,Y,Z} : (X \otimes Y) \otimes Z \to X \otimes (Y \otimes Z)$ of the associativity natural isomorphism $\alpha$, we obtain a natural isomorphism
\begin{align*}
\begin{split}
\yo_*(\alpha_{X,Y,Z}) = (\homset_\mathcal{C}(-, (X \otimes Y) \otimes Z) \Rightarrow \homset_\mathcal{C}(-, X \otimes (Y \otimes Z))).
\end{split}
\end{align*}
\noindent Thus we have an isomorphism of vector spaces
\begin{align*}
\begin{split}
[\yo_*(\alpha_{X,Y,Z})](W) : \homset_\mathcal{C}(W, (X \otimes Y) \otimes Z) \xrightarrow{\sim} \homset_\mathcal{C}(W, X \otimes (Y \otimes Z));
\end{split}
\end{align*}
\noindent that is, an invertible matrix. In other words, the associativity is given by matrices indexed by $X, Y, Z, W$. But we can simplify things further! Suppose we now choose representatives $\{X_i\}_{i \in \Gamma}$ of isomorphism classes of simple objects and fix a choice of basis (gauge) for each {\em multiplicity space} $H_{i,j}^h := \homset_\mathcal{C}(X_h, X_i \otimes X_j)$. Then by semisimplicity,
\begin{align*}
\begin{split}
X_i \otimes X_j \cong \bigoplus_{h \in \Gamma} X_h^{\dim_\mathbbm{k}(H_{i,j}^h)},
\end{split}
\end{align*}
\noindent whence we have both
\begin{align*}
\begin{split}
\bigoplus_{m\in\Gamma} H_{m,i_3}^{i_0} \otimes_\mathbbm{k} H_{i_1,i_2}^m &= \bigoplus_{m\in\Gamma} \homset_\mathcal{C}(X_{i_0}, X_m \otimes X_{i_3}) \otimes_\mathbbm{k} \homset_\mathcal{C}(X_m, X_{i_1} \otimes X_{i_2})\\
&\cong \homset_\mathcal{C}(X_{i_0}, (X_{i_1} \otimes X_{i_2}) \otimes X_{i_3})
\end{split}
\end{align*}
\noindent and
\begin{align*}
\begin{split}
\bigoplus_{n\in\Gamma} H_{i_1,n}^{i_0} \otimes_\mathbbm{k} H_{i_2,i_3}^n &= \bigoplus_{n\in\Gamma} \homset_\mathcal{C}(X_{i_0}, X_{i_1} \otimes X_n) \otimes_\mathbbm{k} \homset_\mathcal{C}(X_n, X_{i_2} \otimes X_{i_3})\\
&\cong \homset_\mathcal{C}(X_{i_0}, X_{i_1} \otimes (X_{i_2} \otimes X_{i_3})).
\end{split}
\end{align*}
\noindent We thus obtain canonical bases for the above Hom-spaces. The {\em (quantum) $6j$-symbols of $\mathcal{C}$} are then defined to be the matrix blocks of the form
\begin{align*}
\begin{split}
\Phi_{i_1,i_2,i_3}^{i_0,m,n} : H_{m,i_3}^{i_0} \otimes_\mathbbm{k} H_{i_1,i_2}^m \to H_{i_1,n}^{i_0} \otimes_\mathbbm{k} H_{i_2,i_3}^n
\end{split}
\end{align*}
\noindent such that the block-diagonal matrices $\Phi_{i_1,i_2,i_3}^{i_0} := \bigoplus_{m\in\Gamma}\bigoplus_{n\in\Gamma} \Phi_{i_1,i_2,i_3}^{i_0,m,n}$ give a change-of-basis
\begin{align*}
\begin{split}
\Phi_{i_1,i_2,i_3}^{i_0} : \homset_\mathcal{C}(X_{i_0}, (X_{i_1} \otimes X_{i_2}) \otimes X_{i_3}) \cong \homset_\mathcal{C}(X_{i_0}, X_{i_1} \otimes (X_{i_2} \otimes X_{i_3}))%\xrightarrow{\sim} \homset_\mathcal{C}(X_{i_0}, X_{i_1} \otimes (X_{i_2} \otimes X_{i_3}))
\end{split}
\end{align*}
\noindent between the canonical bases. These matrices $\Phi_{i_1,i_2,i_3}^{i_0}$ themselves form the blocks of a block-diagonal matrix $\Phi_{i_1,i_2,i_3} \coloneqq \bigoplus_{i_0 \in \Gamma} \Phi_{i_1,i_2,i_3}^{i_0}$, which gives us a change-of-basis $(X_{i_1} \otimes X_{i_2}) \otimes X_{i_3} \cong X_{i_1} \otimes (X_{i_2} \otimes X_{i_3})$ describing how the associator acts on each summand $X_{i_0}$. As it happens, we in fact have that%\xrightarrow{\sim} X_{i_1} \otimes (X_{i_2} \otimes X_{i_3})$ describing how the associator acts on each summand $X_{i_0}$. As it happens, we in fact have that
\begin{align*}
\begin{split}
\Phi_{i_1,i_2,i_3}^{i_0,m,n} = v_n^{-1}(X_{i_0}, X_{i_1}, X_{i_2} \otimes X_{i_3}) \circ [\yo_*(\alpha_{X_{i_1}, X_{i_2}, X_{i_3}})]({X_{i_0}}) \circ u_m(X_{i_0}, X_{i_1} \otimes X_{i_2}, X_{i_3}),
\end{split}
\end{align*}
\noindent where
\begin{align*}
\begin{split}
[u_m(W, U, V)](f \otimes_\mathbbm{k} g) \coloneqq (g \otimes \id_V) \circ f\quad\text{and}\quad [v_n(W, U, V)](f' \otimes_\mathbbm{k} g') \coloneqq (\id_U \otimes g') \circ f'
\end{split}
\end{align*}
\noindent for all $f \in \homset_\mathcal{C}(W, X_m \otimes V)$, $g \in \homset_\mathcal{C}(X_m, U)$, $f' \in \homset_\mathcal{C}(W, U \otimes X_n)$ and $g \in \homset_\mathcal{C}(X_n, V))$.\\
\newpage

\noindent More explicitly, suppose we have an isomorphism
\begin{align*}
\begin{split}
\homset_\mathcal{C}(X_4, (X_1 \otimes X_2) \otimes X_3) \cong \homset_\mathcal{C}(X_4, X_1 \otimes (X_2 \otimes X_3)).%\xrightarrow{\sim} \homset_\mathcal{C}(X_4, X_1 \otimes (X_2 \otimes X_3)).
\end{split}
\end{align*}
\noindent Let $X_5$ and $X_6$ be simple summands of $(X_1 \otimes X_2)$ and $(X_2 \otimes X_3)$, respectively. Then we can determine our isomorphism by determining the block matrices of the form
\begin{align*}
\begin{split}
\homset_\mathcal{C}(X_4, X_5 \otimes X_3) \to \homset_\mathcal{C}(X_4, X_1 \otimes X_6)
\end{split}
\end{align*}
\noindent for all such $X_5$ and $X_6$. These matrices are exactly the $6j$ symbols, where the six simple objects $X_1, X_2, X_3, X_4, X_5, X_6$ play the role of the eponymous ``six $j$'s''. This is also where $u$ and $v$ come in; given $f \in H_{5,3}^4$ and $g \in H_{1,2}^5$, we have $[u_5(X_4, X_1 \otimes X_2, X_3)](f \otimes_\mathbbm{k} g) : X_4 \to (X_1 \otimes X_2) \otimes X_3$, and similarly $[v_6(X_4, X_1, X_2 \otimes X_3)](f' \otimes_\mathbbm{k} g') : X_4 \to X_1 \otimes (X_2 \otimes X_3)$ for $f' \in H_{1,6}^4$ and $g' \in H_{2,3}^6$.\\

%\noindent Note that these matrices are indeed parameterized by all ``six $j$'s'', as there could be many invertible matrices that give us a maps of the aforementioned form. We need $X_2$ in order to use the pentagon diagram for the associativity constraint and hence determine the specific invertible matrix\linebreak corresponding to the associator. Moreover, $X_2$ tells us how the blocks
%\begin{align*}
%\begin{split}
%\homset_\mathcal{C}(X_4, X_5 \otimes X_3) \to \homset_\mathcal{C}(X_4, X_1 \otimes X_6)
%\end{split}
%\end{align*}
%\noindent fit together into the block diagonal matrix
%\begin{align*}
%\begin{split}
%\homset_\mathcal{C}(X_4, (X_1 \otimes X_2) \otimes X_3) \xrightarrow{\sim} \homset_\mathcal{C}(X_4, X_1 \otimes (X_2 \otimes X_3)).
%\end{split}
%\end{align*}
%\noindent\\[-\linespacing]

\noindent\begin{example}\textup{($\textcat{Vec}_{\mathbb{Z}/2\mathbb{Z}}$).} Consider $\textcat{Vec}_{\mathbb{Z}/2\mathbb{Z}}$, a category with two isomorphism classes of simple objects -- $[\mathbbm{1}]$ and $[X]$ -- satisfying the fusion rule
\begin{align*}
\begin{split}
[X] \otimes [X] = [\mathbbm{1}].
\end{split}
\end{align*}
\noindent Let's assume that it's skeletal -- that is, $X \otimes X = \mathbbm{1}$ -- and start by computing the change-of-basis matrix $\Phi_{X,X,X}$. This matrix can be written in block diagonal form as
\begin{align*}
\begin{split}
\Phi_{X,X,X} = \begin{pmatrix}\Phi_{X,X,X}^\mathbbm{1}&0\\0&\Phi_{X,X,X}^X\end{pmatrix}.
\end{split}
\end{align*}
\noindent First, observe that
\begin{align*}
\begin{split}
\Phi_{X,X,X}^\mathbbm{1} : \homset(\mathbbm{1}, (X \otimes X) \otimes X) \to \homset(\mathbbm{1}, X \otimes (X \otimes X));
\end{split}
\end{align*}
\noindent but $(X \otimes X) \otimes X = X = X \otimes (X \otimes X)$, so $\Phi_{X,X,X}^\mathbbm{1}$ is just the isomorphism of $0$-dimensional vector spaces; that is, $\Phi_{X,X,X}^\mathbbm{1} = 0$. Let's now compute $\Phi_{X,X,X}^X$. %Denote $X \otimes X$ by $\widetilde{\mathbbm{1}}$.
We shall start by choosing bases for the multiplicity spaces $H_{X,X}^{{\mathbbm{1}}}$, $H_{{\mathbbm{1}},X}^X$ and $H_{X,{\mathbbm{1}}}^X$. These spaces are all $1$-dimensional, so we will choose basis elements $\iota_{X,X}^{{\mathbbm{1}}}$, $\lambda_X^{-1}$ and $\rho_X^{-1}$, respectively. Here we have a canonical choice of basis elements for $H_{{\mathbbm{1}},X}^X$ and $H_{X,{\mathbbm{1}}}^X$; namely, the left and right unitors $\lambda$ and $\rho$. This culminates in the basis elements
\begin{align*}
\begin{split}
\iota_{(X \otimes X) \otimes X}^X \coloneqq (\iota_{X,X}^{{\mathbbm{1}}} \otimes \id_X) \circ \lambda_X^{-1} = [u_{{\mathbbm{1}}}(X, X \otimes X, X)](\lambda_X^{-1} \otimes_\mathbbm{k} \iota_{X,X}^{{\mathbbm{1}}}),
\end{split}
\end{align*}
%\noindent and
\begin{align*}
\begin{split}
\iota_{X \otimes (X \otimes X)}^X \coloneqq (\id_X \otimes \iota_{X,X}^{{\mathbbm{1}}}) \circ \hphantom{\lambda}\nhphantom{$\rho$}\rho_X^{-1} = [v_{{\mathbbm{1}}}(X, X, X \otimes X)\hphantom{u}\nhphantom{$v$}](\rho_X^{-1}\hphantom{\lambda}\nhphantom{$\rho$} \otimes_\mathbbm{k} \iota_{X,X}^{{\mathbbm{1}}})\hphantom{,}
\end{split}
\end{align*}
\noindent for $\homset(X, (X \otimes X) \otimes X)$ and $\homset(X, X \otimes (X \otimes X))$, respectively. These are once again both $1$-dimensional spaces, so let's define a constant $\omega_1 \in \mathbbm{k}^\times$ for which
\begin{align*}
\begin{split}
\Phi_{X,X,X}^X(\iota_{(X \otimes X) \otimes X}^X) \eqqcolon \omega_1 \cdot \iota_{X \otimes (X \otimes X)}^X.
\end{split}
\end{align*}
\noindent To gain constraints on $\omega_1$, we must use the pentagon equation. To this end, consider the Hom-spaces
\begin{align*}
\begin{split}
\homset({\mathbbm{1}}, ({\mathbbm{1}} \otimes X) \otimes X),\\
\homset({\mathbbm{1}}, {\mathbbm{1}} \otimes (X \otimes X)),\\
\homset({\mathbbm{1}}, (X \otimes {\mathbbm{1}}) \otimes X),\\
\homset({\mathbbm{1}}, X \otimes ({\mathbbm{1}} \otimes X)),\\
\homset({\mathbbm{1}}, (X \otimes X) \otimes {\mathbbm{1}}),\\
\homset({\mathbbm{1}}, X \otimes (X \otimes {\mathbbm{1}})).
\end{split}
\end{align*}
\newpage
\noindent We can write bases for these Hom-spaces in terms of the bases we chose previously. That is,
\begin{align*}
\begin{split}
\iota_{({\mathbbm{1}} \otimes X) \otimes X}^{\mathbbm{1}} &\coloneqq (\lambda_X^{-1} \otimes \id_X) \circ \iota_{X,X}^{{\mathbbm{1}}},\\
\iota_{{\mathbbm{1}} \otimes (X \otimes X)}^{\mathbbm{1}} &\coloneqq (\id_{\mathbbm{1}} \otimes \iota_{X,X}^{{\mathbbm{1}}}) \circ \lambda_{\mathbbm{1}}^{-1},\\
\iota_{(X \otimes {\mathbbm{1}}) \otimes X}^{\mathbbm{1}} &\coloneqq (\rho_X^{-1} \otimes \id_X) \circ \iota_{X,X}^{{\mathbbm{1}}},\\
\iota_{X \otimes ({\mathbbm{1}} \otimes X)}^{\mathbbm{1}} &\coloneqq (\id_X \otimes \lambda_X^{-1}) \circ \iota_{X,X}^{{\mathbbm{1}}},\\
\iota_{(X \otimes X) \otimes {\mathbbm{1}}}^{\mathbbm{1}} &\coloneqq (\iota_{X,X}^{{\mathbbm{1}}} \otimes \id_{\mathbbm{1}}) \circ \lambda_{\mathbbm{1}}^{-1},\\
\iota_{X \otimes (X \otimes {\mathbbm{1}})}^{\mathbbm{1}} &\coloneqq (\id_X \otimes \rho_X^{-1}) \circ \iota_{X,X}^{{\mathbbm{1}}}.
\end{split}
\end{align*}
\noindent This culminates in the three new constants given by
\begin{align*}
\begin{split}
\Phi_{{\mathbbm{1}},X,X}^{\mathbbm{1}}(\iota_{({\mathbbm{1}} \otimes X) \otimes X}^{\mathbbm{1}}) &\eqqcolon \omega_2 \cdot \iota_{{\mathbbm{1}} \otimes (X \otimes X)}^{\mathbbm{1}},\\
\Phi_{X,{\mathbbm{1}},X}^{\mathbbm{1}}(\iota_{(X \otimes {\mathbbm{1}}) \otimes X}^{\mathbbm{1}}) &\eqqcolon \omega_3 \cdot \iota_{X \otimes ({\mathbbm{1}} \otimes X)}^{\mathbbm{1}},\\
\Phi_{X,X,{\mathbbm{1}}}^{\mathbbm{1}}(\iota_{(X \otimes X) \otimes {\mathbbm{1}}}^{\mathbbm{1}}) &\eqqcolon \omega_4 \cdot \iota_{X \otimes (X \otimes {\mathbbm{1}})}^{\mathbbm{1}}.
\end{split}
\end{align*}
\noindent The final bases we need are for the Hom-spaces
\begin{align*}
\begin{split}
\homset({\mathbbm{1}}, ((X \otimes X) \otimes X) \otimes X),\\
\homset({\mathbbm{1}}, (X \otimes (X \otimes X)) \otimes X),\\
\homset({\mathbbm{1}}, (X \otimes X) \otimes (X \otimes X)),\\
\homset({\mathbbm{1}}, X \otimes ((X \otimes X) \otimes X)),\\
\homset({\mathbbm{1}}, X \otimes (X \otimes (X \otimes X))).
\end{split}
\end{align*}
\noindent Following the same procedure as before, we have
\begin{align*}
\begin{split}
\iota_{((X \otimes X) \otimes X) \otimes X}^{\mathbbm{1}} &\coloneqq (\iota_{(X \otimes X) \otimes X}^X \otimes \id_X) \circ \iota_{X,X}^{{\mathbbm{1}}} = ((\iota_{X,X}^{{\mathbbm{1}}} \otimes \id_X) \otimes \id_X) \circ \iota_{({\mathbbm{1}} \otimes X) \otimes X}^{\mathbbm{1}},\\
\iota_{(X \otimes (X \otimes X)) \otimes X}^{\mathbbm{1}} &\coloneqq (\iota_{X \otimes (X \otimes X)}^X \otimes \id_X) \circ \iota_{X,X}^{{\mathbbm{1}}} = ((\id_X \otimes \iota_{X,X}^{{\mathbbm{1}}}) \otimes \id_X) \circ \iota_{(X \otimes {\mathbbm{1}}) \otimes X}^{\mathbbm{1}},\\
\iota_{(X \otimes X) \otimes (X \otimes X)}^{\mathbbm{1}} &\coloneqq (\iota_{X,X}^{{\mathbbm{1}}} \otimes (\id_X \otimes \id_X)) \circ \iota_{{\mathbbm{1}} \otimes (X \otimes X)}^{\mathbbm{1}} = ((\id_X \otimes \id_X) \otimes \iota_{X,X}^{{\mathbbm{1}}}) \circ \iota_{(X \otimes X) \otimes {\mathbbm{1}}}^{\mathbbm{1}},\\ %(\iota_{X,X}^{{\mathbbm{1}}} \otimes \iota_{X,X}^{{\mathbbm{1}}}) \circ \lambda_{\mathbbm{1}}^{-1},\\
\iota_{X \otimes ((X \otimes X) \otimes X)}^{\mathbbm{1}} &\coloneqq (\id_X \otimes \iota_{(X \otimes X) \otimes X}^X) \circ \iota_{X,X}^{{\mathbbm{1}}} = (\id_X \otimes (\iota_{X,X}^{\mathbbm{1}} \otimes \id_X)) \circ \iota_{X \otimes ({\mathbbm{1}} \otimes X)}^{\mathbbm{1}},\\
\iota_{X \otimes (X \otimes (X \otimes X))}^{\mathbbm{1}} &\coloneqq (\id_X \otimes \iota_{X \otimes (X \otimes X)}^X) \circ \iota_{X,X}^{{\mathbbm{1}}} = (\id_X \otimes (\id_X \otimes \iota_{X,X}^{{\mathbbm{1}}})) \circ \iota_{X \otimes (X \otimes {\mathbbm{1}})}^{\mathbbm{1}}.
\end{split}
\end{align*}
\noindent The pentagon diagram thus gives us
\begin{center}
\begin{tikzcd}[arrows={|->}, row sep=1.2cm, column sep=2cm]
& \iota_{((X \otimes X) \otimes X) \otimes X}^{\mathbbm{1}}\arrow[dl, "\alpha_{X,X,X} \otimes \id_X"']\arrow[dr, "\alpha_{X \otimes X,X,X}"] &\\
\omega_1 \cdot \iota_{(X \otimes (X \otimes X)) \otimes X}^{\mathbbm{1}}\arrow[d, "\alpha_{X,X \otimes X,X}"'] & & \omega_2 \cdot \iota_{(X \otimes X) \otimes (X \otimes X)}^{\mathbbm{1}}\arrow[d, "\alpha_{X,X,X \otimes X}"]\\
\omega_1\omega_3 \cdot \iota_{X \otimes ((X \otimes X) \otimes X)}^{\mathbbm{1}}\arrow[r, "\id_X\otimes\alpha_{X,X,X}"'] & \omega_1^2\omega_3 \cdot \iota_{X \otimes (X \otimes (X \otimes X))}^{\mathbbm{1}}\arrow[r, equal] & \omega_2\omega_4 \cdot \iota_{X \otimes (X \otimes (X \otimes X))}^{\mathbbm{1}}\\
\end{tikzcd}.
\end{center}
\newpage
\noindent This gives us the relation $\omega_1^2\omega_3 = \omega_2\omega_4$. However, there could be more constraints on these constants! As it turns out, though, there is a simple way of determining these. Suppose we use the more insightful labeling convention
\begin{align*}
\begin{split}
\omega(X, X, X) &\coloneqq \omega_1,\\
\omega(\nhphantom{${\mathbbm{1}}$}\hphantom{X}{\mathbbm{1}}, X, X) &\coloneqq \omega_2,\\
\omega(X, \nhphantom{${\mathbbm{1}}$}\hphantom{X}{\mathbbm{1}}, X) &\coloneqq \omega_3,\\
\omega(X, X, \nhphantom{${\mathbbm{1}}$}\hphantom{X}{\mathbbm{1}}) &\coloneqq \omega_4.
\end{split}
\end{align*}
\noindent The reason why we have chosen this relabeling is because now acting by $\alpha_{A, B, C}$ in the pentagon diagram introduces a factor of $\omega(A, B, C)$. If we apply this to the general pentagon diagram
\begin{center}
\begin{tikzcd}[row sep=1.2cm, column sep=2cm]
& ((A \otimes B) \otimes C) \otimes D\arrow[dl, "\alpha_{A,B,C} \otimes \id_D"']\arrow[dr, "\alpha_{A \otimes B,C,D}"] &\\
(A \otimes (B \otimes C)) \otimes D\arrow[d, "\alpha_{A,B \otimes C,D}"'] & & (A \otimes B) \otimes (C \otimes D)\arrow[d, "\alpha_{A,B,C\otimes D}"]\\
A \otimes ((B \otimes C) \otimes D)\arrow[rr, "\id_A\otimes\alpha_{B,C,D}"'] & & A \otimes (B \otimes (C \otimes D))
\end{tikzcd},
\end{center}
\noindent we obtain the constraints
\begin{align*}
\begin{split}
\omega(A, B, C)\omega(A, BC, D)\omega(B, C, D) = \omega(AB, C, D)\omega(A, B, CD)
\end{split}
\end{align*}
\noindent for all $A, B, C, D \in \obset(\textcat{Vec}_{\mathbb{Z}/2\mathbb{Z}})$. This is exactly the definition for a $3$-cocycle $\omega : (\mathbb{Z}/2\mathbb{Z})^{\oplus 3} \to \mathbbm{k}^\times$! In general, given a finite Abelian group $G$, there is a bijective correspondence between cohomologous $3$-cocycles on $G$ and monoidal equivalence classes of $6j$-symbols. We shall henceforth denote by $\textcat{Vec}_G^\omega$ the category of $G$-graded vector spaces whose $6j$-symbols are given by the non-trivial $3$-cocycle $\omega : G \times G \times G \to \mathbbm{k}^\times$, and $\textcat{Vec}_G$ the category whose $6j$-symbols are trivial.\\
\end{example}

\noindent Consider $\textcat{Vec}_G^\omega$, for some non-trivial $3$-cocycle $\omega$. By the strictification theorem of Mac Lane, this category is monoidally equivalent to a strict monoidal category, say $\mathcal{S}$. But if the associator for $\mathcal{S}$ is trivial, wouldn't the $6j$-symbols too be trivial? Well, suppose that for any pair $g, h \in G$, we pick an isomorphism $\mu_{g,h} : g \otimes h \to gh$. Note that in the skeletal setting $g \otimes h = gh$, but in the strict setting they are only guaranteed to be isomorphic. Because $\homset(g \otimes (h \otimes k), (g \otimes h) \otimes k)$ is $1$-dimensional,
\begin{align*}
\begin{split}
\mu_{g,hk} \circ (\id_g \otimes \mu_{h,k}) = \omega(g, h, k) \mu_{gh,k} \circ (\mu_{g,h} \otimes \id_k)
\end{split}
\end{align*}
\noindent for some $\omega(g, h, k) \in \mathbbm{k}^\times$. Doing this for each vertex of the pentagon diagram, we recover that $\omega$ must be a $3$-cocycle. If we choose another isomorphism, say $\mu'_{g,h}$, then it must satisfy $\mu'_{g,h} = \beta(g,h)\mu_{g,h}$ for some $\beta(g, h) \in \mathbbm{k}^\times$. Replacing $\mu$ with $\mu'$ gives us a new scalar
\begin{align*}
\begin{split}
\omega'(g, h, k) = \beta(h, k)^{-1}\beta(g, hk)^{-1}\beta(gh, k)\beta(g, h)\omega(g, h, k).
\end{split}
\end{align*}
\noindent In other words, it multiplies $\omega$ by a $3$-coboundary, giving us a cohomologous $3$-cocycle.
\newpage

\ruledsection{The Cuntz Algebra Approach of Izumi}{2}
%\noindent\\ Take $\textcat{Vec}_G$ to be skeletal. Consider an associativity constraint $a_{ghk} : ghk \to ghk$. Since $ghk$ is a simple object, $\homset(ghk, ghk) \cong \mathbbm{k}$, whence $a_{ghk} = \mu_{ghk}\id_{ghk}$ for some $\mu_{ghk} \in \mathbbm{k}^\times$. Note that the pentagon diagram enforces certain conditions on our choice of $\mu_{ghk}$; in particular, if we look at this diagram, we'll see that $\mu_{ghk} = \omega(g, h, k)$ for some $3$-cocycle $\omega$. By this, we mean a map $\omega : G \times G \times G \to \mathbbm{k}^\times$ satisfying
%\begin{align*}
%\begin{split}
%\omega(x, y, zw)\omega(xy, z, w)\omega(y, z, w)^{-1}\omega(x, yz, w)^{-1}\omega(x, y, z)^{-1} = 1
%\end{split}
%\end{align*}
%\noindent for all $x, y, z, w \in G$. We will henceforth denote by $\textcat{Vec}_G^\omega$ the category of $G$-graded vector spaces with associativity constraint $a_{ghk} = \omega(g, h, k)\id_{ghk}$, for all $g, h, k \in G$, and $\textcat{Vec}_G$ the category of $G$-graded vector spaces with trivial associativity.\\

\noindent Consider the category $\textup{End}(M)$, for $M$ a hyperfinite type III factor. This category is strict, as $\rho \otimes \sigma \coloneqq \rho \circ \sigma$ by definition. Every near-group category with group $G$ contains some copy of $\textcat{Vec}_G^\omega$ corresponding to the group-like part. Because every unitary near-group category is a subcategory of $\textup{End}(M)$ and is hence itself strict, we know that it will actually contain the ``strictification'' of some $\textcat{Vec}_G^\omega$. However, Izumi shows that if $\mathcal{C}$ is any fusion category containing a simple object that is fixed under tensor products with invertibles (that is, there exists some simple object $X$ such that $X \otimes g \cong X$ for all invertible $g$), then it contains a copy of $\textcat{Vec}_G$, for $G$ the group of isomorphism classes of invertible objects. He shows in addition that if the fusion category is also unitary, then $g \otimes X = X$ (but we may not necessarily have that $X \otimes g = X$). The upshot is that we almost know how objects are tensored, since the group-like part will have trivial associativity (that is, $g \otimes h = gh$). We just need to understand $X \otimes g$ and $X \otimes X$, as well as the morphisms.\\

\noindent In \cite{Izu17}, Izumi showed that every unitary near-group category $\mathcal{C}$ with multiplicity $m$ is equivalent to a subcategory of $\textup{End}(M)$, where $M$ is the hyperfinite type $\textup{III}_1$ factor. In particular, it is generated by a single irreducible endomorphism $\rho \in \textup{End}_0(M)$ satisfying the fusion rules
\begin{align*}
\begin{split}
[\alpha_g] \otimes [\alpha_h] = [\alpha_{gh}],\\
[\alpha_g] \otimes [\rho] = [\rho] \otimes [\alpha_g] = [\rho],\\
[\rho] \otimes [\rho] = \bigoplus_{g \in G} [\alpha_g] \oplus [\rho]^{\oplus m},\\
\end{split}
\end{align*}
\noindent where the map $\alpha : G \to \textup{Aut}(M)$ induces an injective homomorphism from $G$ into $\textup{Out}(M)$.\\

\noindent The main result of \cite{Izu17} is \cite[Theorem 4.9]{Izu17}. Essentially, there is a bijective correspondence between the set of equivalence classes of unitary near-group categories with finite group $G$ and multiplicity parameter $m$ and the set of equivalence classes of admissible tuples $(\mathcal{K}, j_1, j_2, V, U_\mathcal{K}, \chi, l)$ (see \cite[Definition 4.8]{Izu17}). Here $\mathcal{K}$ is the finite-dimensional Hilbert space $\homset(\rho, \rho^2)$, $j_1$ and $j_2$ are two antilinear isometries of $\mathcal{K}$, $V$ and $U_\mathcal{K}$ are unitary representations of $G$ on $\mathcal{K}$, $\{\chi_g\}_{g \in G}$ are characters of $G$ and $l$ is a linear map from $\mathcal{K}$ to the set $\mathcal{B}(\mathcal{K}, \mathcal{K} \otimes \mathcal{K})$ of bounded operators $\mathcal{K} \to \mathcal{K} \otimes \mathcal{K}$.\\

\noindent By \cite[Theorem 9.1]{Izu17}, the unitary near-group categories with finite Abelian group $G$ and $m = \abs{G}$ are completely classified tuples of the form $(\langle\cdot,\cdot\rangle, a, b, c)$, where $\langle\cdot,\cdot\rangle : G \times G \to \mathbb{T}$ is a non-degenerate symmetric bicharacter and where $a : G \to \mathbb{T}$, $b : G \to \mathbb{T}$ and $c \in \mathbb{T}$ satisfy various conditions. When we say that $\langle\cdot,\cdot\rangle$ is a bicharacter, we mean that
\begin{align*}
\begin{split}
\langle xy, z\rangle = \langle x, z\rangle\langle y, z\rangle\qquad\text{and}\qquad\langle x, yz\rangle = \langle x, y\rangle\langle x, z\rangle
\end{split}
\end{align*}
for all $x, y, z \in G$. By non-degenerate, we mean that
\begin{align*}
\begin{split}
\langle x, \cdot\rangle = \langle y, \cdot\rangle
\end{split}
\end{align*}
\noindent if and only if $x = y$. This is equivalent to the map $\varphi : G \to \homset(G, \mathbb{T})$ given by $x \mapsto \langle x, \cdot\rangle$ being an isomorphism.\\

\noindent\begin{definition}\textup{(Cuntz Algebra).} Let $\{S_i\}_{i=1}^n$ be a set of isometries on an infinite-dimensional Hilbert space $\mathscr{H}$. Suppose moreover that these isometries satisfy the {\em Cuntz relation}
\begin{align*}
\begin{split}
\sum_{k=1}^n S_kS_k^* = 1.
\end{split}
\end{align*}
\noindent The {\em Cuntz algebra $\mathcal{O}_n$} is the universal $C^*$-algebra $C^*(S_1, \dots, S_n)$.\\
\end{definition}

\noindent\begin{remark} Note that, as isometries, $S_i^*S_i = 1$. In particular, we must have that $S_i^*S_j = \delta_{i,j}$ for all $i, j \in \{1, \dots, n\}$. This follows from the fact that a sum of projections is itself a projection if and only if the projections in the sum are pairwise orthogonal. The Cuntz relation is essentially ensuring that the sum of the projections $S_iS_i^*$ is the trivial projection.\\
\end{remark}

\noindent 
\newpage

\noindent\begin{example}\textup{(Fibonacci Category).} Let's look at the Fibonacci category. This is the near-group with $G = \{0\}$ and $m = 1$. Our choice for $\langle\cdot,\cdot\rangle$ is obvious, and \cite[Lemma 7.1]{Izu17} tells us that
\begin{align*}
\begin{split}
c^3 a(0) = \sqrt{n} = 1 \implies a(0) = c^{-3}.
\end{split}
\end{align*}
\noindent Moreover, \cite[Theorem 9.1]{Izu17} tells us that $b$ is defined by $b : 0 \mapsto -1/d$, where $d$ corresponds to the dimension of our irreducible generator $\rho$. Let's determine $c$ and $d$. Because $b$ is equal to its own Fourier transform, \cite[Theorem 9.1]{Izu17} tells us that
\begin{align*}
\begin{split}
b(0) = ca(0)b(0) \implies a(0) = c^{-1}.
\end{split}
\end{align*}
\noindent In order for $c^{-1} = c^{-3}$, we require $c = \pm 1$. Finally, \cite[Equation (9.5)]{Izu17} tells us that
\begin{align*}
\begin{split}
b(0)b(0)b(0) &= b(0)b(0) \mp \frac{1}{d},
\end{split}
\end{align*}
\begin{align*}
\begin{split}
\implies &-\frac{1}{d^3} = \frac{1}{d^2} \mp \frac{1}{d},\\
\implies &\pm d^2 - d - 1 = 0.
\end{split}
\end{align*}
\noindent This only has a real solution when $c = 1$, whence $d$ is nothing but the golden ratio (as it cannot be negative in the unitary case - this alternative solution, known as the Galois dual, corresponds to a non-unitary near-group in this case and many others). This is exactly what we would expect, as $d$ is the dimension of $X$ (where $d^2 = 1 + d$ comes from the fusion rule $X^2 = \mathbbm{1} \oplus X$).\\
\end{example}

\noindent\begin{example}\textup{($G = \mathbb{Z}/2\mathbb{Z}$).} Let's look at the case where $G = \mathbb{Z}/2\mathbb{Z}$ and $m = 2$. This near-group corresponds to the even part of the type $A_4$ subfactor. We know the dimension is
\begin{align*}
\begin{split}
d_\pm \coloneqq \frac{m \pm \sqrt{m^2 + 4n}}{2} = 1 \pm \sqrt{3}.
\end{split}
\end{align*}
\noindent In the unitary setting, we of course ask that $d$ be positive, and hence we choose $d = d_+$. The only possibility for a non-degenerate bicharacter is
\begin{align*}
\begin{split}
\langle 0, 0\rangle = 1,\qquad\langle 0, 1\rangle = \langle 1, 0\rangle = 1\qquad\text{and}\qquad\langle 1, 1\rangle = -1.
\end{split}
\end{align*}
\noindent From \cite[Equation (7.8)]{Izu17}, it follows that
\begin{align*}
\begin{split}
a(0) = 1\qquad\text{and}\qquad a(1) = \pm i.
\end{split}
\end{align*}
\noindent Meanwhile, \cite[Equation (9.4)]{Izu17} tells us that
\begin{align*}
\begin{split}
\overline{b(1)} = \pm i b(1) \implies \mathfrak{R}(b(1)) = \mp \mathfrak{I}(b(1)),
\end{split}
\end{align*}
\noindent whence \cite[Equation (9.3)]{Izu17} gives us
\begin{align*}
\begin{split}
\mathfrak{R}(b(1))^2 + \mathfrak{I}(b(1))^2 = (b(1)\overline{b(1)})^2 = \frac{1}{2} \implies b(1) = \frac{1 - a(1)}{2}.
\end{split}
\end{align*}
\noindent It then follows from evaluating \cite[Equation (9.1)]{Izu17} with $g = 0$ and rearranging for $c$ that
\begin{align*}
\begin{split}
c = \frac{1 - \sqrt{3} + a(1)(1 + \sqrt{3})}{2\sqrt{2}}.
\end{split}
\end{align*}
\noindent Note that we may choose either $a(1) = i$ or $a(1) = -i$; both of these lead to solutions. Moreover, in the non-unitary setting, we may take the Galois conjugate of $d$.
\end{example}
\newpage

\noindent\begin{example}\label{UHIZ2}\textup{($G = \mathbb{Z}/2\mathbb{Z}$).} Let's determine the Haagerup--Izumi categories with $G = \mathbb{Z}/2\mathbb{Z}$. Let
\begin{align*}
\begin{split}
d_\pm \coloneqq \frac{n \pm \sqrt{n^2 + 4}}{2},
\end{split}
\end{align*}
\noindent where in this example $d \coloneqq 1 + \sqrt{2}$. Izumi's classification involves a triplet $(\epsilon_h(g), \omega(g), A_{h,k}(g))$, where $\epsilon_h(g) \in \{-1, 1\}$, $\omega(g) \in \mathbb{T}$ and $A_{h,k}(g) \in \mathbb{C}$ satisfy \cite[Equations (4.1)--(4.9)]{Izu18}. Well, we know
\begin{align*}
\begin{split}
\epsilon_0(0) = \epsilon_1(0) = 1\qquad\text{and}\qquad\epsilon_0(1) = \epsilon_0(1)\epsilon_0(1) \implies \epsilon_0(1) = 1.
\end{split}
\end{align*}
\noindent By \cite[Equation (4.7)]{Izu18},
\begin{align*}
\begin{split}
A_{0,0}(g) = A_{0,0}(g)\omega(g),
\end{split}
\end{align*}
\noindent which tells us that either $\omega(g) = 1$ or $A_{0,0}(g) = 0$ for each $g \in G$. Let's fix any $g \in G$ and consider the case when $A_{0,0}(g) = 0$. In this case, however, \cite[Equations (4.3) and (4.4)]{Izu18} give us
\begin{align*}
\begin{split}
A_{1,0}(g)\overline{A_{\delta_{g,0}-g,0}(g)} = 1 - \frac{\abs{\omega(g)}}{d} \implies \text{``}\frac{1}{d^2} = 1 - \frac{1}{d}\text{''}.
\end{split}
\end{align*}
\noindent This ``equality'' is nonsense; we must therefore have $\omega(g) = 1$ for all $g \in G$. Suppose now that $\epsilon_1(1) = 1$. Then \cite[Equation (4.7)]{Izu18} gives us
\begin{align*}
\begin{split}
A_{0,1}(0) = A_{1,1}(0) = A_{1,0}(0)\qquad\text{and}\qquad A_{0,1}(1) = A_{1,1}(1) = A_{1,0}(1),
\end{split}
\end{align*}
\noindent while \cite[Equation (4.8)]{Izu18} gives us $A_{1,1}(0) = A_{1,1}(1)$. Now, \cite[Equations (4.4) and (4.6)]{Izu18} tell us
\begin{align*}
\begin{split}
A_{0,1}(0) A_{1,1}(1) + A_{1,1}(0) A_{1,0}(1) = 0.
\end{split}
\end{align*}
\noindent Thus $A_{0,1}(g) = A_{1,1}(g) = A_{1,0}(g) = 0$ and hence $A_{0,0}(g) = -1/d$ by \cite[Equation (4.3)]{Izu18}. However, in this case we cannot satisfy \cite[Equation (4.9)]{Izu18}. Suppose instead that $\epsilon_1(1) = -1$. With this new $2$-cocycle, \cite[Equation (4.7)]{Izu18} now gives us
\begin{align*}
\begin{split}
A_{0,1}(0) = A_{1,1}(0) = A_{1,0}(0)\qquad\text{and}\qquad A_{0,1}(1) = -A_{1,1}(1) = A_{1,0}(1),
\end{split}
\end{align*}
\noindent while \cite[Equation (4.8)]{Izu18} gives us $A_{1,1}(1) = -A_{1,1}(0)$. We then see by \cite[Equation (4.4)]{Izu18} that
\begin{align*}
\begin{split}
A_{0,1}(0) A_{1,0}(0) + A_{1,1}(0) A_{1,1}(0) = 1 \implies A_{1,0}(0) = \pm\frac{1}{\sqrt{2}} = \pm\frac{1}{d-1},
\end{split}
\end{align*}
\noindent and by \cite[Equation (4.9)]{Izu18} that
\begin{align*}
\begin{split}
A_{0,0}(0)A_{1,0}(0)^2 = A_{1,0}(0)^2 + A_{1,0}(0)^3 \implies A_{0,0}(0) = 1 + A_{1,0}(0) = \frac{d-1 \pm 1}{d-1}.
\end{split}
\end{align*}
\noindent Finally, \cite[Equation (4.3)]{Izu18} allows us to deduce
\begin{align*}
\begin{split}
A_{1,0}(0) = -\frac{1}{d-1},
\end{split}
\end{align*}
\noindent whence
\begin{align*}
\begin{split}
A(0) = \frac{1}{d-1}\begin{pmatrix}d-2 & -1\\ -1 & -1\end{pmatrix}\qquad\text{and}\qquad A(1) = \frac{1}{d-1}\begin{pmatrix}d-2 & -1\\ -1 & \hphantom{-}1\end{pmatrix}.
\end{split}
\end{align*}
\noindent This category is nothing but the even part of the type $A_7$ subfactor.\\
\end{example}
\newpage

\noindent\begin{remark} Suppose that $\abs{G}$ is odd. Then \cite[Equation (4.1)]{Izu18} tells us that $\epsilon_h(g) = 1$, while \cite[Equation (4.2)]{Izu18} tells us that $\omega(g)$ does not depend on $g$. Moreover, $A_{h,k}(g)$ cannot depend on $g$ by \cite[Equation (4.5)]{Izu18}, and either $\omega = 1$ or $A_{0,0} = 0$ by \cite[Equation (4.7)]{Izu18}. In this case, \cite[Equations (4.1)--(4.9)]{Izu18} reduce to the following four equations.
\begin{align*}
\begin{split}
A_{h,k} = A_{-k,h-k}\omega = A_{k-h,-h}\overline{\omega},
\end{split}
\end{align*}
\begin{align*}
\begin{split}
\sum_{h \in G} A_{h,0} = -\frac{\overline{\omega}}{d_\pm},
\end{split}
\end{align*}
\begin{align*}
\begin{split}
\sum_{h \in G} A_{h-g,k} A_{k,h-g'} = \delta_{g,g'} - \frac{\delta_{k,0}}{d_\pm},
\end{split}
\end{align*}
\begin{align*}
\begin{split}
\sum_{l \in G} A_{x+y,l} A_{-x,l+p} A_{-y,l+q} = A_{p+x,q+x+y} A_{q+y,p+x+y} - \frac{\delta_{x,0}\delta_{y,0}}{d_\pm}.
\end{split}
\end{align*}
\end{remark}
\noindent The first three equations above are precisely \cite[Equations (4.7)--(4.9)]{EG17}! In particular, to see that our third equation is equivalent to \cite[Equation (4.9)]{EG17}, we simply make the change of variables $\hat{g} \coloneqq g' - g$ and $\hat{h} \coloneqq h - g'$, whence we obtain
\begin{align*}
\begin{split}
\sum_{\hat{h} \in G} A_{\hat{h}+\hat{g},k} A_{k,\hat{h}} = \delta_{\hat{g},0} - \frac{\delta_{k,0}}{d_\pm}.
\end{split}
\end{align*}
\noindent Similarly, using our first equation while making the change of variables $\hat{l} \coloneqq l-x-y$, $\hat{p} \coloneqq p+x+y$, $\hat{q} \coloneqq q+x+y$, $\hat{x} \coloneqq -x$ and $\hat{y} \coloneqq -y$, our fourth equation becomes
\begin{align*}
\begin{split}
\overline{\omega}\sum_{\hat{l} \in G} A_{\hat{l},\hat{x}+\hat{y}} A_{\hat{x},\hat{l}+\hat{p}} A_{\hat{y},\hat{l}+\hat{q}} = A_{\hat{y}+\hat{p},\hat{q}} A_{\hat{x}+\hat{q},\hat{p}} - \frac{\delta_{\hat{x},0}\delta_{\hat{y},0}}{d_\pm},
\end{split}
\end{align*}
\noindent showing that it is equivalent to \cite[Equation (4.11)]{EG17}.
\newpage

\ruledsection{The Leavitt Algebra Approach of Evans--Gannon}{3}
\noindent\\ The important results are \cite[Theorem 1 and Theorem 2]{EG17}.\\

\noindent\begin{definition}\textup{(Leavitt Algebra).} Let $X \coloneqq (x_{ij})$ and $Y \coloneqq (y_{ij})$ be $m \times n$ and $n \times m$ matrices of symbols, respectively. The {\em Leavitt $\mathbbm{k}$-algebra of type $(m, n)$} is the free associative unital $\mathbbm{k}$-algebra
\begin{align*}
\begin{split}
\mathcal{L}_\mathbbm{k}(m, n) \coloneqq \frac{\mathbbm{k}[x_{ij}, y_{ij}]}{\langle XY = I_m, YX = I_n\rangle}.
\end{split}
\end{align*}
\noindent In other words, it is the universal $\mathbbm{k}$-algebra with generators
\begin{align*}
\begin{split}
\{x_{ij} : 1 \leq i \leq m, 1 \leq j \leq n\} \sqcup \{y_{ij} : 1 \leq i \leq n, 1 \leq j \leq m\}
\end{split}
\end{align*}
\noindent and {\em Leavitt--Cuntz relations}
\begin{align*}
\begin{split}
\sum_{k=1}^m y_{ik} x_{kj} = \delta_{i,j}\qquad\text{and}\qquad\sum_{k=1}^n x_{ik} y_{kj} = \delta_{i,j},
\end{split}
\end{align*}
\noindent for all suitable $i, j$.\\
\end{definition}

\noindent Consider the Leavitt $\mathbb{C}$-algebra of type $(1, n)$, which we shall henceforth denote by $\mathcal{L}_n \coloneqq \mathcal{L}_\mathbb{C}(1, n)$. We have that $\mathcal{O}_n = C^*(\mathcal{L}_n)$, where $x_i = S_i$ and $y_i = S_i^*$. Let's think about what this means precisely. The Leavitt--Cuntz relations for $m = 1$ become
\begin{align*}
\begin{split}
y_i x_j = \delta_{i,j}\qquad\text{and}\qquad\sum_{k=1}^n x_k y_k = 1.
\end{split}
\end{align*}
\noindent We may endow $\mathcal{L}_n$ with the structure of a $*$-algebra by defining an involutive conjugate homogeneous antiautomorphism that sends $x_i \mapsto y_i$ and $y_i \mapsto x_i$. We further define
\begin{align*}
\begin{split}
\norm{a} \coloneqq \sup\{p(a) : \textup{$p$ is a $C^*$-seminorm on $\mathcal{L}_n$}\}
\end{split}
\end{align*}
\noindent for all $a \in \mathcal{L}_n$, where a $C^*$-seminorm is just a seminorm for which $p(a^*a) = p(a)^2$ and $p(ab) \leq p(a)p(b)$. Note that $0 \leq \norm{a} \leq 1$, as $1 = \norm{y_ix_i} = \norm{x_i^2}$ and hence $\norm{x_i} = \norm{y_i} = 1$ for all $i$. The condition $p(ab) \leq p(a)p(b)$ ensures that $\mathcal{I} \coloneqq \{a \in \mathcal{L}_n : \norm{a} = 0\}$ is an ideal in $\mathcal{L}_n$. Our $C^*$-seminorm then descends to a $C^*$-norm on the quotient $\mathcal{L}_n/\mathcal{I}$. The completion of $\mathcal{L}_n/\mathcal{I}$ with respect to this $C^*$-norm is known as the universal $C^*$-algebra of $\mathcal{L}_n$, denoted by $C^*(\mathcal{L}_n)$. This is precisely $\mathcal{O}_n$ by definition. We may therefore view $\mathcal{L}_n$ as the polynomial part of $\mathcal{O}_n$. \textcolor{red}{Is this stuff really important?}\\

\noindent Because $\mathcal{L}_n$ is a unital algebra over $\mathbbm{k}$, its algebra endomorphisms define a strict preadditive $\mathbbm{k}$-linear tensor category $\textcat{End}(\mathcal{L}_n)$. The objects of $\textcat{End}(\mathcal{L}_n)$ are algebra endomorphisms of $\mathcal{L}_n$, while the morphisms are intertwiners. In other words, a morphism $r \in \homset_{\textcat{End}(\mathcal{L}_n)}(\beta, \gamma)$ is an element $r \in \mathcal{L}_n$ for which $r\beta(x) = \gamma(x)r$ for all $x \in \mathcal{L}_n$, with composition being multiplication in $\mathcal{L}_n$. We endow $\textcat{End}(\mathcal{L}_n)$ with a tensor product given on objects as $\beta \otimes \gamma \coloneqq \beta \circ \gamma$ for all $\beta, \gamma \in \obset(\textcat{End}(\mathcal{L}_n))$ and on morphisms as $r \otimes s \coloneqq r\beta(s) = \gamma(s)r \in \homset_{\textcat{End}(\mathcal{L}_n)}(\beta\circ\rho, \gamma\circ\sigma)$ for all $r \in \homset_{\textcat{End}(\mathcal{L}_n)}(\beta, \gamma)$ and $s \in \homset_{\textcat{End}(\mathcal{L}_n)}(\rho, \sigma)$. Note that this is indeed well-defined, since
\begin{align*}
\begin{split}
r\beta(s)\beta(\rho(x)) = \gamma(s)r\beta(\rho(x)) = \gamma(s)\gamma(\rho(x))r = \gamma(s\rho(x))r = \gamma(\sigma(x)s)r = \gamma(\sigma(x))\gamma(s)r
\end{split}
\end{align*}
\noindent for all $x \in \mathcal{L}_n$. \textcolor{red}{We should be more general here.}\\

\noindent\textcolor{red}{Define rigid objects (see EGNO Definition 2.10.1).}\\

\noindent Let $\mathcal{A}$ now be a collection of algebra endomorphisms of $\mathcal{L}_n$ that is closed under composition and that contains the identity. We denote by $\textcat{End}(\mathcal{A})$ the subcategory of $\textcat{End}(\mathcal{L}_n)$ restricted to $\mathcal{A}$. This subcategory is itself a $\mathbb{C}$-linear tensor category, and $\End_{\textcat{End}(\mathcal{A})}(\id) = \mathbbm{k}$. Suppose we now take its Karoubi envelope, $\overline{\textcat{End}(\mathcal{A})}$. Its objects consist of pairs $(p, \beta)$, where $\beta \in \mathcal{A}$ and $p \in \End_{\textcat{End}(\mathcal{A})}(\beta)$ is an idempotent. The hom-spaces are of the form $\homset_{\overline{\textcat{End}(\mathcal{A})}}((p, \beta), (q, \gamma)) \coloneqq q\homset_{\textcat{End}(\mathcal{A})}(\beta, \gamma)p$, with composition given once more by multiplication. We endow $\overline{\textcat{End}(\mathcal{A})}$ with the structure of a tensor category by defining a monoidal product on objects by ${(p, \beta) \otimes (q, \gamma) \coloneqq (p \otimes q, \beta \otimes \gamma) = (p\beta(q), \beta\circ\gamma)}$ and on morphisms by $(qrp) \otimes (q'r'p') \coloneqq qrp\beta(q'r'p')$, for $qrp \in \homset((p,\beta), (q,\gamma))$ and\linebreak $q'r'p' \in \homset((p',\beta'), (q',\gamma'))$. We also endow $\overline{\textcat{End}(\mathcal{A})}$ with the structure of an additive category as follows. The objects %in this category
are ordered $n$-tuples ${((p_1, \beta_1), \dots, (p_n, \beta_n)) \eqcolon (p_1, \beta_1) \oplus \cdots \oplus (p_n, \beta_n)}$, while the hom-spaces are
\begin{align*}
\begin{split}
\homset(((p_1, \beta_1), \dots, (p_n, \beta_n)), ((q_1, \gamma_1), \dots, (q_m, \gamma_m)))
\end{split}
\end{align*}
\begin{align*}
\begin{split}
\qquad\quad= \begin{pmatrix} q_1\homset(\beta_1, \gamma_1)p_1 & \cdots & q_1\homset(\beta_n, \gamma_1)p_n\\ \vdots & \ddots & \vdots\\ q_m\homset(\beta_1, \gamma_m)p_1 & \cdots & q_m\homset(\beta_n, \gamma_m)p_n\end{pmatrix}.
\end{split}
\end{align*}
\noindent Composition of morphisms in this category corresponds to matrix multiplication. The monoidal product $((p_1, \beta_1), \dots, (p_n, \beta_n)) \otimes ((q_1, \gamma_1), \dots, (q_m, \gamma_m))$ is the direct sum of each $(p_i, \beta_i) \otimes (q_j, \gamma_j)$. On morphisms, the monoidal product acts as the Kronecker product, with $(ij, i'j')$th entry given by $q_ir_{ji}p_j \otimes q_{i'}'r_{j'i'}'p_{j'}'$. We will write $\overline{\textcat{End}(\mathcal{A})}_\oplus$ for the Karoubi envelope $\overline{\textcat{End}(\mathcal{A})}$ extended by direct sums.\\

\noindent Suppose as before that $\mathcal{A}$ is a collection of $\mathcal{L}_n$-endomorphisms that is closed under composition and contains the identity. Suppose that $\homset(\beta, \gamma) = \homset(\tilde{\beta}, \tilde{\gamma})$ in $\mathcal{L}_n$ for all $\beta, \gamma \in \mathcal{A}$, where $\tilde{\beta}(x) \coloneqq \beta(x')'$, and moreover that these hom-spaces are all finite-dimensional. Then $\overline{\textcat{End}(\mathcal{A})}_\oplus$ is a strict rigid semisimple $\mathbb{C}$-linear tensor category with finite-dimensional Hom-spaces by \cite[Lemma 2]{EG17}.\\

\noindent\textcolor{red}{Why do we do this? Well, for the Yang--Lee example it's not an issue, since we're working in $\mathcal{L}_2$. For example, given a sum $\rho \oplus (\rho^3 \oplus \rho^5)$, we can write it in the Leavitt algebra as}
\begin{align*}
\begin{split}
\textcolor{red}{s\rho s' + t(s\rho^3 s' + t\rho^5 t')t'.}
\end{split}
\end{align*}
\noindent\textcolor{red}{For an arbitrary category, however, we aren't so fortunate! In particular, we can only write sums whose number of simple summands $k$ obeys $k \equiv 1\ (\textup{mod }m+n-1)$ as genuine Leavitt algebra endomorphisms. This is exactly why we need to consider the idempotent completion.}
\newpage

\begin{example}\textup{(Yang--Lee Category).} Let $G = \{0\}$. Then \cite[Equation (4.7)]{EG17} demands that
\begin{align*}
\begin{split}
A_{0,0} = \omega A_{0,0} = \overline{\omega}A_{0,0} \implies \omega = 1,
\end{split}
\end{align*}
whence \cite[Equation (4.8)]{EG17} tells us that
\begin{align*}
\begin{split}
A_{0,0} = -\frac{1}{d_\pm}.
\end{split}
\end{align*}
\noindent The rest of \cite[Equations (4.7)--(4.10)]{EG17} are satisfied by these choices. Hence by \cite[Theorem 2]{EG17}, we have two fusion categories for $G = \{0\}$; a unitary one with $\pm = +$ (the Fibonacci category) and a non-unitary one with $\pm = -$ (the Yang--Lee category).\\
\end{example}

\noindent\begin{example}\textup{($G = \mathbb{Z}/2\mathbb{Z}$).} The equations we must satisfy for $\abs{G}$ even are given in \cite{Izu18} \textcolor{red}{(is this true?)}. Adapting our argument from \hyperref[UHIZ2]{Example \ref*{UHIZ2}}, we see that there is exactly one non-unitary Haagerup--Izumi category with $G = \mathbb{Z}/2\mathbb{Z}$. This corresponds to $\epsilon_h(g) = (-1)^{gh}$, $\omega(g) = 1$,
\begin{align*}
\begin{split}
A(0) = \frac{1}{d-1}\begin{pmatrix}d & 1\\ 1 & 1\end{pmatrix}\qquad\text{and}\qquad A(1) = \frac{1}{d-1}\begin{pmatrix}d & \hphantom{-}1\\ 1 & -1\end{pmatrix}.
\end{split}
\end{align*}
\end{example}
\newpage

\ruledsection{Deconstruction}{4}
\noindent\\ Let $\mathcal{C}$ be a near-group category with fusion rule $(G, m)$ over an algebraically closed field $\mathbbm{k}$ \textcolor{red}{whose characteristic does not divide $n \coloneqq \abs{G}$}.\\ %\textcolor{red}{Recall that duals of simple objects are simple and that duals preserve quantum dimension. Because the invertible objects all have quantum dimension $1$ while the non-invertible simple object has a non-unit quantum dimension, it follows that this non-invertible simple object is isomorphic to its left and right duals. We shall choose for our category a rigid structure such that the non-invertible simple object is equal simultaneously to both of its duals by \hyperref[SelfDual]{Lemma \ref*{SelfDual}}}.\\

%Because these invertible objects have quantum dimension $1$, our fusion rules tell us that the dimension $d$ of the non-invertible object must satisfy $d^2 = md + n$. In other words,
%\begin{align*}
%\begin{split}
%d = \frac{m \pm \sqrt{m^2 + 4n}}{2}.
%\end{split}
%\end{align*}
\noindent In this section, we assume that there exist algebra endomorphisms realizing the fusion rules of $\mathcal{C}$. We will make this precise momentarily. Our aim will be to show that these endomorphisms restrict to endomorphisms of the Leavitt algebra $\mathcal{L} \coloneqq \mathcal{L}_{n+m}$, and then to derive \textcolor{red}{certain data / invariants}.\\


% we will assume that there exist endomorphisms $\rho$ and $\{\alpha_g\}_{g \in G}$ of some algebra $A$ realizing the fusion rules of $\mathcal{C}$; that is,
%\begin{gather*}
%[\alpha_g] \circ [\alpha_h] = [\alpha_{gh}],\\
%[\alpha_g] \circ [\rho] = [\rho] \circ [\alpha_g] = [\rho],\\
%[\rho] \circ [\rho] = \bigoplus_{g \in G} [\alpha_g] + \sum_{i=1}^m [\rho],
%\end{gather*}
%\noindent where isomorphisms are given by intertwiners.

%Assume this category may be realized as $\mathcal{E}(A)$ for some algebra $A$. That is, assume the existence of algebra endomorphisms $\rho$ and $\alpha_g$ of $A$ satisfying %the fusion rules
%\noindent %Let $\mathcal{L} \coloneqq \mathcal{L}_{m+n}$ and consider $s_i, s_i', t_g, t_g' \in \mathcal{L}$, for all $i \in \{1, \dots, m\}$ and $g \in G$.

\noindent Let us first write our fusion rules in the language of algebra endomorphisms. Suppose there exist endomorphisms $\rho$ and $\{\alpha_g\}_{g \in G}$ of some algebra $A$ such that
\begin{gather}
[\alpha_g] \circ [\alpha_h] = [\alpha_{gh}],\\
[\alpha_g] \circ [\rho] = [\rho] = [\rho] \circ [\alpha_g],
\end{gather}
\noindent where isomorphisms are given by invertible intertwiners. Let $\alpha_g' \in [\alpha_g]$ such that $\alpha_g' \circ \rho = \rho'$. Then there exists some invertible $\omega_g \in \homset_{\textcat{End}(A)}(\rho', \rho)$ for which $\alpha_g'(\rho(x)) = \omega_g^{-1}\rho(x)\omega_g$. We may therefore choose representatives $\alpha_g : x \mapsto \omega_g\alpha_g'(x)\omega_g^{-1}$ such that $\alpha_g \circ \rho = \rho$. Moreover, there must exist some $\mu_{g,h} \in \homset_{\textcat{End}(A)}(\alpha_{gh}, \alpha_g \circ \alpha_h)$ such that $\alpha_g(\alpha_h(x)) = \mu_{g,h}\alpha_{gh}(x)\mu_{g,h}^{-1}$. Thus
\begin{align}
\rho(x) = \alpha_g(\alpha_h(\rho(x))) = \mu_{g,h}\alpha_{gh}(\rho(x))\mu_{g,h}^{-1} = \mu_{g,h}\rho(x)\mu_{g,h}^{-1}.
\end{align}
\noindent It follows that $\mu_{g,h} \in \homset_{\textcat{End}(A)}(\rho, \rho)$. Since $\rho$ is simple, we must assume $\homset_{\textcat{End}(A)}(\rho, \rho)$ to be $1$-dimensional. Thus $\mu_{g,h}$ is a scalar, whence $\alpha_g \circ \alpha_h = \alpha_{gh}$ for all $g, h \in G$. With such choices, we may only expect $\rho(\alpha_g(x)) = U(g)\rho(x)U(g)^{-1}$, for some invertible $U(g) \in \homset_{\textcat{End}(A)}(\rho, \rho \circ \alpha_g)$.\\

\noindent We therefore assume that there exist endomorphisms $\rho$ and $\{\alpha_g\}_{g \in G}$ of some algebra $A$, together with $s_g, s_g', t_i, t_i' \in A$ and invertibles $U(g) \in A^\times$ such that %for $i \in \{1, \dots, m\}$ and $g \in G$ such that
\begin{gather}
\alpha_g(\alpha_h(x)) = \alpha_{gh}(x),\\
\alpha_g(\rho(x)) = \rho(x),\qquad\rho(\alpha_g(x)) = U(g)\rho(x)U(g)^{-1},\\
\rho(\rho(x)) = \sum_{g \in G}{s_g\alpha_g(x)s_g'} + \sum_{i=1}^m{t_i\rho(x)t_i'}\label{Rho2Fusion}
\end{gather}
\noindent for all $x \in A$, where
\begin{align}
\sum_{g \in G}{s_gs_g'} + \sum_{i=1}^m{t_it_i'} = 1,\qquad s_g's_h = \delta_{g,h},\qquad t_i't_j = \delta_{i,j}\qquad\text{and}\qquad s_g't_i = 0 = t_i's_g,
\end{align}
\noindent for all $g, h \in G$ and $i, j \in \{1, \dots, m\}$. For the sake of convenience, we will define
\begin{align}
S \coloneqq \sum_{g \in G}{s_gs_g'}\qquad\text{and}\qquad T \coloneqq \sum_{i=1}^m{t_it_i'},
\end{align}
\noindent where $S + T = 1$ by definition. Finally, for $\rho$ and $\{\alpha_g\}_{g \in G}$ to be (absolutely) simple, we assume that
\begin{gather}
\homset_{\textcat{End}(A)}(\rho, \rho) = \mathbbm{k},\\
\homset_{\textcat{End}(A)}(\rho, \alpha_g) = \{0\} = \homset_{\textcat{End}(A)}(\alpha_g, \rho),\\
\homset_{\textcat{End}(A)}(\alpha_g, \alpha_h) = \mathbbm{k}\delta_{g,h},
\end{gather}
\noindent for all $g, h \in G$. With these assumptions in mind, we are able to deduce the following.\\

\noindent\begin{lemma}\label{HomDimensions} Let $\rho$ and $\{\alpha_g\}_{g \in G}$ be endomorphisms of $A$ satisfying our prior assumptions. Then
\begin{alignat}{2}
&\homset_{\textcat{End}(A)}(\alpha_g, \rho^2) &&= \textup{span}_\mathbbm{k}(s_g),\label{Hom(alpha_g, rho^2)}\\
&\homset_{\textcat{End}(A)}(\rho^2, \alpha_g) &&= \textup{span}_\mathbbm{k}(s_g'),\label{Hom(rho^2, alpha_g)}\\
&\homset_{\textcat{End}(A)}(\rho, \rho^2) &&= \textup{span}_\mathbbm{k}(t_1, \dots, t_m),\label{Hom(rho, rho^2)}\\
&\homset_{\textcat{End}(A)}(\rho^2, \rho) &&= \textup{span}_\mathbbm{k}(t_1', \dots, t_m'),\label{Hom(rho^2, rho)}\\
&\homset_{\textcat{End}(A)}(\rho^2, \rho^2) &&= \textup{span}_\mathbbm{k}(\{s_gs_g'\}_{g \in G} \sqcup \{t_it_j'\}_{i,j = 1}^m),\label{Hom(rho^2, rho^2)}
\end{alignat}
\noindent for all $g \in G$.\\
\end{lemma}

\noindent\begin{proof} From the fusion rules, $\rho^2(x)s_g = s_gx$, $s_g'\rho^2(x) = xs_g'$, $\rho^2(x)t_i = t_i\rho(x)$ and $t_i'\rho^2(x) = \rho(x)t_i'$, for all $x \in A$, $g \in G$ and $i \in \{1, \dots, m\}$. In other words, $s_g \in \homset_{\textcat{End}(A)}(\alpha_g, \rho^2)$, $s_g' \in \homset_{\textcat{End}(A)}(\rho^2, \alpha_g)$, $t_i \in \homset_{\textcat{End}(A)}(\rho, \rho^2)$ and $t_i' \in \homset_{\textcat{End}(A)}(\rho^2, \rho)$. Composing these tells us also that we must have $s_gs_g', t_it_j' \in \homset_{\textcat{End}(A)}(\rho^2, \rho^2)$. Suppose $r \in \homset_{\textcat{End}(A)}(\alpha_g, \rho^2)$. Then $s_h'r \in \homset_{\textcat{End}(A)}(\alpha_g, \alpha_h) = \mathbbm{k}\delta_{g,h}$ and $t_i'r \in \homset_{\textcat{End}(A)}(\alpha_g, \rho) = \{0\}$, whence
\begin{align}
r = (S + T)r = s_gs_g'r \in \mathbbm{k}s_g.
\end{align}
\noindent Similar arguments give us Equations \eqref{Hom(rho^2, alpha_g)}, \eqref{Hom(rho, rho^2)}, \eqref{Hom(rho^2, rho)} and \eqref{Hom(rho^2, rho^2)}. This completes the proof.
\end{proof}\\

\noindent We will find it convenient now to denote $\mathcal{K} \coloneqq \homset_{\textcat{End}(A)}(\rho, \rho^2)$ and $\mathcal{K}' \coloneqq \homset_{\textcat{End}(A)}(\rho^2, \rho)$, and let
\begin{align}
\mathcal{K}^p\mathcal{K}'^q \coloneqq \textup{span}_\mathbbm{k}\{t_{i_1}t_{i_2} \cdots t_{i_p}t_{j_1}'t_{j_2}' \cdots t_{j_q}'\}_{i_1,\dots,i_p,j_1,\dots,j_q=1}^m
\end{align}
\noindent for $p, q \in \mathbb{N}$. In other words, $\mathcal{K}^p\mathcal{K}'^q \cong \mathcal{K}^{\otimes p} \otimes (\mathcal{K}')^{\otimes q}$. We also note that $\mathcal{K}\mathcal{K}' = \textup{End}(\mathcal{K})$. Certainly $\mathcal{K}\mathcal{K}' \subseteq \textup{End}(\mathcal{K})$, since $t_it_j'\mathcal{K} \subseteq \mathcal{K}$. The converse direction comes by comparing dimensions. A dual argument with $\mathcal{K}'t_it_j' \subseteq \mathcal{K}'$ shows also that $\mathcal{K}\mathcal{K}' = \textup{End}(\mathcal{K}')$.\\

\noindent Observe that
\begin{gather}
U(g)U(h)\rho(x)U(h)^{-1}U(g)^{-1} = \rho(\alpha_g(\alpha_h(x))) = \rho(\alpha_{gh}(x)) = U(gh)\rho(x)U(gh)^{-1}\\
\implies U(gh)^{-1}U(g)U(h)\rho(x) = \rho(x)U(gh)^{-1}U(g)U(h).
\end{gather}
\noindent Thus $U(gh)^{-1}U(g)U(h)$ intertwines $\rho$ and hence lies in $\mathbbm{k}^\times$. In particular, $U : G \mapsto \homset_{\textcat{End}(A)}(\rho^2, \rho^2)$ defines a projective representation of $G$. Now, $\alpha_g(\alpha_e(x)) = \alpha_g(x)$ for all $x \in A$, so $\alpha_e = \id$ and $U(g)s_e \in \homset_{\textcat{End}(A)}(\id, \rho^2)$. We shall choose to rescale $U(g)$ such that $U(g)s_e = s_e$ for all $g \in G$, making $U$ a genuine representation of $G$. Moreover, we have from \hyperref[Hom(rho^2, rho^2)]{Equation \eqref*{Hom(rho^2, rho^2)}} that
\begin{align}
U(g) = \sum_{h \in G}{\chi_h(g)s_hs_h'} + U_\mathcal{K}(g)
\end{align}
\noindent for $\chi_h : G \to \mathbbm{k}$ and $U_\mathcal{K}(g) \in \textup{span}\{t_it_j'\}_{i,j = 1}^m = \mathcal{K}\mathcal{K}'$. In particular, $U_\mathcal{K} : G \to \textup{End}(\mathcal{K})$ defines a representation as the projection of $U(g)$ onto $\mathcal{K}\mathcal{K}'$, and $\chi_h \in \homset(G, \mathbbm{k}^\times)$. Indeed, if $\chi_h(g) = 0$, then $U(g)s_h = 0 = s_h'U(g)$; this would imply $s_h = 0 = s_h'$ by the invertibility of $U(g)$, which is nonsense. Moreover, the fact that $U$ and $U_\mathcal{K}$ are group homomorphisms shows $\chi_h(g_1g_2) = \chi_h(g_1)\chi_h(g_2)$.
%\begin{align*}
%\begin{split}
%\sum_{h \in G}{\chi_h(g_1 g_2)s_hs_h'} + U_\mathcal{K}(g_1 g_2) = U(g_1g_2) = U(g_1)U(g_2) = \sum_{h \in G}{\chi_h(g_1)\chi_h(g_2)s_hs_h'} + U_\mathcal{K}(g_1 g_2).
%\end{split}
%\end{align*}
 In other words, $\chi_h$ is a character of $G$.\\

\noindent\textcolor{red}{Since $s_h \in \homset_{\textcat{End}(A)}(\alpha_h, \rho^2)$ and since $\alpha_g$ is an automorphism, $\alpha_g(s_h) \in \mathbbm{k}^\times s_{gh}$. Similarly, because $s_h' \in \homset_{\textcat{End}(A)}(\rho^2, \alpha_h)$, we have that $\alpha_g(s_h') \in \mathbbm{k}^\times s_{gh}'$. In what follows, we will choose $s_g$ and $s_g'$ such that $\alpha_g(s_e) = s_g$ and $\alpha_g(s_e') = s_g'$, for all $g \neq e$. Then from $\alpha_g \circ \alpha_h = \alpha_{gh}$, it follows that $\alpha_g(s_h) = s_{gh}$.}\\

\noindent\textcolor{red}{The above choice can definitely be removed. But we'll have to recheck our proofs.}\\

\noindent\begin{lemma} The automorphism $\alpha_g$ admits the dual $\alpha_{g^{-1}}$ and restricts to an automorphism of $\mathcal{L}$, for all $g \in G$. In particular, there exists a representation $V : G \to \textup{End}(\mathcal{K})$ such that
\begin{gather}
\alpha_g(x) = V(g)x\qquad\textup{and}\qquad\alpha_g(y) = yV(g)^{-1},
\end{gather}
\noindent for all $g \in G$, $x \in \mathcal{K}$ and $y \in \mathcal{K}'$.\\
\end{lemma}

\noindent\begin{proof} We see that
\begin{align}
\homset_{\textcat{End}(A)}(\id, \alpha_g \circ \alpha_{g^{-1}}) = \homset_{\textcat{End}(A)}(\alpha_e, \alpha_e) = \mathbbm{k} = \homset_{\textcat{End}(A)}(\alpha_{g^{-1}} \circ \alpha_g, \id),
\end{align}
\noindent whence it immediately follows by symmetry that $\alpha_{g^{-1}}$ is both a left and right dual for $\alpha_g$. \textcolor{red}{We have already shown that $\alpha_g(s_h) \in \mathcal{L}$ for all $g, h \in G$.} From the fact that $\alpha_g$ is an endomorphism, we know that $\alpha_g(x) \in \mathcal{K}$ for all $x \in \mathcal{K}$ and $\alpha_g(y) \in \mathcal{K}'$ for all $y \in \mathcal{K}'$. In particular, there must exist an element $V(g) \in \mathcal{K}\mathcal{K}'$ satisfying $\alpha_g(x) = V(g)x$ for all $x \in \mathcal{K}$, defining a final representation $V : G \to \textup{End}(\mathcal{K})$. In fact, $1 = \alpha_g(t_i't_i) = \alpha_g(t_i')V(g)t_i$ tells us that $\alpha_g(y) = yV(g)^{-1}$ for all $y \in \mathcal{K}'$, as both $V(g)$ and $t_i$ have unique left inverses. This completes the proof.
\end{proof}\\

\noindent From our fusion rules, we know that $s_e \in \homset_{\textcat{End}(A)}(\id, \rho^2)$ and $s_e' \in \homset_{\textcat{End}(A)}(\rho^2, \id)$. Because $\rho$ is an endomorphism, we know that $\rho(s_e) \in \homset_{\textcat{End}(A)}(\rho, \rho^3)$. Similarly, $t_i \in \homset_{\textcat{End}(A)}(\rho, \rho^2)$ and $t_i' \in \homset_{\textcat{End}(A)}(\rho^2, \rho)$. Thus $t_i'\rho(s_e) \in \homset_{\textcat{End}(A)}(\rho, \rho^2)$ and $\rho(s_e')t_i \in \homset_{\textcat{End}(A)}(\rho^2, \rho)$. We shall therefore define linear maps $j_1 : \mathcal{K}' \to \mathcal{K}$ and $j_1' : \mathcal{K} \to \mathcal{K}'$ by
\begin{align}
j_1 : y \mapsto y\rho(s_e)\qquad\textup{and}\qquad j_1' : x \mapsto \rho(s_e')x,
\end{align}
\noindent for all $x \in \mathcal{K}$ and $y \in \mathcal{K}'$. Similarly, since $\rho(t_i')s_e \in \homset_{\textcat{End}(A)}(\rho, \rho^2)$ and $s_e'\rho(t_i) \in \homset_{\textcat{End}(A)}(\rho^2, \rho)$, we will also define linear maps $j_2 : \mathcal{K}' \to \mathcal{K}$ and $j_2' : \mathcal{K} \to \mathcal{K}'$ by
\begin{align}
j_2 : y \mapsto \rho(y)s_e\qquad\textup{and}\qquad j_2' : x \mapsto s_e'\rho(x).
\end{align}
\noindent These allow us to show the following.\\

\noindent\begin{lemma} The endomorphism $\rho$ restricts to an endomorphism of $\mathcal{L}$. In particular, there exists some $a \in \mathbbm{k}$ such that
\begin{align}
\rho(s_e) &= a\sum_{h \in G}s_h + \sum_{i=1}^m t_i j_1(t_i'),\label{PartialRhoS}\\
\rho(s_e') &= a\sum_{h \in G}s_h' + \sum_{i=1}^m j_1'(t_i)t_i',\label{PartialRho'S}
\end{align}
\noindent as well as linear maps and $l_h : \mathcal{K} \to \mathcal{K}$, $l : \mathcal{K} \to \mathcal{K}^2\mathcal{K}'$, $l_h' : \mathcal{K}' \to \mathcal{K}'$ and $l' : \mathcal{K}' \to \mathcal{K}(\mathcal{K}')^2$ such that
\begin{align}
\rho(x) &= \sum_{h \in G}s_h\alpha_h(j_2'(x)) + \sum_{h \in G} \alpha_h(l_e(x))s_hs_h' + l(x),\label{PartialRhoT}\\
\rho(y) &= \sum_{h \in G}\alpha_h(j_2(y))s_h' + \sum_{h \in G} s_hs_h'\alpha_h(l_e'(y)) + l'(y),\label{PartialRho'T}
\end{align}
\noindent for all $x \in \mathcal{K}$ and $y \in \mathcal{K}'$.\\
\end{lemma}

\noindent\begin{proof} Observe that $s_e'\rho(s_e) \in \homset_{\textcat{End}(A)}(\rho, \rho) = \mathbbm{k}^\times$ and $\rho(s_e')s_e \in \homset_{\textcat{End}(A)}(\rho, \rho) = \mathbbm{k}^\times$. Let
\begin{align}
a \coloneqq s_e'\rho(s_e)\qquad\textup{and}\qquad a' \coloneqq \rho(s_e')s_e.
\end{align}
\noindent We first observe that, by \hyperref[Rho2Fusion]{Equation \eqref*{Rho2Fusion}}, we have
\begin{align}
a = \rho(a) = \rho(s_e')\rho^2(s_e) = \sum_{g \in G} \rho(s_e') s_g s_g s_g' + \sum_{i=1}^m \rho(s_e') t_i\rho(s_e)t_i' = a'S + aT.
\end{align}
\noindent It follows that $a' = a$ and hence
\begin{align}
\rho(s_e) &= (S + T)\rho(s_e) = a\sum_{h \in G}s_h + \sum_{i=1}^m t_i j_1(t_i') \in \mathcal{L},\\
\rho(s_e') &= \rho(s_e')(S + T) = a\sum_{h \in G}s_h' + \sum_{i=1}^m j_1'(t_i)t_i' \in \mathcal{L}.
\end{align}
\noindent Similarly, $t_i'\rho(t_j) \in \homset_{\textcat{End}(A)}(\rho^2, \rho^2)$ and $\rho(t_j')t_i \in \homset_{\textcat{End}(A)}(\rho^2, \rho^2)$. Thus by \hyperref[Hom(rho^2, rho^2)]{Equation \eqref*{Hom(rho^2, rho^2)}}, we have $T\rho(x) \in \mathcal{K}\homset_{\textcat{End}(A)}(\rho^2, \rho^2)$ for all $x \in \mathcal{K}$, whence there must exist linear maps $l_h : \mathcal{K} \to \mathcal{K}$ and $l : \mathcal{K} \to \mathcal{K}^2\mathcal{K}'$ for which
\begin{align}
T\rho(x) = \sum_{h \in G} l_h(x)s_hs_h' + l(x).
\end{align}
\noindent Dually, since $\rho(y)T \in \homset_{\textcat{End}(A)}(\rho^2, \rho^2)\mathcal{K}'$ for all $y \in \mathcal{K}'$, we must also have linear maps $l_h' : \mathcal{K}' \to \mathcal{K}'$ and $l' : \mathcal{K}' \to \mathcal{K}(\mathcal{K}')^2$ such that
\begin{align}
\rho(y)T = \sum_{h \in G} s_hs_h'l_h'(y) + l'(y).
\end{align}
\noindent Because $\alpha_g \circ \rho = \rho$, we must have
\begin{align}
T\alpha_g(\rho(x)) = \sum_{h \in G}{\alpha_g(l_h(x)) s_{gh} s_{gh}'} + \alpha_g(l(x)) = T\rho(x);
\end{align}
\noindent thus $l_g = \alpha_g \circ l_e$, $\alpha_g \circ l = l$, $l_g' = \alpha_g \circ l_e'$, $\alpha_g \circ l' = l'$. The result follows. This completes the proof. %Thus
%\begin{align}
%\rho(x) &= (S + T)\rho(x) = \sum_{h \in G}s_h\alpha_h(j_2'(x)) + \sum_{h \in G} \alpha_h(l_e(x))s_hs_h' + l(x) \in \mathcal{L},\label{PartialRhoT}\\
%\rho(y) &= \rho(y)(S + T) = \sum_{h \in G}\alpha_h(j_2(y))s_h' + \sum_{h \in G} s_hs_h'\alpha_h(l_e'(y)) + l'(y) \in \mathcal{L},\label{PartialRho'T}
%\end{align}
%\noindent for all $x \in \mathcal{K}$ and $y \in \mathcal{K}'$. This completes the proof.
\end{proof}\\

\noindent\textcolor{red}{For now, let's just assume that $a \coloneqq \nu/d$, for $\nu \in \{-1, 1\}$ and $d^2 = n + md$. Then we should be able to show $\rho(s_e')t_it_j'\rho(s_e) = \frac{\delta_{i,j}}{d}$, all the properties of $\varphi$ and $\varphi'$ and that $l_e = \varphi$, $l_e' = \varphi'$.}\\

\newpage\noindent\\\newpage

%\noindent\textcolor{red}{Should we define $\varphi \coloneqq a^{-1} j_2 \circ j_1'$, $\varphi^{-1} \coloneqq a^{-1} j_1 \circ j_2'$, $\varphi' \coloneqq a^{-1} j_2' \circ j_1$ and $\varphi'^{-1} \coloneqq a^{-1} j_1' \circ j_2$? Need to show that $a$ is non-zero .}\\
%
%\newpage
%
%\noindent .\\
%\newpage
%
%\noindent Note that
%\begin{align}
%\rho(s_e')T\rho(s_e) = \rho(s_e')(S + T)\rho(s_e) - \rho(s_e')S\rho(s_e) = 1 - n a'a.
%\end{align}
%\noindent Using \hyperref[PartialRhoS]{Equation \eqref*{PartialRhoS}}, we have
%\begin{align}
%\rho^2(s_e) = a^2 \sum_{g,h \in G} U(g)s_hU(g^{-1}) + a \sum_{g \in G}\sum_{i=1}^m U(g)t_i j_1(t_i')U(g^{-1}) + \sum_{i=1}^m \rho(t_i) \rho(j_1(t_i'))\label{Rho2Expansion}
%\end{align}
%\noindent and hence
%\begin{align}
%s_e'\rho^2(s_e) = a^2 \sum_{g,h \in G}\chi_e(g)\chi_h(g^{-1})s_hs_h' + \sum_{i=1}^m j_2'(t_i)\rho(j_1(t_i')).
%\end{align}
%\noindent From our fusion rules, it is clear that $s_e'\rho^2(s_e) = s_es_e'$, whence
%\begin{align}
%1 = s_e's_e'\rho^2(s_e)s_e = n a^2 + \sum_{i=1}^m s_e'j_2'(t_i)\rho(j_1(t_i'))s_e = n a^2 + \sum_{i=1}^m s_e's_e'\rho(t_i)\rho(t_i')\rho^2(s_e)s_e.
%\end{align}
%\noindent However,
%\begin{align}
%\sum_{i=1}^m s_e's_e'p(t_i)p(t_i')p^2(s_e)s_e = s_e'\rho^2(s_e')\rho(T)\rho^2(s_e)s_e = s_e'\rho(\rho(s_e')T\rho(s_e))s_e = 1 - n a'a.
%\end{align}
%\noindent This, along with a similar argument for $\rho^2(s_e')s_e = s_es_e'$, gives us that $a' = a$.\\
%
%
%\noindent Observe that, because $s_e$ is an intertwiner in $\homset_{\textcat{End}(A)}(\rho^2, \id)$,
%\begin{align}
%j_2'(j_2(y)) = s_e'\rho^2(y)\rho(s_e) = ys_e'\rho(s_e) = ay
%\end{align}
%\noindent for all $y \in \mathcal{K}'$, and similarly $j_2(j_2'(x)) = ax$ for all $x \in \mathcal{K}$.
%\noindent\\
%
%
%\noindent\\
%
%\noindent Suppose for the sake of contradiction that $a = 0$. Then $T\rho(s_e) = \rho(s_e)$ and hence $s_e'\rho(T)\rho^2(s_e) = s_e'\rho^2(s_e) = s_es_e'$. However, note that by \hyperref[PartialRhoS]{Equation \eqref*{PartialRhoT}} and \hyperref[PartialRhoS]{Equation \eqref*{PartialRho'T}},
%\begin{align}
%\sum_{i=1}^m s_e'\rho(t_i)\rho(t_i')\rho^2(s_e) = \sum_{i=1}^m j_2'(t_i)\rho(t_i')\rho^2(s_e).
%\end{align}
%\noindent Expanding $\rho(t_i')$, we have
%\begin{gather}
%j_2'(t_i)\alpha_h(j_2(t_i'))s_h' \in \mathcal{K}'\mathcal{K}s_h' = \mathbbm{k}s_h',\\
%j_2'(t_i)s_h \in \mathcal{K}'s_h = \{0\},\\
%j_2'(t_i)l'(y) \in \mathcal{K}'\mathcal{K}(\mathcal{K}')^2 = (\mathcal{K}')^2.
%\end{gather}
%\noindent \\
%
%
%\noindent\\
%
%
%\noindent We will now repeat this with $t_i'\rho^2(s_e) = \rho(s_e)t_i'$ and \hyperref[Rho2Expansion]{Equation \eqref*{Rho2Expansion}}. First, we have
%\begin{align}
%t_j'\rho^2(s_e) = a \sum_{g \in G}\sum_{i=1}^m t_j'U_\mathcal{K}(g)t_i t_i'\rho(s_e)U(g^{-1}) + \sum_{i=1}^m t_j'\rho(t_i)\rho(t_i')\rho^2(s_e).
%\end{align}
%\noindent We once more refer to our fusion rules to find that $t_j'\rho^2(s_e) = \rho(s_e)t_j'$. If $a = 0$, then
%\begin{align}
%\rho^2(s_e) = \rho(T)\rho^2(s_e)\qquad\text{and}\qquad t_j'\rho^2(s_e) = t_j'\rho(T)\rho^2(s_e).
%\end{align}
%\noindent \\
%
%
%\noindent\textcolor{red}{How can we show that $a^2 = 1/d^2$? What about $\rho(s_e')t_it_j'\rho(s_e) = \delta_{i,j}/d$? If I can at least show that $a \neq 0$, I can probably translate some results from fusion categories.}
%\noindent\\ 
%
%\newpage
%
%\noindent Observe that $M_m(\mathbbm{k}) \cong \mathcal{K}\mathcal{K}'$, where $T$ is the identity matrix. Let $C_{ij} \coloneqq \rho(s_e')t_it_j'\rho(s_e)$ and define
%\begin{align}
%C \coloneqq \sum_{i,j=1}^m C_{ij} t_it_j'.
%\end{align}
%\noindent Consider the standard trace $\textup{Tr} : X \mapsto \sum_{i=1}^m t_i'Xt_i$ on $M_m(\mathbbm{k})$. We have shown that
%\begin{align}
%\textup{Tr}^L : X \mapsto \frac{1}{m}\textup{Tr}(C)\textup{Tr}(X).
%\end{align}
%\noindent is itself a tracial linear map. We have shown that $\rho(s_e')T\rho(s_e) = 1 - na^2$, meaning
%\begin{align}
%\textup{Tr}^L(X) = \frac{1 - na^2}{m}\textup{Tr}(X).
%\end{align}
%
%\noindent\\\\
%
%\noindent Observe that $M_m(\mathbbm{k}) \cong \mathcal{K}\mathcal{K}'$, where $T$ is the identity matrix. Let
%\begin{align}
%X \coloneqq \sum_{i,j=1}^m X_{ij} t_it_j'\qquad\text{and}\qquad Y \coloneqq \sum_{i,j=1}^m Y_{ij} t_it_j'.
%\end{align}
%\noindent We see that
%\begin{align}
%\rho(s_e')XY\rho(s_e) = \sum_{i,j,k=1}^m X_{ij}Y_{jk}\rho(s_e')t_it_k'\rho(s_e)
%\end{align}
%\noindent We have already shown that $\rho(s_e')T\rho(s_e) = 1 - na^2$, and
%\begin{align}
%1 = \rho(s_e')\rho(s_e) = na^2 + \sum_{i,j=1}^m \rho(s_e')t_it_j'\rho(s_e).
%\end{align}
%
%
%%Then $\textup{Tr}(T) = m$. and any tracial linear map on $\mathcal{K}\mathcal{K}'$ must be related by a scalar.
%
%
%%\noindent From \hyperref[PartialRhoS]{Equation \eqref*{PartialRhoS}} together with \hyperref[PartialRho'S]{Equation \eqref*{PartialRho'S}}, we observe that
%%\begin{align}
%%1 = \rho(s_e')\rho(s_e) = a'a n + \sum_{i=1}^m j_1'(t_i)j_1(t_i') = a^2 n + \rho(s_e')T\rho(s_e).
%%\end{align}
%%\noindent 
%
%
%\newpage
%
%\noindent Now, from our fusion rules, we have $s_e \in \homset_{\textcat{End}(A)}(\id, \rho^2)$ and $s_e' \in \homset_{\textcat{End}(A)}(\rho^2, \id)$. Because $\rho$ is an endomorphism, we know that $\rho(s_e) \in \homset_{\textcat{End}(A)}(\rho, \rho^3)$, whence $s_e'\rho(s_e) \in \homset_{\textcat{End}(A)}(\rho, \rho) = \mathbbm{k}$. A similar argument with $\rho(s_e') \in \homset_{\textcat{End}(A)}(\rho^3, \rho)$ shows $\rho(s_e')s_e \in \homset_{\textcat{End}(A)}(\rho, \rho) = \mathbbm{k}$. We will write
%\begin{align}
%a \coloneqq s_e'\rho(s_e)\qquad\textup{and}\qquad a' \coloneqq \rho(s_e')s_e.
%\end{align}
%\noindent Playing the same game with $t_i \in \homset_{\textcat{End}(A)}(\rho, \rho^2)$ and $t_i' \in \homset_{\textcat{End}(A)}(\rho^2, \rho)$, we have that both $t_i'\rho(s_e) \in \homset_{\textcat{End}(A)}(\rho, \rho^2)$ and $\rho(s_e')t_i \in \homset_{\textcat{End}(A)}(\rho^2, \rho)$. We shall therefore define maps $j_1 : \mathcal{K}' \to \mathcal{K}$ and $j_1' : \mathcal{K} \to \mathcal{K}$ by linearly extending
%\begin{align}
%j_1 : t_i' \mapsto t_i'\rho(s_e)\qquad\textup{and}\qquad j_1' : t_i \mapsto \rho(s_e')t_i.
%\end{align}
%\noindent Together, these allow us to conclude that
%\begin{align}
%\rho(s_e) &= (S + T)\rho(s_e) = a\sum_{g \in G}s_g + \sum_{i=1}^m t_i j_1(t_i') \in \mathcal{L},\\
%\rho(s_e') &= \rho(s_e')(S + T) = a'\sum_{g \in G}s_g' + \sum_{i=1}^m j_1'(t_i)t_i' \in \mathcal{L}.
%\end{align}
%\noindent Through the same process, we know that
%\begin{align}
%s_e'\rho(t_i) \in \homset_{\textcat{End}(A)}(\rho^2, \rho),\\
%\rho(t_i')s_e \in \homset_{\textcat{End}(A)}(\rho, \rho^2),\\
%t_j'\rho(t_i) \in \homset_{\textcat{End}(A)}(\rho^2, \rho^2),\\
%\rho(t_i')t_j \in \homset_{\textcat{End}(A)}(\rho^2, \rho^2).
%\end{align}
%\noindent 
%
%\noindent\begin{lemma}\label{RhoExpansionPartial} We have $\rho(s_g), \rho(s_g') \in \mathcal{L}$ for all $g \in G$. In particular,
%\begin{align}
%\rho(s_e) = \sum_{h \in G}{\frac{\nu}{d}s_h} + \sum_{i=1}^m t_i j_1(t_i')\qquad\text{and}\qquad \rho(s_e') = \sum_{h \in G}{\frac{\nu}{d}s_h'} + \sum_{i=1}^m j_1'(t_i)t_i'
%\end{align}
%\noindent for $\nu \in \{-1, 1\}$ and $d$ satisfying $d^2 = md + n$.\\
%\end{lemma}
%
%\noindent\begin{proof} An immediate consequence of our previous observations is that
%\begin{align}
%\rho(s_e) &= (S + T)\rho(s_e) = a\sum_{g \in G}s_g + \sum_{i=1}^m t_i j_1(t_i') \in \mathcal{L},\\
%\rho(s_e') &= \rho(s_e')(S + T) = a'\sum_{g \in G}s_g' + \sum_{i=1}^m j_1'(t_i)t_i' \in \mathcal{L}.
%\end{align}
%\noindent \textcolor{red}{Not sure how to show that $a = \nu/d = a'$...} This completes the proof.
%\end{proof}\\

\noindent\begin{corollary} We have
\begin{align}
\rho(s_e')t_it_j'\rho(s_e) = \frac{\delta_{i,j}}{d},
\end{align}
\noindent for all $i, j \in \{1, \dots, m\}$.\\
\end{corollary}

\noindent\begin{proof} \textcolor{red}{This proof is incomplete. Need to establish the quantum trace and duals.} Observe that
\begin{align*}
\begin{split}
1 = \rho(s_e')\rho(s_e) = \frac{n}{d^2} + \sum_{i=1}^m \rho(s_e')t_it_i'\rho(s_e) \implies \sum_{i=1}^m \rho(s_e')t_it_i'\rho(s_e) = \frac{m}{d}.
\end{split}
\end{align*}
\noindent Recall that the left quantum trace of an element $x \in \homset_{\textcat{End}(A)}(\rho^2, \rho^2)$ is given by
\begin{align*}
\begin{split}
\textup{Tr}^L(x) \coloneqq \textup{ev}_\rho\rho(\textup{ev}_\rho)x\rho(\textup{coev}_\rho)\textup{coev}_\rho = d^2 s_e'\rho(s_e')x\rho(s_e)s_e =  d^2 \rho(s_e')x\rho(s_e).
\end{split}
\end{align*}
\noindent The final equality follows from the fact that $\rho(s_e')x\rho(s_e) \in \mathbbm{k}$, as it is in the $1$-dimensional space $\homset_{\textcat{End}(A)}(\rho, \rho)$, together with $s_e's_e = 1$. Because $M_m(\mathbbm{k}) \cong \mathcal{K}\mathcal{K}'$ admits a unique tracial state given by the matrix trace $\textup{Tr}$, we know it must be related to the trace $\textup{Tr}^L$ by a scalar. Since $\textup{Tr}(T) = m$ while $\textup{Tr}^L(T) = md$ by our previous observation, we have for all $x \in \mathcal{K}\mathcal{K}' \subset \homset_{\textcat{End}(A)}(\rho^2, \rho^2)$ that
\begin{align*}
\begin{split}
\rho(s_e')x\rho(s_e) = \frac{1}{d^2}\textup{Tr}^L(x) = \frac{1}{d}\textup{Tr}(x) = \frac{1}{d}\sum_{i=k}^m t_k'xt_k,
\end{split}
\end{align*}
\noindent whence the result follows by considering $x = t_it_j'$. This completes the proof.
\end{proof}\\

%\noindent \\

%\noindent\textcolor{red}{Need to show that if we define $\nu \in \{-1, 1\}$ and $d \in \mathbbm{k}^\times$ such that $\rho(s_e')s_e = \nu/d$, then $s_e'\rho(s_e) = \nu/d$ and $d^2 = md + n$.}\\

%\noindent There are in general no canonical choices for these intertwiners, so we will proceed as follows. Because both $s_e \in \homset_{\textcat{End}(A)}(\id, {\rho \otimes \rho})$ and $s_e' \in \homset_{\textcat{End}(A)}({\rho \otimes \rho}, \id)$ must live in $1$-dimensional spaces, we can assume that they behave as scalar multiples of the coevaluator and evaluator, normalized with respect to the direct sum condition $s_e's_e = 1$. By \hyperref[DimensionFrobeniusSchur]{Remark \ref*{DimensionFrobeniusSchur}}, we have
%\begin{align*}
%\begin{split}
%s_e \coloneqq \frac{1}{\sqrt{d}}\coev_\rho\qquad\text{and}\qquad s_e' \coloneqq \frac{1}{\sqrt{d}}(\ev_\rho \circ (\psi_\rho \otimes \id_\rho)) = \frac{\nu}{\sqrt{d}}\ev_\rho,
%\end{split}
%\end{align*}
%\noindent where the pivotal structure $\psi_\rho$ is multiplication by the Frobenius--Schur indicator $\nu \in \{-1, 1\}$. Thus
%\begin{align*}
%\begin{split}
%\rho(s_e')s_e = \frac{\nu}{d}(\id_\rho \otimes \ev_\rho) \circ (\coev_\rho \otimes \id_\rho) = \frac{\nu}{d} = \frac{\nu}{d}(\ev_\rho \otimes \id_\rho) \circ (\id_\rho \otimes \coev_\rho) = s_e'\rho(s_e).
%\end{split}
%\end{align*}
%\noindent Suppose now that $s_g \coloneqq \alpha_g(s_e)$. Recalling the definition of the monoidal product of morphisms in our category and our assumption that $\alpha_g \otimes \rho = \rho$,% this means that
%\begin{align*}
%\begin{split}
%s_g \coloneqq \id_{\alpha_g} \otimes s_e \in \homset_{\textcat{End}(A)}(\alpha_g, {\rho \otimes \rho}).
%\end{split}
%\end{align*}
%\noindent Because $\homset_{\textcat{End}(A)}(\alpha_g, {\rho \otimes \rho})$ is $1$-dimensional, this is going to be a linear scaling of some injection satisfying the direct sum properties, and hence it too will satisfy them. We then have the left inverse
%\begin{align*}
%\begin{split}
%s_g' \coloneqq \id_{\alpha_g} \otimes s_e'
%\end{split}
%\end{align*}
%\noindent for $s_g$. We will now find, for each $g \in G$, isomorphisms $U(g) \in \homset_{\textcat{End}(A)}({\rho \otimes \rho}, {\rho \otimes \rho})$ satisfying the normalization condition $U(g)s_e = s_e$. Because $\rho \otimes \alpha_g \cong \rho$, the space $\homset_{\textcat{End}(A)}(\rho, \rho \otimes \alpha_g)$ is $1$-dimensional, whence there must exist a unique scalar $U_g \in \homset_{\textcat{End}(A)}(\rho, \rho \otimes \alpha_g)$ satisfying
%\begin{align*}
%\begin{split}
%(U_g \otimes \id_\rho) \circ s_e = s_e,
%\end{split}
%\end{align*}
%\noindent where we note that $U_g \otimes \id_\rho = U_g$ as elements of $A$. We claim that
%\begin{align*}
%\begin{split}
%U_{gh} = (U_g \otimes \id_{\alpha_h}) \circ U_h. %(U(g) \otimes \id_\rho) \circ (U(h) \otimes \id_\rho) = U(gh) \otimes \id_\rho
%\end{split}
%\end{align*}
%\noindent This follows from the fact that, for all $g, h \in G$,
%\begin{align*}
%\begin{split}
%(((U_g \otimes \id_{\alpha_h}) \circ U_h) \otimes \id_\rho) \circ s_e = s_e = (U_{gh} \otimes \id_\rho) \circ s_e.
%\end{split}
%\end{align*}
%\noindent together with the fact that $\homset_{\textcat{End}(A)}(\rho, \rho \otimes \alpha_{gh})$ is $1$-dimensional. Thus the map $U : g \mapsto U_g \otimes \id_{\rho}$ defines a representation $U : G \to \homset_{\textcat{End}(A)}({\rho \otimes \rho}, {\rho \otimes \rho})$. In other words, $U(g)$ is invertible with $U(g)^{-1} = U(g^{-1})$. It follows that $\rho(\alpha_g(x)) = U_g\rho(x)U_g^{-1} = U(g)\rho(x)U(g)^{-1}$.\\

%\noindent We will find it convenient now to denote both $\mathcal{K} \coloneqq \homset_{\textcat{End}(A)}(\rho, {\rho \otimes \rho}) = \textup{span}_\mathbbm{k}\{t_1, \dots, t_m\}$ and $\mathcal{K}' \coloneqq \homset_{\textcat{End}(A)}({\rho \otimes \rho}, \rho) = \textup{span}_\mathbbm{k}\{t_1', \dots, t_m'\}$, and let
%\begin{align*}
%\begin{split}
%\mathcal{K}^p\mathcal{K}'^q \coloneqq \textup{span}_\mathbbm{k}\{t_{i_1}t_{i_2} \cdots t_{i_p}t_{j_1}'t_{j_2}' \cdots t_{j_q}'\}_{i_1,\dots,i_p,j_1,\dots,j_q=1}^m
%\end{split}
%\end{align*}
%\noindent for $p, q \in \mathbb{N}$. In other words, $\mathcal{K}^p\mathcal{K}'^q \cong \mathcal{K}^{\otimes p} \otimes (\mathcal{K}')^{\otimes q}$. We will also define the projections
%\begin{align*}
%\begin{split}
%S \coloneqq \sum_{h \in G}{s_hs_h'}\qquad\text{and}\qquad T \coloneqq \sum_{i=1}^m{t_it_i'},
%\end{split}
%\end{align*}
%\noindent where we recall that $S + T = 1$.\\

%\noindent  Every map in $\homset_{\textcat{End}(A)}({\rho \otimes \rho}, {\rho \otimes \rho})$ is a linear combination of maps ${\rho \otimes \rho} \to X \to {\rho \otimes \rho}$ for some simple object $X$, whence $\homset_{\textcat{End}(A)}({\rho \otimes \rho}, {\rho \otimes \rho})$ is spanned by $\{s_gs_g'\}_{g \in G} \sqcup \{t_it_j'\}_{i,j = 1}^m$. %Because morphisms in $\textcat{End}(A)$ are nothing but elements of the algebra $A$, we have that
%\begin{align*}
%\begin{split}
%\homset_{\textcat{End}(A)}(\rho, \rho \otimes \alpha_g) \subseteq \homset_{\textcat{End}(A)}({\rho \otimes \rho}, \rho \otimes \alpha_g \otimes \rho) = \homset_{\textcat{End}(A)}({\rho \otimes \rho}, {\rho \otimes \rho}).
%\end{split}
%\end{align*}
%\noindent In other words, $\homset_{\textcat{End}(A)}(\rho, \rho \otimes \alpha_g)$ is also spanned by $\{t_it_j'\}_{i,j = 1}^m \sqcup \{s_gs_g'\}_{g \in G}$.
%Thus
%\begin{align*}
%\begin{split}
%U(g) = \sum_{h \in G}{\chi_h(g)s_hs_h'} + U_\mathcal{K}(g)
%\end{split}
%\end{align*}
%\noindent for $\chi_h : G \to \mathbbm{k}$ and $U_\mathcal{K}(g) \in \textup{span}\{t_it_j'\}_{i,j = 1}^m = \mathcal{K}\mathcal{K}'$. Now, note that $\mathcal{K}\mathcal{K}' = \textup{End}(\mathcal{K})$. Certainly $\mathcal{K}\mathcal{K}' \subseteq \textup{End}(\mathcal{K})$, since $t_it_j'\mathcal{K} \subseteq \mathcal{K}$. The converse direction comes by comparing dimensions. Thus $U_\mathcal{K} : G \to \textup{End}(\mathcal{K})$ defines a representation as the projection of $U(g)$ onto $\mathcal{K}\mathcal{K}'$. It follows then that $\chi_h \in \homset(G, \mathbbm{k}^\times)$. Indeed, if $\chi_h(g) = 0$, then $U(g)s_h = 0 = s_h'U(g)$; this would imply $s_h = 0 = s_h'$ by the invertibility of $U(g)$, which is nonsense. Moreover, the fact that $U$ and $U_\mathcal{K}$ are group homomorphisms shows $\chi_h(g_1g_2) = \chi_h(g_1)\chi_h(g_2)$.
%\begin{align*}
%\begin{split}
%\sum_{h \in G}{\chi_h(g_1 g_2)s_hs_h'} + U_\mathcal{K}(g_1 g_2) = U(g_1g_2) = U(g_1)U(g_2) = \sum_{h \in G}{\chi_h(g_1)\chi_h(g_2)s_hs_h'} + U_\mathcal{K}(g_1 g_2).
%\end{split}
%\end{align*}
% In other words, $\chi_h$ is a character of $G$.\\

%\noindent Another consequence of the definition of $\mathcal{K}$ is that $\alpha_g(x) \in \mathcal{K}$ for all $x \in \mathcal{K}$, and similarly $\alpha_g(y) \in \mathcal{K}'$ for all $y \in \mathcal{K}'$. We must therefore have an element $V(g) \in \mathcal{K}\mathcal{K}'$ satisfying $\alpha_g(x) = V(g)x$ for all $x \in \mathcal{K}$, defining a final representation $V : G \to \textup{End}(\mathcal{K})$. In fact, $1 = \alpha_g(t_i't_i) = \alpha_g(t_i')V(g)t_i$ tells us that $\alpha_g(y) = yV(g)^{-1}$ for all $y \in \mathcal{K}'$, as both $V(g)$ and $t_i$ have unique left inverses.\\% In particular, $\alpha_g(U_\mathcal{K}(h)) = V(g)U_\mathcal{K}(h)V'(g)$.\\ %In particular, we have the following result.\\

%\noindent We wish to show that $\rho$ and $\alpha_g$ restrict to endomorphisms of %the Leavitt algebra
%$\mathcal{L} \coloneqq \mathcal{L}_{m+n}$, with generators $s_g, s_g', t_i, t_i'$ for $g \in G$ and $i \in \{1, \dots, m\}$. Recall that $\alpha_g(s_h) = \id_{\alpha_g} \otimes (\id_{\alpha_h} \otimes s_e) = \id_{\alpha_{gh}} \otimes s_e = s_{gh}$, and similarly for $\alpha_g(s_h')$. In other words, $\alpha_g(s_h) \in \mathcal{L}$ for all $g, h \in G$. Of course, $\alpha_g(x) \in \mathcal{K}$ and $\alpha_g(y) \in \mathcal{K}'$ for all $x \in \mathcal{K}$, $y \in \mathcal{K}'$ and $g \in G$. All that remains is to show how $\rho$ acts on $\mathcal{K}$ and $\mathcal{K}'$.\\

%\noindent\begin{lemma}\label{RhoExpansion} We have $\rho(s_g), \rho(s_g') \in \mathcal{L}$ for all $g \in G$. In particular,
%\begin{align*}
%\begin{split}
%\rho(s_e) = \sum_{h \in G}{\frac{\nu}{d}s_h} + \sum_{i=1}^m{t_it_i'\rho(s_e)}\qquad\text{and}\qquad \rho(s_e') = \sum_{h \in G}{\frac{\nu}{d}s_h'} + \sum_{i=1}^m{\rho(s_e')t_it_i'}
%\end{split}
%\end{align*}
%\noindent with
%\begin{align*}
%\begin{split}
%\rho(s_e')t_it_j'\rho(s_e) = \frac{\delta_{i,j}}{d},
%\end{split}
%\end{align*}
%\noindent where $\rho(s_g) = U(g)\rho(s_e)U(g)^{-1}$ and $\rho(s_g') = U(g)\rho(s_e')U(g)^{-1}$ for all $g \in G$.\\
%\end{lemma}
%
%\noindent\begin{proof} First, observe that for $\rho(s_g)$, we have
%\begin{align*}
%\begin{split}
%\rho(s_g) = \rho(\alpha_g(s_e)) = U(g)\rho(s_e)U(g)^{-1}.
%\end{split}
%\end{align*}
%\noindent Similarly, $\rho(s_g') = U(g)\rho(s_e')U(g)^{-1}$. Thus it suffices to show how $\rho$ acts on $s_e$ and $s_e'$. Using $(S + T)\rho(s_e)$ together with $s_e'\rho(s_e) = \nu/d$ from before, we obtain
%\begin{align*}
%\begin{split}
%S\rho(s_e) = \sum_{h \in G}{s_hs_h'\rho(s_e)} = \sum_{h \in G}{\alpha_h(s_es_e'\rho(s_e))} = \sum_{h \in G}{\frac{\nu}{d}\alpha_h(s_e)} = \sum_{h \in G}{\frac{\nu}{d}s_h}.
%\end{split}
%\end{align*}
%\noindent Meanwhile, for $T\rho(s_e)$, we have that $t_i'\rho(s_e) \in \mathcal{K}$ and hence $T\rho(s_e) \in \mathcal{K}^2$. Repeating the same process but with $\rho(s_e')(S + T)$ and $\rho(s_e')s_e = \nu/d$ gives us
%\begin{align*}
%\begin{split}
%\rho(s_e')S = \sum_{h \in G}{\frac{\nu}{d}s_h'},
%\end{split}
%\end{align*}
%\noindent where $\rho(s_e')t_i \in \mathcal{K}'$ and hence $\rho(s_e')T \in (\mathcal{K}')^2$. To prove the final relation, we first observe that
%\begin{align*}
%\begin{split}
%1 = \rho(s_e')\rho(s_e) = \frac{n}{d^2} + \sum_{i=1}^m \rho(s_e')t_it_i'\rho(s_e) \implies \sum_{i=1}^m \rho(s_e')t_it_i'\rho(s_e) = \frac{m}{d}.
%\end{split}
%\end{align*}
%\noindent Recall now that the left quantum trace of an element $x \in \homset_{\textcat{End}(A)}({\rho \otimes \rho}, {\rho \otimes \rho})$ is given by
%\begin{align*}
%\begin{split}
%\textup{Tr}^L(x) \coloneqq \textup{ev}_\rho\rho(\textup{ev}_\rho)x\rho(\textup{coev}_\rho)\textup{coev}_\rho = d^2 s_e'\rho(s_e')x\rho(s_e)s_e =  d^2 \rho(s_e')x\rho(s_e).
%\end{split}
%\end{align*}
%\noindent The final equality follows from the fact that $\rho(s_e')x\rho(s_e) \in \mathbbm{k}$, as it is in the $1$-dimensional space $\homset_{\textcat{End}(A)}(\rho, \rho)$, together with $s_e's_e = 1$. Because $M_m(\mathbbm{k}) \cong \mathcal{K}\mathcal{K}'$ admits a unique tracial state given by the matrix trace $\textup{Tr}$, we know it must be related to the trace $\textup{Tr}^L$ by a scalar. Since $\textup{Tr}(T) = m$ while $\textup{Tr}^L(T) = md$ by our previous observation, we have for all $x \in \mathcal{K}\mathcal{K}' \subset \homset_{\textcat{End}(A)}({\rho \otimes \rho}, {\rho \otimes \rho})$ that
%\begin{align*}
%\begin{split}
%\rho(s_e')x\rho(s_e) = \frac{1}{d^2}\textup{Tr}^L(x) = \frac{1}{d}\textup{Tr}(x) = \frac{1}{d}\sum_{i=k}^m t_k'xt_k,
%\end{split}
%\end{align*}
%\noindent whence the result follows by considering $x = t_it_j'$. This completes the proof.
%\end{proof}\\

%\noindent \textcolor{red}{Suppose we now let $V(g) : x \mapsto \id_{\alpha_g} \otimes x$ for all $x \in \homset_{\textcat{End}(A)}(\rho, {\rho \otimes \rho})$, which defines another representation $V : G \to \textup{End}(\homset_{\textcat{End}(A)}(\rho, {\rho \otimes \rho}))$.}\\

%\noindent We now wish to show that $\rho$ and $\alpha_g$ restrict to endomorphisms of the Leavitt algebra $\mathcal{L} \coloneqq \mathcal{L}_{m+n}$, with generators $s_g, s_g', t_i, t_i'$ for $g \in G$ and $i \in \{1, \dots, m\}$. \textcolor{red}{Step 1: figure out $j_1$ and $j_2$, then Lem 3.3.}\\

%\noindent\textcolor{red}{Questions: What is Frobenius reciprocity?}\\

%\noindent Consider the ``one-click'' (120 degree) rotations given by
%\begin{align*}
%\begin{split}
%\varphi_1 : t &\mapsto (\lambda_\rho\hphantom{{}_{{}^*}} \otimes \id_{\rho^*}) \circ (\ev_\rho\hphantom{{}_{{}^*}} \otimes \id_\rho\hphantom{{}_{{}^*}} \otimes \id_{\rho^*}) \circ (\id_{\rho^*} \otimes t \otimes \id_{\rho^*}\nhphantom{$\id_{\rho^*}$}\hphantom{\psi_\rho^{-1}}) \circ (\id_{\rho^*} \otimes \coev_\rho\hphantom{{}_{{}^*}}) \circ \rho_{\rho^*}^{-1},\\
%\varphi_2 : t &\mapsto (\lambda_{\rho^*} \otimes \id_\rho\hphantom{{}_{{}^*}}) \circ (\ev_\rho\hphantom{{}_{{}^*}} \otimes \id_{\rho^*} \otimes \id_\rho\hphantom{{}_{{}^*}}) \circ (\id_{\rho^*} \otimes t \otimes \psi_\rho^{-1}) \circ (\id_{\rho^*} \otimes \coev_{\rho^*}) \circ \rho_{\rho^*}^{-1},\\
%\varphi_3 : t &\mapsto (\lambda_\rho\hphantom{{}_{{}^*}} \otimes \id_\rho\hphantom{{}_{{}^*}}) \circ (\ev_{\rho^*} \otimes \id_\rho\hphantom{{}_{{}^*}} \otimes \id_\rho\hphantom{{}_{{}^*}}) \circ (\psi_\rho\nhphantom{$\psi_\rho$}\hphantom{\id_{\rho^*}} \otimes t \otimes \psi_\rho^{-1}) \circ (\id_\rho\hphantom{{}_{{}^*}} \otimes \coev_{\rho^*}) \circ \rho_\rho^{-1},
%\end{split}
%\end{align*}
%\noindent where
%\begin{align*}
%\begin{split}
%\varphi_1 : \homset_{\textcat{End}(A)}(\rho\hphantom{{}_{{}^*}}, \rho\hphantom{{}_{{}^*}} \otimes \rho\hphantom{{}_{{}^*}}) \to \homset_{\textcat{End}(A)}(\rho^*, \rho\hphantom{{}_{{}^*}} \otimes \rho^*),\\
%\varphi_2 : \homset_{\textcat{End}(A)}(\rho^*, \rho\hphantom{{}_{{}^*}} \otimes \rho^*) \to \homset_{\textcat{End}(A)}(\rho^*, \rho^* \otimes \rho\hphantom{{}_{{}^*}}),\\
%\varphi_3 : \homset_{\textcat{End}(A)}(\rho^*, \rho^* \otimes \rho\hphantom{{}_{{}^*}}) \to \homset_{\textcat{End}(A)}(\rho\hphantom{{}_{{}^*}}, \rho\hphantom{{}_{{}^*}} \otimes \rho\hphantom{{}_{{}^*}}).
%\end{split}
%\end{align*}
%\noindent Consider the map $j_1 : \homset_{\textcat{End}(A)}(\rho, {\rho \otimes \rho}) \to \homset_{\textcat{End}(A)}(\rho^* \otimes \rho^*, \rho^*)$ defined by
%\begin{align*}
%\begin{split}
%j_1 : t \mapsto\ \lambda_{\rho^*} &\circ (\ev_\rho \otimes \id_{\rho^*}) \circ (\id_{\rho^*} \otimes \lambda_\rho \otimes \id_{\rho^*}) \circ (\id_{\rho^*} \otimes \ev_\rho \otimes \id_\rho \otimes \id_{\rho^*})\\ &\circ (\id_{\rho^*} \otimes \id_{\rho^*} \otimes t \otimes \id_{\rho^*}) \circ (\id_{\rho^*} \otimes \id_{\rho^*} \otimes \coev_\rho) \circ (\id_{\rho^*} \otimes \rho_{\rho^*}^{-1})
%\end{split}
%\end{align*}
%\noindent and the map $j_2 : \homset_{\textcat{End}(A)}(\rho^* \otimes \rho^*, \rho^*) \to \homset_{\textcat{End}(A)}(\rho, {\rho \otimes \rho})$ defined by
%\begin{align*}
%\begin{split}
%j_2 : t \mapsto (\lambda_\rho \otimes \id_\rho) &\circ (\ev_{\rho^*} \otimes \id_\rho \otimes \id_\rho) \circ (\psi_\rho \otimes t \otimes \psi_\rho^{-1} \otimes \psi_\rho^{-1})\\ &\circ (\id_\rho \otimes \id_{\rho^*} \otimes \coev_{\rho^*} \otimes \id_{\rho^{**}}) \circ (\id_{\rho \otimes \rho}_{\rho^*}^{-1} \otimes \id_{\rho^{**}}) \circ (\id_\rho \otimes \coev_{\rho^*}) \circ \rho_\rho^{-1}.
%\end{split}
%\end{align*}
%\noindent In other words, $j_1 : t \mapsto t^*$ while 

%\noindent Consider the ``one-click'' (120 degree) rotation $\varphi : \mathcal{K} \to \mathcal{K}$ given by
%\begin{align*}
%\begin{split}
%\varphi : x &\mapsto (\id_\rho \otimes \rho_\rho) \circ (\id_\rho \otimes \id_\rho \otimes \ev_\rho) \circ (\id_\rho \otimes x \otimes \id_\rho) \circ (\coev_\rho \otimes \id_\rho) \circ \lambda_\rho^{-1} = \frac{d}{\nu}\rho^2(s_e')\rho(x) s_e,
%\end{split}
%\end{align*}
%\noindent with inverse
%\begin{align*}
%\begin{split}
%\varphi^{-1} : x &\mapsto (\lambda_\rho \otimes \id_{\rho^*}) \circ (\ev_\rho \otimes \id_\rho \otimes \id_{\rho^*}) \circ (\id_{\rho^*} \otimes x \otimes \id_{\rho^*}) \circ (\id_{\rho^*} \otimes \coev_\rho) \circ \rho_{\rho^*}^{-1} = \frac{d}{\nu}s_e'\rho(x)\rho(s_e).
%\end{split}
%\end{align*}
%\noindent where we recall that $\rho^* = \rho$. We observe that
%\begin{align*}
%\begin{split}
% &[\varphi \circ V(g) \circ \varphi^{-1}](x)\\
% &= (\id_\rho \otimes \id_{\alpha_g} \otimes \ev_\rho \otimes \id_\rho \otimes \ev_\rho) \circ (\id_\rho \otimes \id_{\alpha_g} \otimes \id_\rho \otimes x \otimes \id_\rho \otimes \id_\rho) \circ (\coev_\rho \otimes \coev_\rho \otimes \id_\rho).
%\end{split}
%\end{align*}
%\noindent Our normalization condition $U_gs_e = s_e$ tells us that $U_g\coev_\rho = \coev_\rho$. Making this substitution allows us to eliminate the zig-zags by rigidity, whence we obtain
%\begin{align*}
%\begin{split}
%[\varphi \circ V(g) \circ \varphi^{-1}](x) = (U_g \otimes \ev_\rho \otimes \id_\rho) \circ (\id_\rho \otimes \id_\rho \otimes x) \circ (\coev_\rho \otimes \id_\rho) = U(g)x = U_\mathcal{K}(g)x.
%\end{split}
%\end{align*}
%\noindent\textcolor{red}{A similar process yields}
%\begin{align*}
%\begin{split}
%[\varphi^{-1} \circ V(g) \circ \varphi](x) &= (\id_\rho \otimes U_{g^{-1}}^{-1}) \circ (x \otimes \id_{\alpha_{g^{-1}}}) \circ U_{g^{-1}} = \rho(U(g)) x U(g)^{-1}.
%\end{split}
%\end{align*}
%\noindent\textcolor{red}{We should show the diagrammatics here.}\\

\noindent\begin{lemma}\label{OneClickRotation} The map $\varphi \in \textup{End}(\mathcal{K}) = \mathcal{K}\mathcal{K}'$ given by
\begin{align*}
\begin{split}
\varphi : x &\mapsto \frac{d}{\nu}\rho^2(s_e')\rho(x)s_e
%\varphi : x &\mapsto d\rho^2(s_e')\rho(x)s_e
\end{split}
\end{align*}
\noindent is invertible, admitting the inverse
\begin{align*}
\begin{split}
\varphi^{-1} : x &\mapsto \frac{d}{\nu}s_e'\rho(x)\rho(s_e).
%\varphi^{-1} : x &\mapsto d s_e'\rho(x)\rho(s_e),
\end{split}
\end{align*}
%\noindent Moreover, we have
\noindent Moreover, we have
\begin{align*}
\begin{split}
\varphi^3(x) = \nu x,\qquad\varphi(\alpha_g(\varphi^{-1}(x))) = U_\mathcal{K}(g)x
\end{split}
\end{align*}
\noindent and
\begin{align*}
\begin{split}
\varphi^{-1}(\alpha_g(\varphi(x))) = \rho(U(g)) x U(g)^{-1}
\end{split}
\end{align*}
\noindent for all $x \in \mathcal{K}$ and $g \in G$.\\
\end{lemma}

\noindent\begin{proof} We begin by showing that $\varphi^{-1}$ is indeed an inverse for $\varphi$. Using the intertwiner relation together with the zig-zag identity $\rho(s_e')s_e = \nu/d$, we obtain
\begin{align*}
\begin{split}
\varphi(\varphi^{-1}(x)) &= \frac{d^2}{\nu^2}\rho^2(s_e')\rho(s_e')\rho^2(x)\rho^2(s_e)s_e = \frac{d^2}{\nu^2}\rho^2(s_e')\rho(s_e')\rho^2(x)s_es_e\\
&= \frac{d^2}{\nu^2}\rho^2(s_e')\rho(s_e')s_exs_e = \frac{d}{\nu}\rho^2(s_e')xs_e = \frac{d}{\nu}x\rho(s_e')s_e = x.
\end{split}
\end{align*}
\noindent A similar process using $s_e'\rho(s_e) = \nu/d$ yields
\begin{align*}
\begin{split}
\varphi^{-1}(\varphi(x)) &= \frac{d^2}{\nu^2}s_e'\rho^3(s_e')\rho^2(x)\rho(s_e)\rho(s_e) = \frac{d^2}{\nu^2}\rho(s_e')s_e'\rho^2(x)\rho(s_e)\rho(s_e)\\
&= \frac{d^2}{\nu^2}\rho(s_e')xs_e'\rho(s_e)\rho(s_e) = \frac{d}{\nu}\rho(s_e')x\rho(s_e) = \frac{d}{\nu}\rho(s_e')\rho^2(s_e)x = x.
\end{split}
\end{align*}
\noindent In order to show that $\varphi^3(x) = \nu x$, recall that the right dual to $x \in \mathcal{K}$ is given by
\begin{align*}
\begin{split}
{}^*x = \frac{d^\frac{3}{2}}{\nu^2}\rho(s_e')\rho^2(s_e')\rho(x)s_e \in \mathcal{K}',
\end{split}
\end{align*}
\noindent while the right dual to $y \in \mathcal{K}'$ is given by
\begin{align*}
\begin{split}
{}^*y = \frac{d^\frac{3}{2}}{\nu}\rho^2(s_e')\rho^2(y)\rho(s_e)s_e \in \mathcal{K}.
\end{split}
\end{align*}
\noindent Thus, for $x \in \mathcal{K}$, we have
\begin{align*}
\begin{split}
{}^{**}x = \frac{d^3}{\nu^3}\rho^2(s_e')\rho^3(s_e')\rho^4(s_e')\rho^3(x)\rho^2(s_e)\rho(s_e)s_e = \varphi^3(x).
\end{split}
\end{align*}
\noindent Because we chose the duals of $\rho$ to be equal to $\rho$, we have that ${}^{**}x = \psi_\rho^2x\psi_\rho = \nu x$, whence $\varphi^3(x) = \nu x$. Now, using the normalization relation $U(g)^{-1}s_e = s_e$ and the definition of morphisms,
\begin{align*}
\begin{split}
\varphi(\alpha_g(\varphi^{-1}(x))) &= \frac{d^2}{\nu^2}\rho^2(s_e')\rho(\alpha_g(s_e'\rho(x)\rho(s_e)))s_e = \frac{d^2}{\nu^2}\rho^2(s_e')U(g)\rho(s_e')\rho^2(x)\rho^2(s_e)U(g)^{-1}s_e\\
&= \frac{d^2}{\nu^2}U(g)\rho^2(s_e')\rho(s_e')\rho^2(x)\rho^2(s_e)s_e = \frac{d^2}{\nu^2}U(g)\rho^2(s_e')\rho(s_e')s_e x s_e\\
&= \frac{d}{\nu}U(g)\rho^2(s_e') x s_e = \frac{d}{\nu}U(g) x \rho(s_e')s_e = U(g)x = U_\mathcal{K}(g)x.
\end{split}
\end{align*}
\noindent Finally, using the normalization relation $s_e' = s_e'U(g)$, we have
\begin{align*}
\begin{split}
%\varphi^2(\alpha_g(\varphi^{-2}(x))) &= d^4 \rho^2(s_e')\rho^3(s_e')\rho^2(\alpha_g(s_e'\rho(s_e')\rho^2(x)\rho^2(s_e)\rho(s_e)))\rho(s_e)s_e\\
% &= d^4 \rho^2(s_e')\rho^3(s_e')\rho(U(g))\rho^2(s_e')\rho^3(s_e')\rho^4(x)\rho^4(s_e)\rho^3(s_e)\rho(U(g)^{-1})\rho(s_e)s_e\\
% &= 
\varphi^{-1}(\alpha_g(\varphi(x))) &= \frac{d^2}{\nu^2}s_e'\rho(\alpha_g(\rho^2(s_e')\rho(x) s_e))\rho(s_e) = \frac{d^2}{\nu^2}s_e'U_g\rho^3(s_e')\rho^2(x)\rho(s_e)U_g^{-1}\rho(s_e)\\
&= \frac{d^2}{\nu^2}s_e'\rho^3(s_e')\rho^2(x)\rho(s_e)U_g^{-1}\rho(s_e) = \frac{d^2}{\nu^2}\rho(s_e')s_e'\rho^2(x)\rho(s_e)U_g^{-1}\rho(s_e)\\
&= \frac{d^2}{\nu^2}\rho(s_e')xs_e'\rho(s_e)U_g^{-1}\rho(s_e) = \frac{d}{\nu}\rho(s_e'U_{g^{-1}}^{-1})xU_{g^{-1}}\rho(s_e)\\
&= \frac{d}{\nu}\rho(s_e')\rho(U_{g^{-1}}^{-1})x\rho(\alpha_{g^{-1}}(s_e))U_{g^{-1}} = \frac{d}{\nu}\rho(s_e')\rho(U_{g^{-1}}^{-1})\rho^2(\alpha_{g^{-1}}(s_e))xU_{g^{-1}}\\
&= \frac{d}{\nu}\rho(s_e')\rho(U_{g^{-1}}^{-1})\rho(U_{g^{-1}})\rho^2(s_e)\rho(U_{g^{-1}}^{-1})xU_{g^{-1}} = \rho(U_{g^{-1}}^{-1})xU_{g^{-1}}.
% &= \frac{d^2}{\nu^2}\rho(s_e')xs_e'U(g)\rho(s_e)U(g)^{-1}\rho(s_e) = \frac{d^2}{\nu^2}\rho(s_e')xs_e'U(g)\rho(s_e)U(g)^{-1}\rho(s_e)
%\rho(U(g)) x U(g)^{-1}.
\end{split}
\end{align*}
%\noindent together with
%\begin{align*}
%\begin{split}
%\varphi^{-1}(\alpha_g(\varphi(x))) = \frac{d^2}{\nu^2}s_e'\rho(\alpha_g(\rho^2(s_e')\rho(x) s_e))\rho(s_e) = \cdots = \rho(U(g)) x U(g)^{-1}.
%\end{split}
%\end{align*}
\noindent %The final result follows from the fact that $\varphi^3(x) = \nu x$. 
This completes the proof.
\end{proof}\\

\noindent The result $\varphi V(g) \varphi^{-1}x = U_\mathcal{K}(g)x$ can be rewritten as $\varphi V(g) = U_\mathcal{K}(g) \varphi$. In other words, the representations $U_\mathcal{K}$ and $V$ are equivalent and intertwined by $\varphi$. This lemma also admits the following ``dual'' that is proved similarly but with left duals rather than right duals for the order $3$ condition.\\

\noindent\begin{lemma}\label{OneClickRotation'} The map $\varphi' \in \textup{End}(\mathcal{K}') = \mathcal{K}\mathcal{K}'$ given by
\begin{align*}
\begin{split}
\varphi' : y &\mapsto \frac{d}{\nu}s_e'\rho(y)\rho^2(s_e)
%\varphi : x &\mapsto d\rho^2(s_e')\rho(x)s_e
\end{split}
\end{align*}
\noindent is invertible, admitting the inverse
\begin{align*}
\begin{split}
\varphi'^{-1} : y &\mapsto \frac{d}{\nu}\rho(s_e')\rho(y)s_e.
%\varphi^{-1} : x &\mapsto d s_e'\rho(x)\rho(s_e),
\end{split}
\end{align*}
%\noindent Moreover, we have
\noindent Moreover, we have
\begin{align*}
\begin{split}
\varphi'^3(y) = \nu y,\qquad\varphi'(\alpha_g(\varphi'^{-1}(y))) = yU_\mathcal{K}(g)^{-1}
\end{split}
\end{align*}
\noindent and
\begin{align*}
\begin{split}
\varphi'^{-1}(\alpha_g(\varphi'(y))) = U(g) y\rho(U(g)^{-1})
\end{split}
\end{align*}
\noindent for all $y \in \mathcal{K}'$ and $g \in G$.\\
\end{lemma}

\noindent Just as with \hyperref[OneClickRotation]{Lemma \ref*{OneClickRotation}}, we have $y\varphi'^{-1}V(g)^{-1}\varphi' = yU_\mathcal{K}(g)^{-1}$ and hence that $\varphi'$ intertwines $U_\mathcal{K}$ and $V$ as representations on $\textup{End}(\mathcal{K}')$.\\

\noindent\begin{lemma}\label{RhoK} There exists a unique linear map $l : \mathcal{K} \to \mathcal{K}^2\mathcal{K}'$ such that
\begin{align*}
\begin{split}
%\rho(x) = \sum_{h \in G}{s_h V(h) s_e'\rho(x)} + \sum_{h \in G}{\alpha_h(\varphi(x))s_hs_h'} + l(x)
\rho(x) = \sum_{h \in G}{s_h s_h'\rho(x)} + \sum_{h \in G}{\alpha_h(\varphi(x))s_hs_h'} + l(x)
\end{split}
\end{align*}
\noindent for all $x \in \mathcal{K}$, where $\alpha_g \circ l = l$ for all $g \in G$.\\
\end{lemma}

\noindent\begin{proof}  %We will determine $S\rho(x)$ and $T\rho(x)$ independently. Well,
%\begin{align*}
%\begin{split}
%S\rho(x) = \sum_{h \in G}{s_h s_h' \rho(x)} = \sum_{h \in G}{s_h \alpha_h(s_e'\rho(x))} = \sum_{h \in G}{s_h V(h) s_e'\rho(x)}.
%\end{split}
%\end{align*}
%As for $T\rho(x)$, note
Let's determine $T\rho(x)$. Observe that $\mathcal{K}'\rho(\mathcal{K}) \subset \homset_{\textcat{End}(A)}({\rho \otimes \rho}, {\rho \otimes \rho})$. Using the same decomposition we used to define $U_\mathcal{K}(g)$ together with the fact that $T\rho(x) \in \mathcal{K}\mathcal{K}'\rho(\mathcal{K})$, we know there must exist linear maps $l_h : \mathcal{K} \to \mathcal{K}$, for $h \in G$, and $l : \mathcal{K} \to \mathcal{K}^2\mathcal{K}'$ such that
\begin{align*}
\begin{split}
T\rho(x) = \sum_{h \in G}{l_h(x) s_h s_h'} + l(x).
\end{split}
\end{align*}
\noindent Because $\alpha_g \circ \rho = \rho$, we must have
\begin{align*}
\begin{split}
T\alpha_g(\rho(x)) = \sum_{h \in G}{\alpha_g(l_h(x)) s_{gh} s_{gh}'} + \alpha_g(l(x)) = T\rho(x);
\end{split}
\end{align*}
\noindent that is, $l_g = \alpha_g \circ l_e$ and $\alpha_g \circ l = l$. We claim that $l_e = \varphi$. To this end, consider the following.
\begin{align*}
\begin{split}
T\rho(\varphi^{-1}(x))S &= T\rho(\varphi^{-1}(x))\sum_{h \in G}{s_hs_h'} = T\frac{d}{\nu}\rho(s_e')\rho^2(x)\rho^2(s_e)\sum_{h \in G}{s_hs_h'}\\
&= T\frac{d}{\nu}\rho(s_e')\rho^2(x)\sum_{h \in G}{s_hs_hs_h'} = T\frac{d}{\nu}\rho(s_e')\sum_{h \in G}{s_h\alpha_h(x)s_hs_h'}\\
&= T\sum_{h \in G}{\alpha_h(x)s_hs_h'} = \sum_{h \in G}{\alpha_h(x)s_hs_h'}.
%
%T\rho(\varphi^{-1}(x)) &= T\rho(\varphi^{-1}(x))(S + T)\\
% &= T\rho(\varphi^{-1}(x))\sum_{h \in G}{s_hs_h'} + T\rho(\varphi^{-1}(x))T\\
% &= T\frac{d}{\nu}\rho(s_e')\rho^2(x)\rho^2(s_e)\sum_{h \in G}{s_hs_h'} + T\rho(\varphi^{-1}(x))T\\
% &= T\frac{d}{\nu}\rho(s_e')\rho^2(x)\sum_{h \in G}{s_hs_hs_h'} + T\rho(\varphi^{-1}(x))T\\
% &= T\frac{d}{\nu}\rho(s_e')\sum_{h \in G}{s_h\alpha_h(x)s_hs_h'} + T\rho(\varphi^{-1}(x))T\\
% &= T\sum_{h \in G}{\alpha_h(x)s_hs_h'} + T\rho(\varphi^{-1}(x))T\\
% &= \sum_{h \in G}{\alpha_h(x)s_hs_h'} + l(\varphi^{-1}(x))T.
%
% T\rho(\varphi^{-1}(x)) &= T\frac{d}{\nu}\rho(s_e')\rho^2(x)\rho^2(s_e)\\
% &= \frac{d}{\nu}T\rho(s_e')\rho^2(x)\sum_{h \in G}{s_hs_hs_h'} + \frac{d}{\nu}T\sum_{i=1}^m{\rho(s_e')\rho^2(x)t_i\rho(s_e)t_i'}\\
% &= \frac{d}{\nu}T\rho(s_e')\sum_{h \in G}{s_h\alpha_h(x)s_hs_h'} + \frac{d}{\nu}T\rho(s_e')\sum_{i=1}^m{t_i\rho(x)\rho(s_e)t_i'}\\
% &= T\sum_{h \in G}{\alpha_h(x)s_hs_h'} + \frac{d}{\nu}T\sum_{i=1}^m{\rho(s_e')t_i\rho(x)\rho(s_e)t_i'}\\
% &= \sum_{h \in G}{\alpha_h(x)s_hs_h'} + \frac{d}{\nu}\sum_{i=1}^m{\sum_{j=1}^m{t_jt_j'\rho(s_e')t_i\rho(x)\rho(s_e)t_i'}}.
% &= \frac{d}{\nu}T\rho(s_e')\sum_{h \in G}{s_h\alpha_h(x)s_hs_h'} + \frac{d}{\nu}T\sum_{i=1}^m{\rho(s_e')\rho^2(x)\rho^2(s_e)t_it_i'}\\
% &= T\sum_{h \in G}{\alpha_h(x)s_hs_h'} + \frac{d}{\nu}T\sum_{i=1}^m{\rho(s_e')\rho^2(x)\rho^2(s_e)t_it_i'}\\
% &= \sum_{h \in G}{\alpha_h(x)s_hs_h'} + \frac{d}{\nu}T\rho(\varphi^{-1}(x))T.
\end{split}
\end{align*}
\noindent Thus we have shown that $\alpha_h(l_e(\varphi^{-1}(x))) = \alpha_h(x)$, whence composing both sides with $\alpha_{h^{-1}}$ gives us the desired result. This completes the proof.
\end{proof}\\

\noindent Once again, we have the following ``dual'' to the lemma above.\\

\noindent\begin{lemma}\label{RhoK'} There exists a unique linear map $l' : \mathcal{K}' \to \mathcal{K}(\mathcal{K}')^2$ such that
\begin{align*}
\begin{split}
%\rho(x) = \sum_{h \in G}{s_h V(h) s_e'\rho(x)} + \sum_{h \in G}{\alpha_h(\varphi(x))s_hs_h'} + l(x)
\rho(y) = \sum_{h \in G}{\rho(y)s_h s_h'} + \sum_{h \in G}{s_hs_h'\alpha_h(\varphi'(y))} + l'(y)
\end{split}
\end{align*}
\noindent for all $y \in \mathcal{K}'$, where $\alpha_g \circ l' = l'$ for all $g \in G$.\\
\end{lemma}

\noindent The proof follows from the fact that $\rho(\mathcal{K}')\mathcal{K} \subset \homset_{\textcat{End}(A)}({\rho \otimes \rho}, {\rho \otimes \rho})$, with the rest of the proof being essentially the same but with the order of multiplication reversed.\\

\newpage\noindent\\\newpage

\noindent We have thus shown that $\rho$ and $\alpha_g$ are endomorphisms of the Leavitt algebra, and moreover that the \textcolor{red}{$6j$-symbols of $\mathcal{C}$} are determined by
\begin{enumerate}[label=\textup{(\roman*).}, ref=\textup{(\roman*)}, leftmargin=4\parindent]
\item two finite-dimensional vector spaces $\mathcal{K}$ and $\mathcal{K}'$;
\item four linear maps $j_1, j_2 \in \homset(\mathcal{K}, \mathcal{K}')$ and $j_1', j_2' \in \homset(\mathcal{K}', \mathcal{K})$;
\item two invertible linear maps $\varphi \in \textup{Aut}(\mathcal{K})$ and $\varphi' \in \textup{Aut}(\mathcal{K}')$;
\item two representations $U_\mathcal{K}, V : G \to \textup{Aut}(\mathcal{K})$;
\item a symmetric bicharacter $\chi : G \times G \to \mathbbm{k}^\times$;
\item two linear maps $l \in \homset(\mathcal{K}, \mathcal{K} \otimes \mathcal{K})$ and $l' \in \homset(\mathcal{K}', \mathcal{K}' \otimes \mathcal{K}')$.
\end{enumerate}
\noindent\textcolor{red}{I don't think we need to make any choices, although I chose $a = \nu/d$.}\\

\noindent Recall that the Leavitt algebra $\mathcal{L}$ admits an involutive antiautomorphism $-' : \mathcal{L} \to \mathcal{L}$ that exchanges each $s_g$ and $t_i$ with their primed versions and vice versa. Note that $\rho$, $\alpha_g$ and $\varphi$ are \textcolor{red}{NOT?} compatible with this map; %in particular, 
$\rho(-)' \neq \rho(-')$, $\alpha_g(-)' \neq \alpha_g(-')$ and $\varphi(-)' \neq \varphi'(-')$. We shall henceforth define $\varphi$ and $l$ on $\mathcal{K}'$ via $\varphi'$ and $l'$, respectively. \textcolor{red}{Double-check this stuff. Maybe replace mentions of $\varphi'$ and $l'$ in what follows.} \textcolor{red}{Check Corollary 1 and Lemma 2 of EG17, i.e. $\homset(\beta, \gamma) = \homset(\tilde{\beta}, \tilde{\gamma})$.}\\

\noindent Our setting diverges from the unitary setting here due to the fact that $U(g)'$ is not necessarily equal to $U(g)^{-1}$, since $\overline{\chi_h(g)}$ is not necessarily equal to $\chi_h(g)^{-1}$ and $U_\mathcal{K}(g)'$ is not necessarily equal to $U_\mathcal{K}(g)^{-1}$.\\

\noindent\textcolor{red}{TO-DO: possibly revisit the other conditions in Izumi's lemma 3.4.}\\

\noindent\textcolor{red}{Prove other properties. The following results follow the work of Izumi closely. First, we work up to a classification theorem.}\\

\noindent\begin{lemma}\label{xVU} We have
\begin{gather*}
\chi_h(g)V(h)U_\mathcal{K}(g) = U_\mathcal{K}(g)V(h),\\
l(\alpha_g(x)) = U(g)l(x)U(g)^{-1},\\
l'(\alpha_g(y)) = U(g)l'(y)U(g)^{-1},
\end{gather*}
\noindent for all $x \in \mathcal{K}$, $y \in \mathcal{K}'$ and $g, h \in G$.\\
\end{lemma}

\noindent\begin{proof} These relations follow from $\rho(\alpha_g(x)) = U(g)\rho(x)U(g)^{-1}$. By \hyperref[OneClickRotation]{Lemma \ref*{OneClickRotation}} and \hyperref[RhoK]{Lemma \ref*{RhoK}},
\begin{align*}
\begin{split}
\rho(\alpha_g(x)) &= \sum_{h\in G} s_hs_h'\rho(\alpha_g(x)) + \sum_{h\in G} V(h)\varphi(\alpha_g(x))s_hs_h' + l(\alpha_g(x))\\
&= \sum_{h\in G} s_hs_h'U(g)\rho(x)U(g)^{-1} + \sum_{h\in G} V(h)U_\mathcal{K}(g)\varphi(x)s_hs_h' + l(\alpha_g(x)).
\end{split}
\end{align*}
\noindent Meanwhile,
\begin{align*}
\begin{split}
U(g)\rho(x)U(g)^{-1} &= \sum_{h\in G} U(g)s_hs_h'\rho(x)U(g)^{-1} + \sum_{h\in G} U(g)V(h)\varphi(x)s_hs_h'U(g)^{-1} + U(g)l(x)U(g)^{-1}\\
&= \sum_{h\in G} s_hs_h'U(g)\rho(x)U(g)^{-1} + \sum_{h\in G} \chi_h(g)^{-1}U_\mathcal{K}(g)V(h)\varphi(x)s_hs_h' + U(g)l(x)U(g)^{-1}.
\end{split}
\end{align*}
\noindent The final equation follows from \hyperref[OneClickRotation']{Lemma \ref*{OneClickRotation'}} and \hyperref[RhoK']{Lemma \ref*{RhoK'}}. This completes the proof.
\end{proof}\\

\noindent\begin{corollary}\label{ChiBicharacter} The map $\chi$ is a symmetric bicharacter. In particular,
\begin{gather*}
\chi_{h}(g) = \chi_g(h),\\
\alpha_h(U(g)) = \chi_h(g)^{-1} U(g)
\end{gather*}
\noindent for all $g, h \in G$.\\
\end{corollary}

\noindent\begin{proof} Observe that
\begin{align*}
\begin{split}
\rho(\alpha_g(x)) = \alpha_h(\rho(\alpha_g(x))) = \alpha_h(U(g)\rho(x)U(g)^{-1}) = \alpha_h(U(g))\rho(x)\alpha_h(U(g)^{-1}).
\end{split}
\end{align*}
\noindent It follows that $\alpha_h(U(g))$ is a scalar multiple of $U(g)$. In particular,
\begin{align*}
\begin{split}
\alpha_h(U(g))s_e = \alpha_h(U(g)s_{h^{-1}}) = \chi_{h^{-1}}(g)\alpha_h(s_{h^{-1}}) = \chi_{h^{-1}}(g)s_e,
\end{split}
\end{align*}
\noindent showing that $\alpha_h(U(g)) = \chi_{h^{-1}}(g)U(g)$. To show that $\chi_h(g) = \chi_g(h)$, we first note that
\begin{align*}
\begin{split}
p(U(g^{-1}))s_e \in \homset_{\textcat{End}(A)}(1, \rho \otimes \rho \otimes \alpha_{g^{-1}}) = \homset_{\textcat{End}(A)}(\alpha_g, \rho \otimes \rho).
\end{split}
\end{align*}
\noindent In other words, $\rho(U(g^{-1}))s_e = c s_g$ for some $c \in \mathbbm{k}$. To show that $c = 1$, we see that
\begin{align*}
\begin{split}
\frac{d}{\nu}s_e'\rho(\rho(U(g^{-1}))s_e) = \frac{d}{\nu}s_e'\rho^2(U(g^{-1}))\rho(s_e) = \frac{d}{\nu}U(g^{-1})s_e'\rho(s_e) = U(g^{-1}),
\end{split}
\end{align*}
\noindent while
\begin{align*}
\begin{split}
\frac{d}{\nu}s_e'\rho(c s_g) = c\frac{d}{\nu}s_e'U(g)\rho(s_e)U(g)^{-1} = c\frac{d}{\nu}s_e'\rho(s_e)U(g^{-1}) = c U(g^{-1}).
\end{split}
\end{align*}
\noindent It follows that
\begin{align*}
\begin{split}
\frac{d}{\nu}\rho(\alpha_h(s_e'))\rho(\alpha_h(U(g)))\rho(U(g^{-1}))s_e = \frac{d}{\nu}\rho(\alpha_h(s_e'))\rho(\alpha_h(U(g)))s_g.
\end{split}
\end{align*}
\noindent But the left-hand side gives us
\begin{align*}
\begin{split}
\frac{d}{\nu}\rho(\alpha_h(s_e'))\rho(\alpha_h(U(g)))\rho(U(g^{-1}))s_e &= \chi_{h^{-1}}(g)\frac{d}{\nu}\rho(\alpha_h(s_e'))\rho(U(g))\rho(U(g^{-1}))s_e\\
&= \chi_{h^{-1}}(g)\frac{d}{\nu}\rho(\alpha_h(s_e'))s_e\\
&= \chi_{h^{-1}}(g)\frac{d}{\nu}U(h)\rho(s_e')U(h)^{-1}s_e\\
&= \chi_{h^{-1}}(g)U(h),
\end{split}
\end{align*}
\noindent while the right-hand side gives us
\begin{align*}
\begin{split}
\frac{d}{\nu}\rho(\alpha_h(s_e'))\rho(\alpha_h(U(g)))s_g &= \frac{d}{\nu}\rho(\alpha_h(s_e'U(g)))s_g\\
&= \frac{d}{\nu}\rho(\alpha_h(s_e'))s_g\\
&= \frac{d}{\nu}U(h)\rho(s_e')U(h)^{-1}s_g\\
&= \chi_g(h^{-1})\frac{d}{\nu}U(h)\alpha_g(\rho(s_e')s_e)\\
&= \chi_g(h^{-1})U(h).
\end{split}
\end{align*}
\noindent The result follows. This completes the proof.
\end{proof}\\

\noindent In what follows, we will let $\lambda$ denote the left regular representation.\\

\noindent\begin{theorem}\label{GroupProperties} If $d$ is irrational, then
\begin{enumerate}[label=\textup{(\roman*).}, ref=\textup{(\roman*)}, leftmargin=4\parindent]
\item $G$ is necessarily Abelian;
\item $m$ is a multiple of $n$;
\item $\bigoplus_{h \in G} \chi_h \cong \lambda$;
\item $V \cong U_\mathcal{K} \cong \frac{m}{n}\lambda$.
\end{enumerate}
\noindent Conversely, if $d$ is rational, there exist two natural numbers $s$ and $t$ such that $d = st$, $m = (s - 1)t$ and $n = st^2$. In particular,
\begin{enumerate}[label=\textup{(\roman*).}, ref=\textup{(\roman*)}, leftmargin=4\parindent]
\item there exist two natural numbers $s$ and $t$ such that $d = st$, $m = (s - 1)t$ and $n = st^2$;
\item when $t = 1$,
\begin{enumerate}[label=$\bullet$, leftmargin=4\parindent]
\item $G$ is Abelian,
\item $\chi_h$ is trivial for all $h \in G$,
\item $1 \oplus V \cong 1 \oplus U_\mathcal{K} \cong \lambda$;
\end{enumerate}
\item when $t > 1$,
\begin{enumerate}[label=$\bullet$, leftmargin=4\parindent]
\item $G$ is non-Abelian,
\item $\abs{\homset(G, \mathbbm{k}^\times)} = t^2$,
\item $\oplus_{h \in G} \chi_h \cong \oplus_{\chi \in \homset(G, \mathbbm{k}^\times)} \chi^{\oplus s}$,
\item $t$ divides $\dim(\pi)$, for all $\pi \in \hat{G}^\dagger$, where $\hat{G}^\dagger$ is the set of equivalence classes of irreducible unitary representations of $G$ with dimension greater than $1$,
\item $V \cong U_\mathcal{K} \cong \bigoplus_{\pi\in\hat{G}^\dagger}\frac{\dim(\pi)}{t}\pi$.\\
\end{enumerate}
\end{enumerate}
\end{theorem}

\noindent\begin{proof} For all $g \in G$, we have $\delta_{g,e} = \rho(s_e's_g) = \rho(s_e')U(g)\rho(s_e)U(g)^{-1}$. Since $U(e) = 1$,
\begin{align*}
\begin{split}
\delta_{g,e} = \rho(s_e')U(g)\rho(s_e) &= \frac{1}{d^2}\sum_{h \in G} \chi_h(g) + \sum_{i,j=1}^m \rho(s_e')t_it_i' U_\mathcal{K}(g)t_jt_j'\rho(s_e).%\\
% &= \frac{1}{d^2}\sum_{h \in G} \chi_h(g) + \rho(s_e')U_\mathcal{K}(g)\rho(s_e).
\end{split}
\end{align*}
\noindent Observe that $t_i' U_\mathcal{K}(g)t_j \in \mathbbm{k}$, as it is in the $1$-dimensional space $\homset_{\textcat{End}(A)}(\rho, \rho)$. It therefore follows from \hyperref[RhoExpansion]{Lemma \ref*{RhoExpansion}} that
\begin{align*}
\begin{split}
\delta_{g,e} &= \frac{1}{d^2}\sum_{h \in G} \chi_h(g) + \sum_{i,j=1}^m \rho(s_e')t_it_i' U_\mathcal{K}(g)t_jt_j'\rho(s_e)\\
&= \frac{1}{d^2}\sum_{h \in G} \chi_h(g) + \frac{1}{d}\sum_{i=1}^m t_i' U_\mathcal{K}(g)t_i\\
&= \frac{1}{d^2}\sum_{h \in G} \chi_h(g) + \frac{1}{d}\textup{Tr}(U_\mathcal{K}(g)).
\end{split}
\end{align*}
\noindent Because $\textup{Tr}(\lambda(g)) = n\delta_{g,e}$, we have
\begin{align*}
\begin{split}
\textup{Tr}(\lambda(g)) = \frac{n}{d^2}\sum_{h \in G} \chi_h(g) + \frac{n}{d}\textup{Tr}(U_\mathcal{K}(g)).
\end{split}
\end{align*}
\noindent Let $\chi \in \homset(G, \mathbbm{k}^\times)$ now be a $1$-dimensional representation. Denote by $a_\chi$ and $b_\chi$ the multiplicities of $\chi$ in the representations $\bigoplus_{h \in G}\chi_h$ and $U_\mathcal{K}$, respectively. Of course, $\chi$ has multiplicity $1$ in $\lambda$. Because the character of a direct sum of representations is the direct sum of the characters of those representations, inspecting the coefficient of $\chi$ on both sides of the previous equation gives us
\begin{align*}
\begin{split}
d^2 = na_\chi + nd b_\chi.
\end{split}
\end{align*}
\noindent If $d$ is irrational, we have no choice but to take $a_\chi = 1$ and $n b_\chi = m$. The former implies that
\begin{align*}
\begin{split}
\bigoplus_{h \in G}\chi_h \cong \bigoplus_{\chi \in \homset(G, \mathbbm{k}^\times)}\chi.
\end{split}
\end{align*}
\noindent In other words, $\abs{G} = \abs{\homset(G, \mathbbm{k}^\times)}$, whence $G$ must be Abelian.\\

\noindent Conversely, suppose that $d$ is rational. Then one can show that the integer $m \pm \sqrt{m^2 + 4n}$ is even and hence that $d$ is an integer. Now, any object tensored with its dual is Morita equivalent to $1$, so $\rho \otimes \rho$ is an algebra object with dimension $d^2$. The restriction of $\rho \otimes \rho$ to $\textcat{Vec}_G$, given by throwing away simple summands not in $\textcat{Vec}_G$, is $\bigoplus_{g \in G} \alpha_g$ with dimension $n$. Thus by \hyperref[QuotientAlgebraObject]{Theorem \ref*{QuotientAlgebraObject}}, there exists an algebra object with dimension $d^2/n$. Because this must be a sum of simple objects with dimensions $d$ or $1$, it follows that $d^2/n = 1 + md/n$ must be an integer. Let $s \coloneqq 1 + md/n$ and $t \coloneqq m/(s - 1)$. Note that $d^2 = n + md$ together with $n(s - 1) = md$ implies $n(s - 1)^2 = sm^2$, whence $t$ must also be an integer. In particular, $d = st$ and $n = st^2$. \textcolor{red}{This is pretty tedious. I'll finish it later. Need to adapt the category theory parts, since we no longer have a category.}
\end{proof}\\

\noindent\textcolor{red}{Does this theorem imply that $\chi$ is non-degenerate (that is, $\chi_h(g) = 1$ for all $h \in G$ if and only if $g = e$)?}\\

\noindent\begin{lemma}\label{RhoU} We have
\begin{align*}
\begin{split}
\rho(U(g)) = \sum_{h \in G} s_h s_{hg}' + \varphi U_\mathcal{K}(g) \varphi' \otimes U(g).
\end{split}
\end{align*}
\noindent for all $g \in G$.\\
\end{lemma}

\noindent\begin{proof} We have shown in the proof for \hyperref[ChiBicharacter]{Corollary \ref*{ChiBicharacter}} that $\rho(U(g))s_h = s_{hg^{-1}}$. Thus
\begin{align*}
\begin{split}
\rho(U(g))S = \sum_{h \in G}\rho(U(g))s_hs_h' = \sum_{h \in G}s_{hg^{-1}}s_h'.
\end{split}
\end{align*}
\noindent It follows that both $S$ and $T$ commute with $\rho(U(g))$, since
\begin{align*}
\begin{split}
\rho(U(g))T = \rho(U(g))(S + T) - \rho(U(g))S = (S + T)\rho(U(g)) - S\rho(U(g)) = T\rho(U(g)).
\end{split}
\end{align*}
\noindent Thus $\rho(U(g))T = \rho(U(g))T^2 = T\rho(U(g))T$ and
\begin{align*}
\begin{split}
\rho(U(g))(S + T) = \sum_{h \in G}s_hs_{hg}' + T\rho(U(g))T.
\end{split}
\end{align*}
\noindent Now, $t_i'\rho(U(g))t_j \in \homset_{\textcat{End}(A)}(\rho, \rho \otimes \alpha_g) = \mathbbm{k}U(g)$. That is, $t_i'\rho(U(g))t_j = U'(g)_{ij} U(g)$ for some $U'(g)_{ij} \in \mathbbm{k}$. %Thus $T\rho(U(g))T = U'(g) \otimes U(g)$, for $U'(g) \in \textup{End}(\mathcal{K})$. Because these matrices define a representation, $U' : G \to \textup{End}(\mathcal{K})$ is also a representation of $G$ and is determined by
Thus $T\rho(U(g))T = U'(g) \otimes U(g)$, where the matrix $U'(g)$ is determined by
\begin{align*}
\begin{split}
s_e't_i'\rho(U(g))t_js_e = U'(g)_{ij} = t_i'U'(g)t_j.
\end{split}
\end{align*}
\noindent Substituting
\begin{align*}
\begin{split}
U(g) = \sum_{h \in G}{\chi_h(g)s_hs_h'} + \sum_{p,q=1}^m{U_\mathcal{K}(g)_{pq}t_pt_q'},
\end{split}
\end{align*}
\noindent we obtain
\begin{align*}
\begin{split}
s_e't_i'\rho(U(g))t_js_e &= \sum_{h \in G}{\chi_h(g)s_e't_i'U(h)\rho(s_es_e')U(h)^{-1}t_js_e} + \sum_{p,q=1}^m{U_\mathcal{K}(g)_{pq}s_e't_i'\rho(t_pt_q')t_js_e}\\
&= \sum_{p,q=1}^m{U_\mathcal{K}(g)_{pq}s_e't_i'\rho(t_pt_q')t_js_e}.
\end{split}
\end{align*}
\noindent Because $t_i'\rho(t_p)$ and $\rho(t_q')t_j$ are in $\homset_{\textcat{End}(A)}({\rho \otimes \rho}, {\rho \otimes \rho})$, they are spanned by $\{s_gs_g'\}_{g \in G} \sqcup \{t_it_j'\}_{i,j = 1}^m$, just like $U(g)$. We therefore have that
\begin{align*}
\begin{split}
t_i'\rho(t_p) &= \sum_{h \in G} a_h(g) s_hs_h' + \sum_{k,l=1}^m a_{kl}(g) t_kt_l',\\
\rho(t_q)t_j &= \sum_{h \in G} b_h(g) s_hs_h' + \sum_{k,l=1}^m b_{kl}(g) t_kt_l'
\end{split}
\end{align*}
\noindent and hence
\begin{align*}
\begin{split}
U'(g)_{ij} = s_e't_i'\rho(U(g))t_js_e &= \sum_{p,q=1}^m{U_\mathcal{K}(g)_{pq}s_e't_i'\rho(t_p)\rho(t_q')t_js_e} = \sum_{p,q=1}^m{U_\mathcal{K}(g)_{pq}a_e(g)b_e(g)}\\
&= \sum_{p,q=1}^m{U_\mathcal{K}(g)_{pq}d^2 \rho(s_e')t_i'\rho(t_p)s_es_e'\rho(t_q')t_j\rho(s_e)}\\
&= \sum_{p,q=1}^m{U_\mathcal{K}(g)_{pq}d^2 t_i'\rho^2(s_e')\rho(t_p)s_es_e'\rho(t_q')\rho^2(s_e)t_j}\\
&= \sum_{p,q=1}^m{U_\mathcal{K}(g)_{pq}t_i'\varphi(t_p)\varphi'(t_q')t_j}.
\end{split}
\end{align*}
\noindent Thus
\begin{align*}
\begin{split}
U'(g) &= \sum_{i,j=1}^m{t_iU'(g)_{ij}t_j'} = \sum_{p,q=1}^m{T \varphi(t_p)U_\mathcal{K}(g)_{pq}\varphi'(t_q') T} = \sum_{p,q=1}^m{\varphi t_pU_\mathcal{K}(g)_{pq}t_q'\varphi'} = \varphi U_\mathcal{K}(g) \varphi',
\end{split}
\end{align*}
\noindent whence the result follows. This completes the proof.
\end{proof}\\

\noindent\textcolor{red}{This is the first point where our setting diverges from the unitary setting.}\\

%\noindent Suppose
%\begin{align*}
%\begin{split}
%U_\mathcal{K}(g) = \sum_{i,j=1}^m u_{ij}(g)t_it_j'\qquad\text{and}\qquad V(g) = \sum_{i,j=1}^m v_{ij}(g)t_it_j'.
%\end{split}
%\end{align*}

\noindent\begin{lemma}\label{Schur} Let $\pi : G \to \textup{End}(A)$ and $\sigma : G \to \textup{End}(B)$ be irreducible representations of $G$. Then
\begin{align*}
\begin{split}
\sum_{g \in G} \pi(g) \circ \gamma \circ \sigma(g)^{-1} = n\delta_{\pi,\sigma}
\end{split}
\end{align*}
\noindent for any $\gamma : B \to A$.\\
\end{lemma}

\noindent\begin{proof} Define
\begin{align*}
\begin{split}
\psi \coloneqq \sum_{g \in G} \pi(g) \circ \gamma \circ \sigma(g)^{-1}.
\end{split}
\end{align*}
\noindent Then for any $h \in G$, we have that
\begin{align*}
\begin{split}
\pi(h) \circ \psi \circ \sigma(h)^{-1} = \sum_{g \in G} \pi(hg) \circ \gamma \circ \sigma(hg)^{-1} = \psi.
\end{split}
\end{align*}
\noindent Thus $\psi$ intertwines $\pi$ and $\sigma$. Provided $\mathbbm{k}$ is algebraically closed, it follows from Schur's lemma that
\begin{align*}
\begin{split}
\sum_{g \in G} \pi(g) \circ \gamma \circ \sigma(g)^{-1} = n\delta_{\pi,\sigma}.
\end{split}
\end{align*}
%\noindent Let $\gamma(g) \coloneqq \pi(g)\sigma(g)^{-1}$. If $\gamma(g) = 1$ for all $g \in G$, then
%\begin{align*}
%\begin{split}
%\sum_{g \in G} \pi(g)\sigma(g)^{-1} = \sum_{g \in G} \gamma(g) = n,
%\end{split}
%\end{align*}
%\noindent and moreover $\gamma(g) = 1$ if and only if $\pi = \sigma$. Suppose instead that there exists some $x \in G$ for which $\gamma(x) \neq 1$. Then
%\begin{align*}
%\begin{split}
%\gamma(x)\sum_{g \in G}\gamma(g) = \sum_{g \in G}\gamma(xg) = \sum_{g \in G} \gamma_{h,k}(g),
%\end{split}
%\end{align*}
%\noindent whence
%\begin{align*}
%\begin{split}
%(\gamma_{h,k}(x) - 1)\sum_{g \in G}\gamma_{h,k}(g) = 0.
%\end{split}
%\end{align*}
%\noindent Since $\gamma_{h,k}(x) \neq 1$, and assuming that $\gamma_{h,k}(x) - 1$ is invertible in $\mathbbm{k}$, the sum must be zero. Thus
%\begin{align*}
%\begin{split}
%\sum_{g \in G}\chi_h(g)\chi_k(g)^{-1} = n\delta_{\chi_h,\chi_k}.
%\end{split}
%\end{align*}
\noindent This completes the proof.
\end{proof}\\

\noindent\begin{lemma} Let $\hat{G}^V$ be the set of irreducible summands of $V$ and let
\begin{align*}
\begin{split}
(U_\mathcal{K}, \mathcal{K}) = \bigoplus_{\pi \in \hat{G}^V}\bigoplus_{a=1}^{m_\pi} (\pi, \mathcal{K}_\pi^a)
\end{split}
\end{align*}
\noindent be the corresponding internal direct sum decomposition, where $m_\pi$ is the multiplicity of $\pi$. Moreover, let $\{t_{\pi,i}^a\}_{i=1}^{\dim(\pi)}$ be a basis for $\mathcal{K}_\pi^a \subseteq \mathcal{K}$ such that
\begin{align*}
\begin{split}
U_\mathcal{K}(g) = \sum_{\pi \in \hat{G}^V}\sum_{a=1}^{m_\pi}\pi(g) t_{\pi}^a {t_{\pi}^a}' = \sum_{\pi \in \hat{G}^V}\sum_{a=1}^{m_\pi}\sum_{i,j=1}^{\dim(\pi)}\pi(g)_{ij} t_{\pi,i}^a {t_{\pi,j}^a}',
\end{split}
\end{align*}
\noindent where $\{{t_{\pi,i}^a}'\}_{i=1}^{\dim(\pi)}$ is the unique set of projections corresponding to $\{t_{\pi,i}^a\}_{i=1}^{\dim(\pi)}$ by \cite[Theorem VIII.2.2]{Mac13}. Then
\begin{gather*}
\sum_{\pi,a,b,i,j}\frac{n}{\dim(\pi)}t_{\pi,i}^a {t_{\pi,j}^a}'\rho(s_e)\rho(s_e') t_{\pi,j}^b{t_{\pi,j}^b}' + \sum_{i=1}^m l(t_i)l'(t_i) = T \otimes T,\\
\sum_{i=1}^m l(t_i)V(h)\rho(t_i')s_e + \frac{\nu n}{\sqrt{d}} \sum_{\pi,a}\delta_{\chi_h,\pi} t_\pi^a {t_\pi^a}' \rho(s_e) = 0.
\end{gather*}
\noindent\\
\end{lemma}

\noindent\begin{proof} To begin, we first note that by \hyperref[RhoExpansion]{Lemma \ref*{RhoExpansion}},
\begin{align*}
\begin{split}
\sum_{g \in G}\rho(s_g)\rho(s_g') =\ &\sum_{g \in G} U(g)\rho(s_e)\rho(s_e')U(g)^{-1}\\
=\ &\frac{1}{d^2}\sum_{g,h,k \in G} \chi_h(g)\chi_k(g)^{-1} s_hs_k'\ +\\
&\frac{\nu}{d}\sum_{g,k \in G}\sum_{\pi,a,i,j} \pi(g)_{ij}\chi_k(g)^{-1} t_{\pi,i}^a{t_{\pi,j}^a}'\rho(s_e)s_k'\ +\\
&\frac{\nu}{d}\sum_{g,h \in G}\sum_{\pi,a,i,j} \chi_h(g)\pi(g)_{ij}^{-1} s_h\rho(s_e')t_{\pi,i}^a{t_{\pi,j}^a}'\ +\\
&\sum_{g \in G}\sum_{\pi,a,i,j}\sum_{\sigma,b,p,q} \pi(g)_{ij}\sigma(g)_{pq}^{-1} t_{\pi,i}^a{t_{\pi,j}^a}'\rho(s_e)\rho(s_e')t_{\sigma,p}^b{t_{\sigma,q}^b}'.
\end{split}
\end{align*}
\noindent By \hyperref[Schur]{Lemma \ref*{Schur}}, \textcolor{red}{check $1/\dim(\pi)$}
\begin{align*}
\begin{split}
\sum_{g \in G}\rho(s_g)\rho(s_g') =\ &\frac{n}{d^2}\sum_{h,k \in G} \delta_{\chi_h,\chi_k} s_hs_k'\ +\\
&\frac{n\nu}{d}\sum_{k \in G}\sum_{\pi,a} \delta_{\pi,\chi_k} t_{\pi}^a{t_{\pi}^a}'\rho(s_e)s_k'\ +\\
&\frac{n\nu}{d}\sum_{h \in G}\sum_{\pi,a} \delta_{\chi_h,\pi} s_h\rho(s_e')t_{\pi}^a{t_{\pi}^a}'\ +\\
&n\sum_{g \in G}\sum_{\pi,a,i,j} \frac{1}{\dim(\pi)}t_{\pi,i}^a{t_{\pi,j}^a}'\rho(s_e)\rho(s_e')t_{\pi,i}^b{t_{\pi,j}^b}'.
\end{split}
\end{align*}
\noindent Conversely, by \hyperref[RhoK]{Lemma \ref*{RhoK}},
\begin{align*}
\begin{split}
\sum_{i = 1}^m\rho(t_i)\rho(t_i') =\ &\sum_{i = 1}^m\sum_{h,k\in G}s_hs_h'\rho(t_i)\rho(t_i')s_ks_k' + \sum_{i = 1}^m\sum_{h,k\in G}s_hs_h'\rho(t_i)s_ks_k'\varphi'(t_i')V(k)^{-1}\ +\\
&\sum_{i = 1}^m\sum_{h\in G}s_hs_h'\rho(t_i)l'(t_i') + \sum_{i = 1}^m\sum_{h,k\in G}V(h)\varphi(t_i)s_hs_h'\rho(t_i')s_ks_k'\ +\\
&\sum_{i = 1}^m\sum_{h\in G}V(h)\varphi(t_i)s_hs_h'\varphi'(t_i')V(h)^{-1} + \sum_{i = 1}^m\sum_{h\in G}V(h)\varphi(t_i)s_hs_h'l'(t_i')\ +\\
&\sum_{i = 1}^m\sum_{h\in G}l(t_i)\rho(t_i')s_hs_h' + \sum_{i = 1}^m\sum_{h\in G}l(t_i)s_hs_h'\varphi'(t_i')V(h)^{-1} + \sum_{i = 1}^m l(t_i)l'(t_i').
\end{split}
\end{align*}
\noindent Now, note that $\rho(t_i')s_k \in \mathcal{K}$, while $s_h'\rho(t_i) \in \mathcal{K}'$. In particular, we have \textcolor{red}{check this}
\begin{align*}
\begin{split}
\sum_{i = 1}^m s_h'\rho(t_i)\rho(t_i')s_k = \sum_{i = 1}^m s_e'\rho(t_i)V(h^{-1}k)\rho(t_i')s_e = \textup{Tr}(V(h^{-1}k)).
\end{split}
\end{align*}
\noindent \textcolor{red}{Somehow $V(h)\varphi(t_i) = t_i$?} Since $V$ is equivalent to $U_\mathcal{K}$, it follows from \hyperref[GroupProperties]{Theorem \ref*{GroupProperties}} that
\begin{align*}
\begin{split}
\textup{Tr}(V(h^{-1}k)) &= \textup{Tr}(U_\mathcal{K}(h^{-1}k)) = \frac{d}{n}\textup{Tr}(\lambda(h^{-1}k)) - \frac{1}{d}\sum_{g\in G}\chi_k(g)\chi_h(g)^{-1} = d\delta_{h,k} - \frac{n}{d}\delta_{\chi_h,\chi_k}.
\end{split}
\end{align*}
\noindent\textcolor{red}{Conclude using $S + T = 1$.} This completes the proof.
\end{proof}\\

\noindent\begin{lemma} In terms of $l$ and $l'$, \hyperref[RhoU]{Lemma \ref*{RhoU}} is equivalent to\\
\begin{align*}
\begin{split}
\varphi U_\mathcal{K}(g) \varphi' \otimes U_\mathcal{K}(g) =\ &\sum_{\pi,\sigma,a,b,i,j,p,q}\sum_{h \in G}\chi_g(h)\pi(h)_{ij}\sigma(h)_{pq}^{-1}t_{\pi,i}^a {t_{\pi,j}^a}'\rho(s_e)\rho(s_e') t_{\sigma,q}^b{t_{\sigma,p}^b}'\ +\\
&\sum_{\pi,a,i,j}\pi(g)_{ij}l(t_{\pi,i}^a)l'({t_{\pi,j}^a}').
\end{split}
\end{align*}
\noindent\\
\end{lemma}

\noindent\begin{proof} Note that
\begin{align*}
\begin{split}
S\varphi U_\mathcal{K}(g) \varphi' = 0.
\end{split}
\end{align*}
\noindent In other words, \hyperref[RhoU]{Lemma \ref*{RhoU}} holds if and only if
\begin{align*}
\begin{split}
S\rho(U(g))S = \sum_{h \in G}s_hs_{hg}'\qquad\text{and}\qquad T\rho(U(g))T = \varphi U_\mathcal{K}(g) \varphi' \otimes U(g).
\end{split}
\end{align*}
\noindent 

\noindent Now, $S\rho(U(g))S$ does not give any condition on $l$, since
\begin{align*}
\begin{split}
.
\end{split}
\end{align*}
\noindent On the other hand, for $T\rho(U(g))T$, we have
\begin{align*}
\begin{split}
T\rho(U(g))T = \sum_{h \in G}\chi_h(g)TU(h)\rho(s_e)\rho(s_e')U(h)^{-1}T + \sum_{\pi,a,i,j}\pi(g)_{ij}T\rho(t_{\pi,i}^a{t_{\pi,j}^a}')T.
\end{split}
\end{align*}
\noindent The first term is simply
\begin{align*}
\begin{split}
\sum_{h \in G}\chi_h(g)TU(h)\rho(s_es_e')U(h)^{-1}T
= \sum_{\pi,\sigma,a,b,i,j,p,q}\sum_{h \in G}\chi_g(h)\pi(h)_{ij}\sigma(h)_{pq}^{-1}t_{\pi,i}^a {t_{\pi,j}^a}'\rho(s_es_e')t_{\sigma,q}^b{t_{\sigma,p}^b}'.
\end{split}
\end{align*}
\noindent As for the second, \textcolor{red}{check this, period 3 condition?}
\begin{align*}
\begin{split}
\sum_{\pi,a,i,j}\pi(g)_{ij}T\rho(t_{\pi,i}^a{t_{\pi,j}^a}')T &= \sum_{h \in G}\sum_{\pi,a,i,j}\pi(g)_{ij}V(h)\varphi(t_{\pi,i}^a)s_hs_h'\varphi({t_{\pi,j}^a}')V(h)^{-1} + \sum_{\pi,a,i,j}\pi(g)_{ij}l(t_{\pi,i}^a)l'({t_{\pi,j}^a}')\\
&= \sum_{h \in G}V(h)\varphi U_\mathcal{K}(g) \varphi'V(h)^{-1} \otimes s_hs_h' + \sum_{\pi,a,i,j}\pi(g)_{ij}l(t_{\pi,i}^a)l'({t_{\pi,j}^a}')\\
&= \sum_{h \in G}\varphi U_\mathcal{K}(g) \varphi' \otimes \chi_h(g)s_hs_h' + \sum_{\pi,a,i,j}\pi(g)_{ij}l(t_{\pi,i}^a)l'({t_{\pi,j}^a}').
\end{split}
\end{align*}
\noindent The result follows, since
\begin{align*}
\begin{split}
\varphi U_\mathcal{K}(g) \varphi' \otimes U(g) - \sum_{h \in G}\varphi U_\mathcal{K}(g) \varphi' \otimes \chi_h(g)s_hs_h' = \varphi U_\mathcal{K}(g) \varphi' \otimes U_\mathcal{K}(g).
\end{split}
\end{align*}
\noindent This completes the proof.
\end{proof}\\

\noindent\textcolor{red}{Check Corollary 1 and Lemma 2 of EG17, i.e. $\homset(\beta, \gamma) = \homset(\tilde{\beta}, \tilde{\gamma})$.}\\

\newpage

\ruledsection{General Reconstruction}{5}
\noindent\\ In 

\newpage

\ruledsection{Irrational Reconstruction}{6}
\noindent\\ Let $v_0 \coloneqq V$, $v_1 \coloneqq U_K$, $v_2 \coloneqq \varphi U_K(g) \varphi^{-1}$. Then by \hyperref[OneClickRotation]{Lemma \ref*{OneClickRotation}} and \hyperref[xVU]{Lemma \ref*{xVU}}, these satisfy
\begin{align*}
\begin{split}
\varphi v_i(g) \varphi^{-1} = v_{i+1}(g)\qquad\text{and}\qquad v_{i+1}(g)v_i(h) = \chi_h(g) v_i(h)v_{i+1}(g),
\end{split}
\end{align*}
\noindent for $i \in \mathbb{Z}/3\mathbb{Z}$. This motivates the following lemma.\\

\noindent\begin{lemma} 
\end{lemma}

\newpage

\ruledsection{Reconstruction for $m = \abs{G}$}{7}
\noindent\\ Let $G$ be a finite Abelian group of order $n$ and $a : G \to \mathbbm{k}^\times$ a self-inverse projective representation whose $2$-cocycle is given by a non-degenerate symmetric bicharacter $\langle\cdot, \cdot\rangle : G \times G \to \mathbbm{k}^\times$. That is,
\begin{align*}
\begin{split}
a(e) = 1,\qquad a(g)a(h) = \langle g, h\rangle a(gh)\qquad\text{and}\qquad a(g) = a(g^{-1}),
\end{split}
\end{align*}
\noindent for all $g, h \in G$. Moreover, suppose we have $b : G \to \mathbbm{k}$ and $c \in \mathbbm{k}^\times$ such that
\begin{gather*}
a(g)b(g^{-1}) = b(g)^{-1},\\
c\frac{\sqrt{n}}{d} + \sum_{g \in G}b(g) = 0,\\
\sum_{g \in G}b(gh)b(g)^{-1} = \delta_{h,0} - \frac{1}{d},
\end{gather*}
\noindent where $d^2 = nd + n$. Let $\mathcal{L}_{2n}$ be the Leavitt algebra with generators $\{s_g, t_h\}_{g,h \in G} \sqcup \{s_g', t_h'\}_{g,h \in G}$. We define an action $\alpha : G \curvearrowright \mathcal{L}_{2n}$ and a representation $U : G \to \mathcal{L}_{2n}$ by
\begin{gather*}
\alpha_g(s_h) \coloneqq s_{gh},\qquad \alpha_g(t_h) \coloneqq \langle g, h\rangle t_h,\\
U(g) \coloneqq \sum_{h \in G}\langle g, h\rangle s_hs_h' + \sum_{h \in G} t_{hg^{-1}}t_h'.
\end{gather*}
\noindent\textcolor{red}{What do we need to show?}

\newpage

\noindent Determine how $\rho$ and $\alpha_g$ act on the generators!\\

\newpage

%\noindent Indeed, suppose we fix a non-zero morphism $s_e \in \homset_{\textcat{End}(\mathcal{L})}(\id, {\rho \otimes \rho})$. We claim that this element admits a left inverse. As an intertwiner, $s_e$ is an element satisfying $s_ex = \rho^2(x)s_e$ for all $x \in \mathcal{L}$. Thus $\mathcal{L}s_e$ is a non-zero two-sided ideal. Because $\mathcal{L} \neq \mathcal{L}_1$ is simple, we must have that $\mathcal{L}s_e = \mathcal{L}$. This means that $1 \in \mathcal{L}s_e$, and hence $s_e$ must admit a left inverse. Denote this by $s_e'$. Fix now a non-zero isomorphism $f_g \in \homset_{\textcat{End}(\mathcal{L})}(\alpha_g \otimes \rho, \rho)$ and let
%\begin{align*}
%\begin{split}
%s_g \coloneqq (f_g \otimes \id_\rho) \circ (\id_{\alpha_g} \otimes s_e) \in \homset_{\textcat{End}(\mathcal{L})}(\alpha_g, {\rho \otimes \rho}),
%\end{split}
%\end{align*}
%\noindent for each $g \in G$. Because $s_e$ admits the left inverse $s_e'$, we have also the left inverse
%\begin{align*}
%\begin{split}
%s_g' \coloneqq (\id_{\alpha_g} \otimes s_e') \circ (f_g^{-1} \otimes \id_\rho)
%\end{split}
%\end{align*}
%\noindent for $s_g$.\\

\newpage

\noindent 

\noindent\textcolor{red}{For each set of fusion rules, find solution set of endomorphisms $\rho$ and $\alpha_g$. Then for each possible solution, show that $\overline{\textcat{End}(\mathcal{A})}_\oplus$ is a fusion category.}
\newpage

\noindent\textcolor{red}{Temporary results.}\\

\noindent Let $\gamma(g) \coloneqq \pi(g)\sigma(g)^{-1}$. If $\gamma(g) = 1$ for all $g \in G$, then
\begin{align*}
\begin{split}
\sum_{g \in G} \pi(g)\sigma(g)^{-1} = \sum_{g \in G} \gamma(g) = n,
\end{split}
\end{align*}
\noindent and moreover $\gamma(g) = 1$ if and only if $\pi = \sigma$. Suppose instead that there exists some $x \in G$ for which $\gamma(x) \neq 1$. Then
\begin{align*}
\begin{split}
\gamma(x)\sum_{g \in G}\gamma(g) = \sum_{g \in G}\gamma(xg) = \sum_{g \in G} \gamma_{h,k}(g),
\end{split}
\end{align*}
\noindent whence
\begin{align*}
\begin{split}
(\gamma_{h,k}(x) - 1)\sum_{g \in G}\gamma_{h,k}(g) = 0.
\end{split}
\end{align*}
\noindent Since $\gamma_{h,k}(x) \neq 1$, and assuming that $\gamma_{h,k}(x) - 1$ is invertible in $\mathbbm{k}$, the sum must be zero. Thus
\begin{align*}
\begin{split}
\sum_{g \in G}\chi_h(g)\chi_k(g)^{-1} = n\delta_{\chi_h,\chi_k}.
\end{split}
\end{align*}

\noindent\textcolor{red}{Part 2:}

\noindent Let $\pi : G \to \textup{GL}(V)$ and $\sigma : G \to \textup{GL}(W)$ be irreducible representations of a finite group $G$. Define
\begin{align*}
\begin{split}
\psi \coloneqq \sum_{g \in G} \pi(g)\sigma(g)^{-1}.
\end{split}
\end{align*}
\noindent Then for any $h \in G$, we have that
\begin{align*}
\begin{split}
\pi(h)\psi\sigma(h)^{-1} = \sum_{g \in G} \pi(hg)\sigma(hg)^{-1} = \psi.
\end{split}
\end{align*}
\noindent In other words, $\psi$ intertwines $\pi$ and $\sigma$. Provided $\mathbbm{k}$ is algebraically closed, Schur's lemma implies that $\psi = \lambda\delta_{\pi,\sigma}$ for some $\lambda \in \mathbbm{k}$. Finally, since $n = \textup{Tr}(\psi) = \lambda\dim(\pi)$ \textcolor{red}{(by prev result)}, it follows that
\begin{align*}
\begin{split}
\sum_{g \in G} \pi(g)\sigma(g)^{-1} = \frac{n}{\dim(\pi)}\delta_{\pi,\sigma}.
\end{split}
\end{align*}

\newpage

\ruledsection{Appendix}{8}
%\noindent\\ .\\

\noindent\begin{proposition}\label{CommutingInvertibles} Let $\mathcal{C}$ be a $\mathbbm{k}$-linear fusion category with group $G$ of invertible objects, and suppose there exists a simple object $X \in \obset(\mathcal{C})$ such that $g \otimes X \cong X$ for all $g \in G$. Then the full Abelian monoidal subcategory generated by $G$ is monoidally equivalent to $\textcat{Vec}_G$ with trivial associativity.\\
\end{proposition}

\noindent\begin{proof} Let $\mathcal{D}$ be the subcategory generated by $G$. This category is easily seen to be fusion, and is hence equivalent to $\textcat{Vec}_G$ on the level of fusion rings. It thus suffices to show that the associativity is trivial. We first choose isomorphisms $f_g \in \homset_\mathcal{C}(g \otimes X, X)$ and $\mu_{g,h}' \in \homset_\mathcal{C}(g \otimes h, gh)$ for all $g, h \in G$. Because $\homset_\mathcal{C}((g \otimes h) \otimes X, gh \otimes X)$ is $1$-dimensional, there exists some $z_{g,h} \in \mathbbm{k}^\times$ for which
\begin{align*}
\begin{split}
z_{g,h}\mu_{g,h}' \otimes \id_X = z_{g,h}(\mu_{g,h}' \otimes \id_X) = f_{gh}^{-1} \circ f_g \circ (\id_g \otimes f_h) \circ \alpha_{g,h,X}.
\end{split}
\end{align*}
\noindent That is, for each pair $g, h \in G$, there is a unique isomorphism $\mu_{g,h} \coloneqq z_{g,h}\mu_{g,h}'$ for which the diagram
\begin{center}
\begin{tikzcd}[/tikz/column 1/.style={column sep = 1.5cm}]
(g \otimes h) \otimes X \arrow[dd, "\alpha_{g,h,X}"'] \arrow[r, "\mu_{g,h} \otimes \id_X"] & gh \otimes X \arrow[dr, "f_{gh}"] \\
& & X\\
g \otimes (h \otimes X) \arrow[r, "\id_g \otimes f_h"'] & g \otimes X \arrow[ur, "f_g"']
\end{tikzcd}
\end{center}
%\begin{center}
%\begin{tikzcd}
% & (g \otimes h) \otimes X\arrow[ld, "\alpha_{g,h,X}"']\arrow[rd, "\mu_{g,h} \otimes \id_X"] &\\
%g \otimes (h \otimes X)\arrow[d, "\id_g \otimes f_{h}"'] & & gh \otimes X\arrow[d, "f_{gh}"]\\
%g \otimes X\arrow[rr, "f_g"'] & & X
%\end{tikzcd}
%\end{center}
\noindent commutes. From this, we obtain the commutative diagram
\begin{center}
\begin{tikzcd}[/tikz/column 1/.style={column sep = -4.0em}, /tikz/column 1/.style={column sep = -4.0em}, /tikz/column 2/.style={column sep = 2.0em}, /tikz/column 3/.style={column sep = 2.0em}, /tikz/column 4/.style={column sep = -1.0em}, /tikz/column 5/.style={column sep = -1.0em}, row sep = 2.0em]
{((g \otimes h) \otimes k) \otimes X} \arrow[dr, "{\alpha_{g \otimes h,k,X}}"] \arrow[rrrr, "{(\mu_{g,h} \otimes \id_k) \otimes \id_X}"] \arrow[dddddd, "{\alpha_{g,h,k} \otimes \id_X}"'] & & & & {(gh \otimes k) \otimes X} \arrow[dl, "{\alpha_{gh,k,X}}"'] \arrow[dddr, "{\mu_{gh,k} \otimes \id_X}"] \\
& {(g \otimes h) \otimes (k \otimes X)} \arrow[dd, "{\alpha_{g,h,k \otimes X}}"] \arrow[dr, "{\id_{g \otimes h} \otimes f_k}"] \arrow[rr, "{\mu_{g,h} \otimes \id_{k \otimes X}}"] & & {gh \otimes (k \otimes X)} \arrow[d, "{\id_{gh} \otimes f_k}"'] \\
& & {(g \otimes h) \otimes X} \arrow[dd, "{\alpha_{g,h,X}}"] \arrow[r, "{\mu_{g,h} \otimes \id_X}"'] & {gh \otimes X} \arrow[dr, "{f_{gh}}"'] \\
& {g \otimes (h \otimes (k \otimes X))} \arrow[dr, "{\id_g \otimes (\id_h \otimes f_k)}"] & & & {X} & {ghk \otimes X} \arrow[l, "{f_{ghk}}"'] \\
& & {g \otimes (h \otimes X)} \arrow[r, "{\id_g \otimes f_h}"] & {g \otimes X} \arrow[ur, "{f_g}"] \\
& {g \otimes ((h \otimes k) \otimes X)} \arrow[uu, "{\id_g \otimes \alpha_{h,k,X}}"'] \arrow[rr, "{\id_g \otimes (\mu_{h,k} \otimes \id_X)}"'] & & {g \otimes (hk \otimes X)} \arrow[u, "{\id_g \otimes f_{hk}}"] \\
{(g \otimes (h \otimes k)) \otimes X} \arrow[rrrr, "{(\id_g \otimes \mu_{h,k}) \otimes \id_X}"'] \arrow[ur, "{\alpha_{g,h \otimes k,X}}"'] & & & & {(g \otimes hk) \otimes X} \arrow[uuur, "{\mu_{g,hk} \otimes \id_X}"'] \arrow[ul, "{\alpha_{g,hk,X}}"]
\end{tikzcd}.
\end{center}
%\begin{center}
%\begin{tikzcd}[/tikz/column 1/.style={column sep = -1.75cm}, /tikz/column 2/.style={column sep = -1.75cm}, /tikz/column 3/.style={column sep = -2cm}, /tikz/column 4/.style={column sep = -0.75cm}, /tikz/column 5/.style={column sep = -0.3cm}, /tikz/column 6/.style={column sep = -1cm}, /tikz/column 7/.style={column sep = 1cm}, row sep = 0.5cm]
% & & & & ((g \otimes h) \otimes k) \otimes X\arrow[dllll, "\alpha_{g,h,k} \otimes \id_X"']\arrow[dd, "\alpha_{g \otimes h, k, X}"]\arrow[drrr, "(\mu_{g,h} \otimes \id_k) \otimes \id_X)"] & & &\\
% (g \otimes (h \otimes k)) \otimes X\arrow[dr, "\alpha_{g, h\otimes k, X}"]\arrow[dddddd, "(\id_g \otimes \mu_{h,k}) \otimes \id_X"'] & & & & & & & (gh \otimes k) \otimes X\arrow[ddl, "\alpha_{gh,k,X}"']\arrow[dddddd, "\mu_{gh,k} \otimes \id_X"]\\
% & g \otimes ((h \otimes k) \otimes X)\arrow[dr, "\id_g\otimes\alpha_{h,k,X}"]\arrow[dddd, "\id_g\otimes(\mu_{h,k} \otimes \id_X)"  {yshift=-1.125cm}] & & & (g \otimes h) \otimes (k \otimes X)\arrow[dll, "\alpha_{g,h,k\otimes X}"]\arrow[d, "\id_{g\otimes h} \otimes f_k"]\arrow[drr, "\mu_{g,h} \otimes \id_{k\otimes X}"] & & &\\
% & & g \otimes (h \otimes (k \otimes X))\arrow[dr, "\id_g \otimes (\id_h \otimes f_k)"'] & & (g \otimes h) \otimes X\arrow[dl, "\alpha_{g,h,X}"']\arrow[dr, "\mu_{g,h} \otimes \id_X"] & & gh \otimes (k \otimes X)\arrow[dl, "\id_{gh} \otimes f_k"] &\\
% & & & g \otimes (h \otimes X)\arrow[d, "\id_g \otimes f_{h}"'] & & gh \otimes X\arrow[d, "f_{gh}"] & &\\
% & & & g \otimes X\arrow[rr, "f_g"'] & & X & &\\
% & g \otimes (hk \otimes X)\arrow[urr, "\alpha_{g,hk,X}"'] & & & & & &\\
% (g \otimes hk) \otimes X\arrow[ur]\arrow[rrrrrrr, "\mu_{g,hk} \otimes \id_X"'] & & & & & & & ghk \otimes X\arrow[uull, "f_{ghk}"']
%\end{tikzcd}.
%\end{center}
%\begin{center}
%\begin{tikzcd}[/tikz/column 1/.style={column sep = -1.75cm}, /tikz/column 2/.style={column sep = -1.75cm}, /tikz/column 3/.style={column sep = -2cm}, /tikz/column 4/.style={column sep = -0.75cm}, /tikz/column 5/.style={column sep = -0.5cm}, /tikz/column 6/.style={column sep = -1cm}, /tikz/column 7/.style={column sep = 1cm}]
% & & & & ((g \otimes h) \otimes k) \otimes X\arrow[dllll]\arrow[dd]\arrow[drrr] & & &\\
% (g \otimes (h \otimes k)) \otimes X\arrow[dr]\arrow[dddddd] & & & & & & & (gh \otimes k) \otimes X\arrow[ddl]\arrow[dddddd]\\
% & g \otimes ((h \otimes k) \otimes X)\arrow[dr]\arrow[dddd] & & & (g \otimes h) \otimes (k \otimes X)\arrow[dll]\arrow[d]\arrow[drr] & & &\\
% & & g \otimes (h \otimes (k \otimes X))\arrow[dr] & & (g \otimes h) \otimes X\arrow[dl]\arrow[dr] & & gh \otimes (k \otimes X)\arrow[dl] &\\
% & & & g \otimes (h \otimes X)\arrow[d] & & gh \otimes X\arrow[d] & &\\
% & & & g \otimes X\arrow[rr] & & X & &\\
% & g \otimes (hk \otimes X)\arrow[urr] & & & & & &\\
% (g \otimes hk) \otimes X\arrow[ur]\arrow[rrrrrrr] & & & & & & & ghk \otimes X\arrow[uull]\\
%\end{tikzcd}
%\end{center}
\newpage
\noindent Note that the leftmost pentagon is the pentagon diagram for the associator, while all other pentagons are versions of the pentagon given at the beginning of the proof. The upper middle quadrilateral comes from the functoriality of $\otimes$, with the remaining quadrilaterals come from the naturality of $\alpha$. If we look at the outermost pentagon, we obtain the commutative diagram
\begin{center}
\begin{tikzcd}[/tikz/column 1/.style={column sep = 1.5cm}]
(g \otimes h) \otimes k \arrow[dd, "\alpha_{g,h,k}"'] \arrow[r, "\mu_{g,h} \otimes \id_k"] & gh \otimes k \arrow[dr, "\mu_{gh,k}"] \\
& & ghk\\
g \otimes (h \otimes k) \arrow[r, "\id_g \otimes \mu_{h,k}"'] & g \otimes hk \arrow[ur, "\mu_{g,hk}"']
\end{tikzcd}.
\end{center}
%\begin{center}
%\begin{tikzcd}
% & (g \otimes h) \otimes k\arrow[ld, "\alpha_{g,h,k}"']\arrow[rd, "\mu_{g,h} \otimes \id_k"] &\\
%g \otimes (h \otimes k)\arrow[d, "\id_g \otimes \mu_{h,k}"'] & & gh \otimes k\arrow[d, "\mu_{gh,k}"]\\
%g \otimes hk\arrow[rr, "\mu_{g,hk}"'] & & ghk
%\end{tikzcd}.
%\end{center}
\noindent Thus the associator for $\mathcal{D}$ is given by
\begin{align*}
\begin{split}
\alpha_{g,h,k} = (\id_g \otimes \mu_{h,k}^{-1}) \circ \mu_{g,hk}^{-1} \circ \mu_{gh,k} \circ (\mu_{g,h} \otimes \id_k).
\end{split}
\end{align*}
\noindent We claim that $\alpha$ is cohomologous to the trivial $3$-cocycle. Indeed, let $\alpha^1_{g,h,k}$ denote the components of the trivial associator in $\mathcal{D}$, for $g, h, k \in G$. This morphism factors as
\begin{align*}
\begin{split}
\alpha^1_{g,h,k} = (\id_g \otimes \beta_{h,k}^{-1}) \circ \beta_{g,hk}^{-1} \circ \beta_{gh,k} \circ (\beta_{g,h} \otimes \id_k),
\end{split}
\end{align*}
\noindent where we define $\beta_{g,h} \in \homset_\mathcal{C}(g\otimes h, gh)$ by $\beta_{g,h}(u \otimes v) \coloneqq uv$ for all $g, h \in G$. By linearity, there exists a $3$-cochain $a : G^2 \to \mathbbm{k}^\times$ such that $\alpha_{g,h,k} = a(g,h,k)\alpha^1_{g,h,k}$. Moreover, for all $g, h \in G$ there exists a $2$-cochain $b : G^2 \to \mathbbm{k}^\times$ satisfying $\mu_{g,h} = b(g, h)\beta_{g,h}$. It follows that
\begin{align*}
\begin{split}
a(g,h,k) = b(g,h)^{-1}b(gh,k)b(g,hk)^{-1}b(g,h).
\end{split}
\end{align*}
\noindent In particular, it is clear that $a^{-1} = d^2(b)$, where $d^2$ is the second differential, implying that $a^{-1}$ and hence $a$ are $3$-coboundaries. The claim follows, with the classification of monoidal structures on $\textcat{Vec}_G$ (\cite[Proposition 2.6.1]{EGNO16}) showing that $\mathcal{D}$ is monoidally equivalent to $\textcat{Vec}_G$ with trivial associativity. This completes the proof.
\end{proof}\\

\noindent\begin{lemma}\label{SelfDual} Let $\mathcal{C}$ be a rigid category with simple object $X$ isomorphic to its dual. Then we may endow $\mathcal{C}$ with a rigid structure such that $X$ is self-dual.\\
\end{lemma}

\noindent\begin{proof} We will consider the left duals, but note that the following proof holds similarly for right duals. Let $f_X : X \to X^*$ be an isomorphism from $X$ to its left dual. We define a new evaluator and a new coevaluator by
\begin{align*}
\begin{split}
\overline{\textup{ev}}_X \coloneqq \textup{ev}_X \circ (f_X \otimes \id_X)\qquad\text{and}\qquad\overline{\textup{coev}}_X \coloneqq (\id_X \otimes f_X^{-1}) \circ \textup{coev}_X,
\end{split}
\end{align*}
\noindent respectively. This gives $X$ the structure of a left dual to itself. Moreover, the left dual $f_X^* : X^{**} \to X^*$ allows us to give $X^*$ the structure of a left dual to itself by defining
\begin{align*}
\begin{split}
\overline{\textup{ev}}_{X^*} \coloneqq \textup{ev}_{X^*} \circ ({f_X^*}^{-1} \otimes \id_{X^*})\qquad\text{and}\qquad\overline{\textup{coev}}_{X^*} \coloneqq (\id_{X^*} \otimes f_X^*) \circ \textup{coev}_{X^*}.
\end{split}
\end{align*}
\noindent In particular, these are consistent in the sense that one can show (with some effort) that
\begin{align*}
\begin{split}
\overline{\textup{ev}}_{X^*}\circ (f_X \otimes f_X) = \overline{\textup{ev}}_X \qquad\text{and}\qquad(f_X^{-1} \otimes f_X^{-1}) \circ \overline{\textup{coev}}_{X^*} = \overline{\textup{coev}}_X.
\end{split}
\end{align*}
\noindent This completes the proof.
\end{proof}\\

\noindent\begin{remark}\label{DimensionFrobeniusSchur} Let $\mathcal{C}$ be a fusion category with simple object $X$ isomorphic to its dual. Let's write down the dimension of $X$. By definition,
\begin{align*}
\begin{split}
\textup{Dim}(X) \coloneqq \ev_{X^{*}} \circ (\psi_X \otimes \id_{X^{*}}) \circ \coev_X.
\end{split}
\end{align*}
\noindent Under \hyperref[SelfDual]{Lemma \ref*{SelfDual}}, we can rewrite this in terms of the new evaluator and coevaluator as
\begin{align*}
\begin{split}
\textup{Dim}(X) &= \overline{\ev}_{X^{*}} \circ (f_X^* \otimes \id_{X^*}) \circ (\psi_X \otimes \id_{X^{*}}) \circ (\id_X \otimes f_X) \circ \overline{\coev}_X\\
&= \overline{\ev}_X \circ (f_X^{-1} \otimes f_X^{-1}) \circ (f_X^* \otimes \id_{X^*}) \circ (\psi_X \otimes \id_{X^{*}}) \circ (\id_X \otimes f_X) \circ \overline{\coev}_X.
\end{split}
\end{align*}
\noindent Consider the map $r_X : f \mapsto f^* \circ \psi_X$ in $\textup{End}(\homset_\mathcal{C}(X, X^*))$. Because $\homset_\mathcal{C}(X, X^*)$ is $1$-dimensional, the aforementioned endomorphism space is not only $1$-dimensional but admits a canonical basis given by $\id_{\homset_\mathcal{C}(X, X^*)} \mapsto \id_{\homset_\mathcal{C}(X, X^*)}$. In other words, $r_X$ is multiplication by a scalar. Since $r_X^2(f) = f$, it follows that $r_X(f) = \nu f$, for $\nu \in \{-1, 1\}$. This is precisely the (second) Frobenius--Schur indicator of $X$. Returning to the dimension, it follows that
\begin{align*}
\begin{split}
\textup{Dim}(X) &= \overline{\ev}_X \circ (f_X^{-1} \otimes f_X^{-1}) \circ (f_X^* \otimes \id_{X^*}) \circ (\psi_X \otimes \id_{X^{*}}) \circ (\id_X \otimes f_X) \circ \overline{\coev}_X\\
&= \nu\ \overline{\ev}_X \circ (f_X^{-1} \otimes f_X^{-1}) \circ (f_X \otimes f_X) \circ \overline{\coev}_X\\
&= \nu\ \overline{\ev}_X \circ \overline{\coev}_X\\
&= \overline{\ev}_X \circ (\overline{\psi}_X \otimes \id_X) \circ \overline{\coev}_X.
\end{split}
\end{align*}
\noindent This shows us that a side effect of taking $X$ to be self-dual is that the new pivotal structure $\overline{\psi}_X : X \to X$ becomes multiplication by the Frobenius--Schur indicator.\\
\end{remark}

\noindent\begin{theorem}\label{QuotientAlgebraObject} Let $\mathcal{C}$ be a fusion category with $\mathcal{D}$ a fusion subcategory, let $A$ be an algebra object in $\mathcal{C}$ and let $B$ be the restriction of $A$ to $\mathcal{D}$, given by throwing away simple summands not in $\mathcal{D}$. Then
\begin{enumerate}[label=\textup{(\roman*).}, ref=\textup{(\roman*)}, leftmargin=4\parindent]
\item $B$ is an algebra object in $\mathcal{C}$;
\item $B$ is a subalgebra object of $A$ (in some sense);
\item $A$ is a $(B, A)$-bimodule object by restriction;
\item there is a factoring of bimodule objects ${_\mathbbm{1}}A_A = {_\mathbbm{1}}B_B \otimes_B {_B}A_A$;
\item the objects ${_A}(A^*)_B \otimes_B {_B}A_A$ and ${_B}A_A \otimes_A {_A}(A^*)_B = {_B}(A^*)_B$ are algebra objects in the categories ${_A}\textcat{Mod}_A(\mathcal{C})$ and ${_B}\textcat{Mod}_B(\mathcal{C})$, respectively.\\
\end{enumerate}
\end{theorem}

\noindent\begin{proof} \textcolor{red}{Talk to Tom about the proof for this.}
\end{proof}\\

\noindent\textcolor{red}{Add citation for Tom's notes.}

\newpage

%\noindent Let $\mathcal{C}$ be a strict near-group category with multiplicity $m$, whose invertible objects form the finite group $G$ of order $n$. The simple objects $\{X\} \sqcup \{g\}_{g \in G}$ obey the fusion rules
%\begin{gather*}
%g \otimes h \cong gh,\\
%g \otimes X \cong X \otimes g \cong X,\\
%X \otimes X \cong \bigoplus_{g \in G} g \oplus X^{\oplus m}.
%\end{gather*}
%\noindent By \hyperref[CommutingInvertibles]{Proposition \ref*{CommutingInvertibles}}, we can take $g \otimes h = gh$ for all $g, h \in G$. The third equation above tells us, by definition of the direct sum, that we must have $s_g \in \homset_\mathcal{C}(g, X \otimes X)$, $s_g' \in \homset_\mathcal{C}(X \otimes X, g)$, $t_i \in \homset_\mathcal{C}(X, X \otimes X)$, $t_i'\in \homset_\mathcal{C}(X \otimes X, X)$ such that
%\begin{align*}
%\begin{split}
%\bigoplus_{g \in G}{s_g \circ s_g'} \oplus \bigoplus_{i=1}^m{t_i \circ t_i'} = \mathbbm{1},\qquad s_g' \circ s_h = \delta_{g,h},\qquad t_i' \circ t_j = \delta_{i,j},\qquad s_g' \circ t_i = 0 = t_i'\circ s_g,
%\end{split}
%\end{align*}
%\noindent for all $g, h \in G$ and $i, j \in \{1, \dots, m\}$. We would like to make a special choice for each $s_g$. Suppose we choose any non-zero $s_e \in \homset_\mathcal{C}(\mathbbm{1}, X \otimes X)$. Because $\homset_\mathcal{C}(\mathbbm{1}, X \otimes X)$ is $1$-dimensional, this is going to be a linear scaling of some injection satisfying the direct sum properties. Let its left inverse be $s_e'$, and choose isomorphisms $f_g \in \homset_\mathcal{C}(g \otimes X, X)$ for all $g \in G$. For each $g \in G$, we define
%\begin{align*}
%\begin{split}
%s_g \coloneqq (f_g \otimes \id_X) \circ (\id_g \otimes s_e) \circ \rho_g^{-1},
%\end{split}
%\end{align*}
%\noindent along with its left inverse
%\begin{align*}
%\begin{split}
%s_g' \coloneqq \rho_g \circ (\id_g \otimes s_e') \circ (f_g^{-1} \otimes \id_X).
%\end{split}
%\end{align*}
%\noindent Once again, because each $\homset_\mathcal{C}(g, X \otimes X)$ is $1$-dimensional, these satisfy the direct sum properties above.

%\newpage

%\noindent\textcolor{red}{Stuff below is garbage.}

%\newpage

\newpage

\renewcommand\thesection{R}
\ruledsectionstar{References}{References}
\begingroup
\setlength{\emergencystretch}{.5em}
\printbibliography[heading=none]
\endgroup
\newpage

\end{document}